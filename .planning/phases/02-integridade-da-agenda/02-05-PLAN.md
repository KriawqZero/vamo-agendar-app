---
phase: 02-integridade-da-agenda
plan: 05
type: execute
wave: 3
depends_on: ['02-02', '02-03', '02-04']
files_modified:
  - supabase/migrations/20260723153155_integridade_agenda.sql
autonomous: false
requirements: [AGE-01, AGE-03, AGE-05]
must_haves:
  truths:
    - "[BLOCKING] O apply usa o caminho REAL do projeto: mcp__supabase__execute_sql (DDL + INSERT no ledger na MESMA chamada), NUNCA apply_migration nem `supabase db push` (CLI não autenticado)."
    - "Após o apply, o ledger é realinhado por DML (version = timestamp do arquivo, name = parte descritiva) e conferido por mcp__supabase__list_migrations — apply_migration/execute_sql não preservam a version do arquivo."
    - "O pré-voo obrigatório (duplicatas (tenant_id, telefone) + sobreposições ativas pós-backfill) roda ANTES do DDL da constraint e retorna ZERO — registrado no plano (Regra transversal de aceite). D-06 limpou os agendamentos de teste, então o pré-voo volta vazio."
    - "D-06: os agendamentos de teste são apagados (preservando o tenant do owner e a estrutura/perfil) para aplicar data_hora_fim NOT NULL + exclusion + unique em terreno limpo; a migration mantém o passo de backfill porque é a mesma que roda em produção no go-live."
    - "NOT VALID não existe para EXCLUDE — a constraint só é criada em terreno limpo; a ordem é coluna nullable → backfill → NOT NULL → CHECK → extension → periodo → EXCLUDE → UNIQUE → RPC → GRANTs."
  artifacts:
    - "Banco Supabase Cloud de dev com data_hora_fim NOT NULL, periodo, ag_sem_sobreposicao, clientes_tenant_telefone_key, a função reaproveitar_ou_criar_cliente e o btree_gist aplicados"
    - "Uma linha no ledger supabase_migrations.schema_migrations com version = timestamp do arquivo de migration"
  key_links:
    - "Build e typecheck passam SEM o apply (o projeto não tem tipos gerados do banco) — por isso o apply é obrigatório antes da verificação: sem ele a constraint/coluna/função não existem de verdade e a prova de concorrência (02-06) seria falso-verde."
  prohibitions:
    - "NÃO usar mcp__supabase__apply_migration (não preserva version) nem `supabase db push` (CLI não autenticado neste projeto)."
    - "NÃO aplicar a constraint com dados sujos — o pré-voo tem de voltar ZERO antes do DDL do EXCLUDE/UNIQUE."
    - "A limpeza D-06 NÃO pode tocar o tenant do owner nem a estrutura/perfis — só os agendamentos de teste (alinhado a OPE-04)."
    - "service_role NUNCA em linha de REVOKE do GRANT da função."
---

<objective>
[BLOCKING] Aplicar a migration de integridade ao Supabase Cloud pelo caminho real
do projeto (MCP execute_sql + realinhamento do ledger), depois do pré-voo
obrigatório e da limpeza dos agendamentos de teste (D-06). Este é o passo que
torna a constraint/coluna/função REAIS — build e typecheck passam sem ele, criando
um falso-verde onde nada existe no banco.

Purpose: fecha a metade de banco de AGE-01/AGE-03/AGE-05 em terreno limpo,
preservando a migration correta (com backfill) que roda em produção no go-live
(Phase 11 / OPE-04). Gated por checkpoint de reversibilidade porque duas decisões
são one-way: a limpeza destrutiva (D-06) e o data_hora_fim NOT NULL (D-02).

Output: banco de dev migrado, ledger realinhado e conferido, pré-voo registrado
com zeros.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-integridade-da-agenda/02-CONTEXT.md
@.planning/phases/02-integridade-da-agenda/02-RESEARCH.md
@.planning/phases/02-integridade-da-agenda/02-02-SUMMARY.md
@docs/SUPABASE_DECLARATIVE-DATABASE-SCHEMA.md
@CLAUDE.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <decision>Autorizar a aplicação da migration de integridade ao Supabase Cloud de dev, incluindo duas ações ONE-WAY: (D-06) apagar os agendamentos de teste e (D-02) tornar data_hora_fim NOT NULL.</decision>
  <context>
    O banco de dev é descartável e migration destrutiva está autorizada (ROADMAP §"Rede de proteção do banco"), mas duas decisões desta wave são irreversíveis: os agendamentos de teste apagados NÃO voltam (dados descartáveis, aceito) e o data_hora_fim NOT NULL vira migration imutável após a Phase 11. O apply usa MCP execute_sql (não apply_migration) e realinha o ledger em seguida. O pré-voo obrigatório será rodado e registrado antes do DDL da constraint.
  </context>
  <options>
    <option id="prosseguir">
      <name>Prosseguir com o apply (limpar agendamentos de teste + NOT NULL + constraint)</name>
      <pros>Terreno limpo, pré-voo volta zero, constraint aplica sem violadoras; caminho alinhado a OPE-04 (produção começa limpa).</pros>
      <cons>Agendamentos de teste do dev são perdidos (aceito — descartáveis); NOT NULL é one-way após Phase 11.</cons>
    </option>
    <option id="abortar">
      <name>Abortar e revisar a migration/pré-voo antes de aplicar</name>
      <pros>Nenhuma ação irreversível até a revisão.</pros>
      <cons>A fase não avança; a prova de concorrência (02-06) fica bloqueada.</cons>
    </option>
  </options>
  <resume-signal>Selecione: prosseguir ou abortar</resume-signal>
</task>

<task type="auto" gate="blocking-human">
  <name>Task 2: Pré-voo obrigatório + limpeza D-06 (via MCP)</name>
  <files>supabase/migrations/20260723153155_integridade_agenda.sql</files>
  <read_first>
    - .planning/phases/02-integridade-da-agenda/02-RESEARCH.md §"Code Examples" (as duas consultas de pré-voo) e §"Anti-Patterns" (constraint sobre dado sujo)
    - supabase/migrations/20260722185755_slug_gratuito_unico.sql:38-43 (precedente: pré-voo registrado no arquivo, "as duas consultas voltaram VAZIAS")
    - .planning/phases/02-integridade-da-agenda/02-CONTEXT.md §D-06 (limpar agendamentos de teste, preservar o tenant do owner)
  </read_first>
  <action>
    Confirmar a identidade do alvo (é o banco de dev deste projeto) antes de
    qualquer escrita. Executar via mcp__supabase__execute_sql, na ordem: (1) limpar
    os agendamentos de teste — DELETE FROM agendamentos filtrando pelos tenants de
    teste, NUNCA o tenant do owner nem a estrutura/perfis (D-06; alinhado a OPE-04);
    limpar também duplicatas de clientes de teste se existirem. (2) Rodar as DUAS
    consultas de pré-voo e registrar as contagens (que devem ser ZERO) no SUMMARY:
    (a) grupos `(tenant_id, telefone)` com count>1 em clientes; (b) self-join de
    sobreposições ativas com `&&` de tstzrange e `status <> 'cancelado'`, `a.id <
    b.id` — esta segunda só é significativa depois que data_hora_fim exista/for
    backfillado, então rodá-la contra o estado pós-limpeza (tabela sem
    agendamentos de teste → vazia). Registrar as duas contagens = 0 no cabeçalho da
    migration e no SUMMARY. Se qualquer contagem for > 0, PARAR e resolver antes do
    apply (a constraint não tem NOT VALID).
  </action>
  <verify>
    <automated>MISSING — pré-voo é consulta via MCP; a evidência é as duas contagens = 0 registradas no SUMMARY e no cabeçalho da migration.</automated>
  </verify>
  <acceptance_criteria>
    - A identidade do alvo foi confirmada antes de qualquer DELETE.
    - As duas contagens de pré-voo estão registradas como 0 no SUMMARY (e refletidas no cabeçalho da migration).
    - Nenhum agendamento/perfil do tenant do owner foi tocado (só tenants de teste).
  </acceptance_criteria>
  <done>Terreno limpo e pré-voo registrado com zeros — a constraint pode ser criada sem violadoras.</done>
  <reversibility rating="one-way" >D-06: agendamentos de teste apagados não voltam (aceito — descartáveis); precedido pelo checkpoint de decisão.</reversibility>
</task>

<task type="auto" gate="blocking-human">
  <name>Task 3: Aplicar a migration via execute_sql + realinhar o ledger</name>
  <files>supabase/migrations/20260723153155_integridade_agenda.sql</files>
  <read_first>
    - .planning/phases/02-integridade-da-agenda/02-RESEARCH.md §"Pitfall 5" (ledger desalinhado) e §"Anti-Patterns" (apply_migration proibido)
    - CLAUDE.md §"Infraestrutura" / §"Banco de dados: schema declarativo" (execute_sql + INSERT no ledger na mesma transação; realinhar version/name)
    - .planning/phases/02-integridade-da-agenda/02-02-SUMMARY.md (nome/timestamp real do arquivo de migration)
  </read_first>
  <action>
    Aplicar o corpo da migration (a partir do passo ADD COLUMN até os COMMENTs) via
    mcp__supabase__execute_sql, com o `INSERT INTO
    supabase_migrations.schema_migrations (version, name)` na MESMA chamada
    (mesma transação), usando `version` = o timestamp EXATO do nome do arquivo e
    `name` = a parte descritiva (`integridade_agenda`). Se a opclass do btree_gist
    não for achada, prefixar `SET search_path = public, extensions;` (Pitfall 3).
    Em seguida, rodar mcp__supabase__list_migrations e conferir que a version bate
    com o timestamp do arquivo; se o MCP tiver carimbado outra version/name,
    realinhar por DML: `update supabase_migrations.schema_migrations set
    version='<timestamp-do-arquivo>', name='integridade_agenda' where
    version='<a-que-o-mcp-inventou>';`. Confirmar por catálogo que existem:
    data_hora_fim NOT NULL, ck_agendamento_fim_apos_inicio, ag_sem_sobreposicao,
    clientes_tenant_telefone_key, e a função reaproveitar_ou_criar_cliente com
    EXECUTE para authenticated/service_role e NADA para anon (has_function_privilege).
  </action>
  <verify>
    <automated>MISSING — apply via MCP; evidência = list_migrations mostrando a version alinhada + catálogo confirmando a coluna/constraint/função. Registrar a saída no SUMMARY.</automated>
  </verify>
  <acceptance_criteria>
    - list_migrations mostra a linha com version = timestamp do arquivo (ledger alinhado).
    - Catálogo confirma data_hora_fim NOT NULL, ag_sem_sobreposicao, clientes_tenant_telefone_key e a função presente.
    - anon NÃO tem EXECUTE na função; authenticated e service_role têm.
    - Não foi usado apply_migration nem `supabase db push`.
  </acceptance_criteria>
  <done>A integridade está aplicada e verdadeira no banco de dev, com o ledger realinhado e conferido.</done>
  <reversibility rating="one-way">D-02: data_hora_fim NOT NULL vira migration imutável após Phase 11; precedido pelo checkpoint de decisão.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planner/executor → Supabase Cloud (service_role/MCP) | DDL destrutivo com privilégio total; a identidade do alvo é conferida antes de qualquer escrita. |
| integridade → RLS | A constraint bypassa RLS por design; o isolamento vive no `tenant_id WITH =`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-05-01 | Tampering | DELETE mirando o banco errado / tenant do owner | high | mitigate | Confirmar identidade do alvo antes; filtrar só tenants de teste; checkpoint de decisão gating. |
| T-02-05-02 | Repudiation | Ledger desalinhado quebra db diff futuro | medium | mitigate | INSERT no ledger na mesma transação + realinhamento por DML + list_migrations. |
| T-02-05-03 | Elevation of Privilege | Função nasce acessível a anon | high | mitigate | GRANT explícito só a authenticated/service_role; verificação por has_function_privilege. |
| T-02-05-04 | Information Disclosure | Constraint sem tenant_id permite mapear agenda | high | mitigate | ag_sem_sobreposicao com `tenant_id WITH =` (autorado no 02-02, conferido aqui). |
| T-02-05-SC | Tampering | npm/pip/cargo installs | low | accept | Nenhum pacote instalado (só DDL). |
</threat_model>

<verification>
- Pré-voo registrado com zeros; ledger alinhado por list_migrations; catálogo confirma coluna/constraint/função/privilégio.
- Nenhum uso de apply_migration/db push.
</verification>

<success_criteria>
Migration aplicada em terreno limpo pelo caminho real do projeto, ledger
realinhado e conferido, privilégio da função correto.
</success_criteria>

<output>
Create `.planning/phases/02-integridade-da-agenda/02-05-SUMMARY.md` when done
</output>
