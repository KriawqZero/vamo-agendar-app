---
phase: 02-integridade-da-agenda
plan: 04
type: execute
wave: 2
depends_on: ['02-01', '02-02']
files_modified:
  - src/app/actions/agendamentos.ts
  - src/app/dashboard/NovoAgendamentoModal.tsx
autonomous: true
requirements: [AGE-01, AGE-03, AGE-04, AGE-05]
must_haves:
  truths:
    - "SC3/SC4 walk-in (AGE-03/AGE-04, D-04): quando o INSERT do walk-in falha com 23P01, a action busca o agendamento conflitante do PRÓPRIO tenant (período sobreposto, status <> 'cancelado') e devolve { ok:false, motivo:'slot_ocupado', conflito:{cliente,servico,horario} }; a UI mostra o aviso COM detalhe e recarrega a agenda."
    - "AGE-01/D-02: criarAgendamentoManual grava data_hora_fim (= data_hora + servico.duracao_minutos) no INSERT."
    - "AGE-05/D-01: o ramo 'cadastro por telefone' do walk-in usa a MESMA RPC reaproveitar_ou_criar_cliente (o unique pode fazer o .insert() solto falhar com 23505 numa corrida; a RPC resolve atomicamente)."
    - "D-03: remarcarAgendamento mantém a duração ORIGINAL reservada — novo data_hora_fim = nova data_hora + (data_hora_fim − data_hora) original; o UPDATE grava data_hora E data_hora_fim; o 23P01 na remarcação também vira slot_ocupado."
    - "É legítimo mostrar o detalhe no walk-in porque é a agenda do próprio profissional (RLS já escopa o SELECT ao tenant) — nunca a mensagem crua do PostgreSQL."
  artifacts:
    - "src/app/actions/agendamentos.ts com data_hora_fim no INSERT, RPC no cadastro por telefone, retorno discriminado slot_ocupado (23P01) em criarAgendamentoManual e remarcarAgendamento, e a duração original congelada na remarcação"
    - "src/app/dashboard/NovoAgendamentoModal.tsx consumindo o retorno discriminado (aviso âmbar com detalhe + refetch da grade), sem string-matching de mensagem"
  key_links:
    - "O único consumidor de criarAgendamentoManual e remarcarAgendamento é NovoAgendamentoModal.tsx (grep confirmado) — o contrato pode mudar sem quebrar outra tela."
    - "O walk-in usa createClient() authenticated: a busca do conflito já é escopada ao próprio tenant pelo RLS — sem risco cross-tenant."
  prohibitions:
    - "O detalhe do conflito no walk-in NÃO pode vazar mais que cliente + serviço + horário do PRÓPRIO tenant — nunca a error.message do Postgres. [D-04]"
    - "O ramo 23P01 do walk-in NÃO pode chamar reportarExcecao — perda de corrida é esperada."
    - "A remarcação NÃO pode pegar a duração VIGENTE do serviço — reabriria parcialmente o bug que a fase fecha. [D-03]"
---

<objective>
Levar a integridade ao fluxo walk-in do dashboard: gravar data_hora_fim, usar a
RPC atômica de cliente, e tratar a perda de corrida (23P01) com um aviso amigável
que mostra o detalhe do agendamento que ocupa o horário (legítimo — é a agenda do
próprio profissional) recarregando a agenda. Congelar a duração ORIGINAL na
remarcação (D-03) e proteger a remarcação com a mesma discriminação de 23P01.

Purpose: fecha SC3/SC4 no walk-in (assimetria B2B/B2C intencional: o profissional
é dono da agenda e pode ver o próprio conteúdo) e o D-03 (remarcar é o mesmo
agendamento em outro horário — o tamanho reservado não muda). Fixa o contrato de
remarcarAgendamento antes da Phase 8, que o reaproveita.

Output: agendamentos.ts com os três comportamentos + NovoAgendamentoModal
consumindo o retorno discriminado.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-integridade-da-agenda/02-CONTEXT.md
@.planning/phases/02-integridade-da-agenda/02-RESEARCH.md
@.planning/phases/02-integridade-da-agenda/02-PATTERNS.md
@.planning/phases/02-integridade-da-agenda/02-02-SUMMARY.md
</context>

<flagged_assumptions>
Sonda de borda sem SPEC devolveu AGE-01/AGE-03/AGE-04/AGE-05 como `unresolved`.
Assunção explícita, derivada do ROADMAP SC3/SC4 + D-03/D-04 (locked): no walk-in
o "perdedor" da corrida é o INSERT/UPDATE que a exclusion constraint recusa com
23P01; o profissional é dono da agenda, então o detalhe do conflito
(cliente/serviço/horário do próprio tenant) É mostrado — a assimetria B2B/B2C é
intencional (o público mostra cópia genérica; o walk-in mostra o detalhe).
</flagged_assumptions>

<!-- planner-discipline-allow: includes('conflita') -->

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: criarAgendamentoManual — data_hora_fim, RPC de cliente e 23P01 → slot_ocupado com detalhe (D-01/D-04)</name>
  <files>src/app/actions/agendamentos.ts</files>
  <read_first>
    - src/app/actions/agendamentos.ts:236-394 (criarAgendamentoManual: guard clauses, cadastro por telefone :299-331, revalidação da engine :334-346, INSERT :348-364)
    - .planning/phases/02-integridade-da-agenda/02-RESEARCH.md §"Pattern 3" (ramo walk-in) e §"Pattern 4" (reuso da RPC no walk-in)
    - .planning/phases/02-integridade-da-agenda/02-PATTERNS.md §"agendamentos.ts" (ramo 23P01, contrato de retorno muda)
    - .planning/phases/02-integridade-da-agenda/02-02-SUMMARY.md (assinatura da RPC)
  </read_first>
  <behavior>
    - Sucesso: retorna { ok:true, agendamento } (contrato discriminado novo).
    - Telefone já cadastrado no tenant reaproveita o cliente pela RPC (uma linha), sem duplicar em corrida.
    - INSERT grava data_hora_fim = data_hora + servico.duracao_minutos.
    - Perda de corrida (23P01) OU revalidação da engine falhando: retorna { ok:false, motivo:'slot_ocupado', conflito:{cliente,servico,horario} } com o agendamento conflitante do próprio tenant; NÃO chama reportarExcecao.
    - As guard clauses de precondição (auth ausente, serviço inválido, cliente não encontrado, horário passado, telefone inválido) permanecem como throw.
  </behavior>
  <action>
    No ramo "cadastro por telefone" (:315-331), trocar o `.insert()` solto de novo
    cliente por `await supabase.rpc('reaproveitar_ou_criar_cliente', { p_tenant_id:
    orgId, p_telefone: telefoneLimpo, p_nome: clienteNome!.trim(), p_email: null })`
    e reler o cliente (id/nome/telefone) para clienteFinal (a RPC roda como INVOKER
    e respeita o RLS do authenticated). No INSERT do agendamento (:348-359),
    acrescentar `data_hora_fim` ao payload (= dataHora + servico.duracao_minutos).
    Converter o retorno da função para discriminado: sucesso vira { ok:true,
    agendamento }. No lugar do `throw new Error('Erro ao criar o agendamento.')`
    (:361-364), inserir o ramo `if (agError?.code === '23P01')` que busca o
    conflitante — `supabase.from('agendamentos').select('data_hora, data_hora_fim,
    clientes(nome), servicos(nome)').eq('tenant_id', orgId).neq('status',
    'cancelado').lt('data_hora', <fim tentado>).gt('data_hora_fim',
    dataHora).maybeSingle()` — e retorna { ok:false, motivo:'slot_ocupado',
    conflito: conflito && { cliente: ...clientes?.nome, servico: ...servicos?.nome,
    horario: ...data_hora } }, sem reportarExcecao. A revalidação da engine
    (:344-346), que hoje faz throw da string de conflito, passa a retornar o mesmo
    { ok:false, motivo:'slot_ocupado', conflito } (reusando a busca do conflitante)
    para unificar a UX. Erro de infra genuíno no INSERT continua com console.error
    + throw (as guard clauses de precondição não mudam). Preservar o
    revalidatePath e o evento manual_booking_created.
  </action>
  <verify>
    <automated>pnpm build && grep -nE "reaproveitar_ou_criar_cliente|data_hora_fim|slot_ocupado|23P01" src/app/actions/agendamentos.ts</automated>
  </verify>
  <acceptance_criteria>
    - criarAgendamentoManual retorna objeto discriminado ({ ok:true, agendamento } | { ok:false, motivo:'slot_ocupado', conflito }).
    - `grep -c 'reaproveitar_ou_criar_cliente' src/app/actions/agendamentos.ts` >= 1; `grep -c 'data_hora_fim' src/app/actions/agendamentos.ts` >= 1; `grep -c 'slot_ocupado' src/app/actions/agendamentos.ts` >= 1.
    - O ramo 23P01 não chama reportarExcecao e não expõe error.message.
    - `pnpm build` compila.
  </acceptance_criteria>
  <done>O walk-in grava data_hora_fim, deduplica cliente por RPC atômica e devolve o conflito detalhado do próprio tenant na perda de corrida.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: remarcarAgendamento — congelar duração original + 23P01 → slot_ocupado (D-03)</name>
  <files>src/app/actions/agendamentos.ts</files>
  <read_first>
    - src/app/actions/agendamentos.ts:401-467 (remarcarAgendamento: SELECT :419-424 com servicos(duracao_minutos), cálculo :434-437, revalidação :440-451, UPDATE :453-462)
    - .planning/phases/02-integridade-da-agenda/02-PATTERNS.md §"D-03 — remarcação congela a duração ORIGINAL"
    - .planning/phases/02-integridade-da-agenda/02-CONTEXT.md §D-03
  </read_first>
  <behavior>
    - A remarcação preserva a duração ORIGINAL: novoDataHoraFim = novaDataHora + (data_hora_fim − data_hora) do agendamento original — NÃO a duração vigente do serviço.
    - O UPDATE grava data_hora E data_hora_fim.
    - Perda de corrida no UPDATE (23P01) vira o mesmo retorno discriminado slot_ocupado.
  </behavior>
  <action>
    No SELECT do agendamento a remarcar (:419-424), trazer `data_hora,
    data_hora_fim` (remover a dependência de servicos(duracao_minutos) para o
    cálculo da duração). Calcular a duração original em minutos a partir de
    (data_hora_fim − data_hora) e derivar `novoDataHoraFim = novaDataHora +
    duracaoOriginal`. Passar essa duração à revalidação da engine
    (obterSlotsDisponiveis, ignorarAgendamentoId já existe). No UPDATE (:453-462),
    gravar `data_hora: novaDataHora` E `data_hora_fim: novoDataHoraFim`. Adicionar
    o ramo `if (error?.code === '23P01')` retornando { ok:false,
    motivo:'slot_ocupado', conflito } (mesma busca do conflitante da Task 1), sem
    reportarExcecao. Converter o retorno de sucesso para discriminado ({ ok:true,
    agendamento }) para casar com a Task 1. Preservar o realinhamento do lembrete
    do QStash já existente (:469+).
  </action>
  <verify>
    <automated>pnpm build && grep -nE "data_hora_fim|slot_ocupado" src/app/actions/agendamentos.ts</automated>
  </verify>
  <acceptance_criteria>
    - remarcarAgendamento calcula novoDataHoraFim a partir do intervalo ORIGINAL (data_hora_fim − data_hora), não da duração vigente do serviço.
    - O UPDATE grava data_hora e data_hora_fim; existe ramo 23P01 → slot_ocupado.
    - O retorno é discriminado, consistente com criarAgendamentoManual.
    - `pnpm build` compila.
  </acceptance_criteria>
  <done>Remarcar mantém o tamanho reservado original e passa pela mesma proteção contra sobreposição (contrato fixado para a Phase 8).</done>
  <reversibility rating="reversible">Mudança de cálculo + ramo de erro; revertível por git.</reversibility>
</task>

<task type="auto">
  <name>Task 3: NovoAgendamentoModal consome o retorno discriminado (aviso âmbar com detalhe + refetch)</name>
  <files>src/app/dashboard/NovoAgendamentoModal.tsx</files>
  <read_first>
    - src/app/dashboard/NovoAgendamentoModal.tsx:236-268 (confirmar: hoje faz string-matching de 'conflita'/'indisponível') e :337-341,:581-585 (caixas de aviso vermelha)
    - .planning/phases/02-integridade-da-agenda/02-PATTERNS.md §"NovoAgendamentoModal.tsx" (trocar string-matching pelo discriminante; caixa âmbar amber-500/30 · amber-700)
  </read_first>
  <action>
    Em `confirmar`, await o resultado discriminado de criarAgendamentoManual /
    remarcarAgendamento em vez de tratar só por throw/catch. Se `res.ok` →
    aoConcluir(). Se `res.motivo === 'slot_ocupado'` → montar a cópia pt-BR NOVA (no
    dashboard, não em mensagens.ts que é do booking público) incluindo o detalhe
    `res.conflito?.cliente`/`res.conflito?.servico`/horário formatado, exibir numa
    caixa ÂMBAR (paleta amber-500/30 · amber-700, coerente com o aviso âmbar do
    double-booking público) e manter o efeito atual (`setSlotSelecionado(null);
    setSlotsCarregados(null); setPasso('horario')`) para recarregar a grade.
    Remover o string-matching `msg.includes('conflita') || msg.includes('indisponível')`.
    O catch permanece para as guard clauses de precondição que ainda lançam
    (auth/serviço/cliente/horário passado) → caixa vermelha existente. A cópia âmbar
    do walk-in é uma constante única no arquivo (disciplina de fonte única), verbatim.
  </action>
  <verify>
    <automated>pnpm build && pnpm lint</automated>
  </verify>
  <acceptance_criteria>
    - O modal decide a recuperação de conflito por `res.motivo === 'slot_ocupado'`, não por substring da mensagem (`grep -c "includes('conflita')" src/app/dashboard/NovoAgendamentoModal.tsx` == 0).
    - A caixa âmbar mostra cliente/serviço/horário do conflito e a grade é recarregada (setSlotsCarregados(null) + volta a 'horario').
    - `pnpm build` e `pnpm lint` verdes.
  </acceptance_criteria>
  <done>O profissional vê o detalhe do agendamento que ocupa o horário e a agenda recarregada; a decisão de conflito não depende mais de casar texto de erro.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dashboard autenticado → Server Action walk-in | Sessão Clerk; createClient() authenticated com RLS por tenant_id; o detalhe do conflito é do próprio tenant. |
| erro do Postgres → UI do dashboard | Só cliente+serviço+horário do próprio tenant atravessam; nunca error.message. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-04-01 | Information Disclosure | Detalhe do conflito vazando dado além do próprio tenant | high | mitigate | Busca escopada por eq('tenant_id', orgId) sob RLS authenticated; devolve só cliente/serviço/horário, nunca error.message. |
| T-02-04-02 | Tampering | Remarcação reabrindo o bug de duração (pega duração vigente) | medium | mitigate | novoDataHoraFim derivado do intervalo original (data_hora_fim − data_hora). |
| T-02-04-03 | Denial of Service (Sentry) | 23P01 do walk-in reportado como exceção | low | mitigate | Ramo 23P01 não chama reportarExcecao. |
| T-02-04-SC | Tampering | npm/pip/cargo installs | low | accept | Nenhum pacote instalado. |
</threat_model>

<verification>
- `pnpm build` + `pnpm lint` verdes; `pnpm test` hermético verde.
- grep confirma data_hora_fim, RPC, slot_ocupado e a ausência de string-matching no modal.
- A prova de atomicidade end-to-end (SC3) é o plano 02-06, após o apply.
</verification>

<success_criteria>
Walk-in grava data_hora_fim, deduplica por RPC, trata 23P01 com detalhe do próprio
tenant; remarcação congela a duração original e reusa a proteção; a UI mostra o
aviso âmbar com detalhe e recarrega a agenda.
</success_criteria>

<output>
Create `.planning/phases/02-integridade-da-agenda/02-04-SUMMARY.md` when done
</output>
