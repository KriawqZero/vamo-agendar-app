---
phase: 02-integridade-da-agenda
plan: 06
type: execute
wave: 4
depends_on: ['02-05']
files_modified:
  - src/app/actions/__tests__/public-booking-escrita.test.ts
  - docs/PENDENCIAS.md
autonomous: true
requirements: [AGE-03, AGE-04, AGE-05]
must_haves:
  truths:
    - "SC3 (AGE-03): a prova de concorrência (EXIGIR_INTEGRACAO=1) dispara N chamadas concorrentes de criarAgendamentoPublico para o MESMO slot → exatamente 1 ok e N-1 slot_indisponivel, e o banco tem UMA linha ativa (count exato)."
    - "SC3 (AGE-03, metade walk-in): a exclusion constraint é role-agnóstica — dois inserts admin diretos de agendamentos sobrepostos no mesmo TENANT_TESTE (status <> 'cancelado') resultam em exatamente 1 vencedor e 1 falha com SQLSTATE 23P01, provando OBRIGATORIAMENTE a metade walk-in do SC3 no nível da constraint (a corrida walk-in autenticada em processo fica best-effort, com a fronteira registrada em SUMMARY/PENDENCIAS)."
    - "SC5 (AGE-05): a prova do upsert COALESCE mostra cliente reincidente pelo telefone reaproveitando o id (uma linha em clientes), com e-mail antes ausente preenchido e nome curado NÃO sobrescrito."
    - "SC4 (AGE-04): o caminho 23P01 → slot_indisponivel não chama reportarExcecao (mock assere zero chamadas no caminho de corrida)."
    - "Phase gate: pnpm lint + pnpm test + pnpm build verdes (output real) + EXIGIR_INTEGRACAO=1 pnpm test verde antes de /gsd-verify-work."
  artifacts:
    - "Casos novos de integração em src/app/actions/__tests__/public-booking-escrita.test.ts (concorrência SC3, upsert COALESCE SC5, 23P01-sem-Sentry)"
    - "docs/PENDENCIAS.md atualizado com o que a fase cria/adia (Definition of Done §6)"
  key_links:
    - "A suíte de integração é o único alvo de test:integracao (vitest.config exclui do glob padrão; opt-in EXIGIR_INTEGRACAO=1). Os casos novos moram nela, filtrando sempre pelo TENANT_TESTE."
    - "Promise.all em processo aproxima a corrida; a garantia real vem da constraint no banco — o teste prova que a constraint está lá e que o 23P01 é discriminado, não vira exceção crua."
  prohibitions:
    - "Os testes de integração NÃO podem ler/escrever tenant real — sempre filtrar por TENANT_TESTE e limpar antes/depois."
    - "O phase gate NÃO pode ser declarado verde sem mostrar a saída REAL dos quatro comandos (regra do projeto e do harness de falso-verde)."
---

<objective>
Provar, contra o banco real, as garantias que só a integração observa: a
atomicidade do double-booking (SC3), o dedupe COALESCE de cliente (SC5) e que a
perda de corrida não vai ao Sentry (SC4). Depois, rodar o phase gate completo com
saída real e registrar em PENDENCIAS o que a fase cria ou adia.

Purpose: a atomicidade não é observável em teste hermético (mock prova só o mock).
Este plano fecha a metade empírica de AGE-03/AGE-04/AGE-05 e sela a fase com os
quatro comandos verdes exigidos pela Definition of Done.

Output: casos de integração novos + PENDENCIAS atualizado + phase gate verde.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-integridade-da-agenda/02-RESEARCH.md
@.planning/phases/02-integridade-da-agenda/02-VALIDATION.md
@.planning/phases/02-integridade-da-agenda/02-05-SUMMARY.md
</context>

<flagged_assumptions>
Sonda de borda sem SPEC devolveu AGE-03/AGE-04/AGE-05 como `unresolved`. Assunção
explícita, derivada do ROADMAP SC3/SC4/SC5 + Validation Architecture do
02-RESEARCH: a prova de atomicidade é a suíte de integração (banco real,
EXIGIR_INTEGRACAO=1), não a hermética; o caso central é N inserts concorrentes →
1 vencedor + banco com 1 linha ativa. A prova do walk-in autenticado é mais cara
(exige mock de auth do Clerk); se inviável em processo, a garantia de banco (a
constraint dispara 23P01 para dois inserts sobrepostos, independente do role) é
provada diretamente e o comportamento de discriminação do walk-in fica coberto
pelo build/typecheck + os testes de comportamento do 02-04 — registrar essa
fronteira em voz alta no SUMMARY.
</flagged_assumptions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prova de concorrência SC3 (público) contra o banco real</name>
  <files>src/app/actions/__tests__/public-booking-escrita.test.ts</files>
  <read_first>
    - src/app/actions/__tests__/public-booking-escrita.test.ts:1-90,160-260,300-460 (mocks, TENANT_TESTE, beforeAll/afterAll de fixture, describe.skipIf(!temCredenciais), padrão de limpeza por tenant_id)
    - .planning/phases/02-integridade-da-agenda/02-RESEARCH.md §"Prova de concorrência para SC3" (o caso central com Promise.all de 8 + count exato)
    - .planning/phases/02-integridade-da-agenda/02-VALIDATION.md (SC3)
  </read_first>
  <behavior>
    - N (ex.: 8) chamadas concorrentes de criarAgendamentoPublico para o MESMO datetime → exatamente 1 resultado ok e N-1 com motivo slot_indisponivel.
    - Após a corrida, o banco tem exatamente 1 agendamento ativo (status <> 'cancelado') para o TENANT_TESTE naquele intervalo.
    - Metade walk-in do SC3 (nível de constraint, role-agnóstico): dois inserts admin diretos de agendamentos sobrepostos no mesmo TENANT_TESTE (status <> 'cancelado') → exatamente 1 vence e o outro falha com SQLSTATE 23P01.
  </behavior>
  <action>
    Acrescentar, dentro do describe.skipIf(!temCredenciais) da suíte de
    integração, um caso que obtém um slot legítimo pela própria engine
    (obterSlotsPublicos, nunca literal cravado), dispara N chamadas concorrentes de
    criarAgendamentoPublico para esse mesmo slot via Promise.all, e assere:
    resultados.filter(r => r.ok).length === 1; resultados.filter(r => !r.ok &&
    r.motivo === 'slot_indisponivel').length === N-1; e um count exato de
    agendamentos ativos do TENANT_TESTE naquele intervalo === 1. Limpar os
    agendamentos do TENANT_TESTE antes/depois do caso (padrão de limpeza já usado
    na suíte). No MESMO caso, provar OBRIGATORIAMENTE a metade walk-in do SC3 no
    nível da constraint (a exclusion constraint é role-agnóstica): dois inserts admin
    diretos de agendamentos com períodos sobrepostos, para o mesmo TENANT_TESTE e com
    status <> 'cancelado', resultam em exatamente um sucesso e o outro falhando com
    SQLSTATE 23P01 (error.code === '23P01'). Essa asserção NÃO é opcional — é a prova
    empírica mínima da metade walk-in do SC3, já que a constraint dispara
    independentemente do role. A corrida walk-in autenticada em processo (mock de auth
    do Clerk) fica como best-effort; se inviável, registrar a fronteira no
    SUMMARY/PENDENCIAS conforme instrui a Task 3.
  </action>
  <verify>
    <automated>EXIGIR_INTEGRACAO=1 pnpm test src/app/actions/__tests__/public-booking-escrita.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - O caso de concorrência existe e passa com EXIGIR_INTEGRACAO=1: exatamente 1 ok, N-1 slot_indisponivel, 1 linha ativa no banco.
    - A asserção de constraint role-agnóstica (dois inserts admin sobrepostos no mesmo TENANT_TESTE → exatamente 1 vence, o outro falha com SQLSTATE 23P01) existe e passa — prova empírica mínima da metade walk-in do SC3, obrigatória e não opcional.
    - O slot é obtido da engine, não cravado como literal.
    - A limpeza filtra sempre por TENANT_TESTE.
  </acceptance_criteria>
  <done>SC3 provado empiricamente: duas requisições simultâneas para o mesmo intervalo resultam em exatamente um agendamento ativo.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prova do upsert COALESCE (SC5) + 23P01 não vai ao Sentry (SC4)</name>
  <files>src/app/actions/__tests__/public-booking-escrita.test.ts</files>
  <read_first>
    - src/app/actions/__tests__/public-booking-escrita.test.ts:395 (caso existente de reaproveitamento por telefone — estender para COALESCE) e o mock de assinaturas/analytics/notificações no topo
    - .planning/phases/02-integridade-da-agenda/02-RESEARCH.md §"Wave 0 Gaps" (teste do upsert COALESCE + asserção de que reportarExcecao NÃO é chamado)
    - .planning/phases/02-integridade-da-agenda/02-VALIDATION.md (SC5/SC4)
  </read_first>
  <behavior>
    - Cliente reincidente pelo MESMO telefone no MESMO tenant reaproveita o id (uma linha em clientes, count === 1).
    - Uma segunda reserva com e-mail (antes ausente) preenche o email; um nome já curado NÃO é sobrescrito pela chamada pública (COALESCE mantém o do banco).
    - No caminho 23P01, reportarExcecao NÃO é chamado (mock com zero chamadas nesse ramo).
  </behavior>
  <action>
    Estender o caso de reaproveitamento (:395) para provar o COALESCE: gravar um
    cliente com nome curado e sem e-mail; agendar de novo pelo mesmo telefone com
    nome diferente e um e-mail — assertar que clientes tem 1 linha para o
    TENANT_TESTE+telefone, o e-mail foi preenchido, e o nome permaneceu o curado
    (não o da segunda chamada). Acrescentar/estender a asserção de que
    reportarExcecao NÃO é chamado no caminho de corrida 23P01 (mockar
    reportarExcecao e provar zero chamadas quando o retorno é slot_indisponivel —
    contrafactual: se o ramo 23P01 fosse removido, o retorno viraria erro_interno e
    reportarExcecao seria chamado). Manter os mocks existentes de notificações e
    analytics.
  </action>
  <verify>
    <automated>EXIGIR_INTEGRACAO=1 pnpm test src/app/actions/__tests__/public-booking-escrita.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - O caso COALESCE passa: 1 linha em clientes, e-mail preenchido, nome curado preservado.
    - Existe asserção de que reportarExcecao NÃO é chamado no caminho 23P01.
    - `EXIGIR_INTEGRACAO=1 pnpm test <arquivo>` verde.
  </acceptance_criteria>
  <done>SC5 e a metade empírica do SC4 provados: dedupe COALESCE atômico e perda de corrida silenciosa para o Sentry.</done>
</task>

<task type="auto">
  <name>Task 3: Phase gate completo (saída real) + PENDENCIAS</name>
  <files>docs/PENDENCIAS.md</files>
  <read_first>
    - docs/PENDENCIAS.md (seções vivas: regras de hermeticidade da suíte, itens de UAT humano)
    - .planning/phases/02-integridade-da-agenda/02-VALIDATION.md §"Manual-Only Verifications" (dois itens de UAT humano: aviso âmbar público e detalhe walk-in)
    - CLAUDE.md §"Definition of Done"
  </read_first>
  <action>
    Rodar os quatro comandos e colar a saída REAL no SUMMARY (nunca afirmar verde
    sem output): `pnpm lint`, `pnpm test`, `pnpm build`, e `EXIGIR_INTEGRACAO=1
    pnpm test`. Atualizar docs/PENDENCIAS.md (Definition of Done §6) registrando: os
    dois itens de UAT humano desta fase (aviso âmbar de conflito na tela pública e
    detalhe cliente/serviço no walk-in — só o owner os fecha), e qualquer fronteira
    deixada explícita (ex.: se a prova de concorrência do walk-in autenticado ficou
    coberta por garantia de banco em vez de teste em processo, registrar com o
    motivo). Não marcar nenhum item de UAT como concluído (nenhum executor pode).
  </action>
  <verify>
    <automated>pnpm lint && pnpm test && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - A saída real dos quatro comandos está no SUMMARY; os quatro verdes.
    - docs/PENDENCIAS.md registra os dois itens de UAT humano da fase (abertos, não marcados) e qualquer fronteira explícita.
    - Nenhum item de UAT foi marcado como concluído.
  </acceptance_criteria>
  <done>A fase está selada: quatro comandos verdes com prova real e PENDENCIAS coerente com o que a fase deixa para o UAT humano.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| suíte de integração → Supabase Cloud de dev | Escreve/apaga só sob TENANT_TESTE; nunca tenant real; limpeza antes/depois. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-06-01 | Tampering | Teste de integração escrevendo em tenant real | medium | mitigate | Sempre filtrar por TENANT_TESTE; limpeza antes/depois (padrão da suíte). |
| T-02-06-02 | Repudiation | Phase gate declarado verde sem output real | medium | mitigate | Colar a saída real dos quatro comandos no SUMMARY (regra anti-falso-verde do projeto). |
| T-02-06-SC | Tampering | npm/pip/cargo installs | low | accept | Nenhum pacote instalado. |
</threat_model>

<verification>
- SC3/SC5/SC4 provados por EXIGIR_INTEGRACAO=1 pnpm test.
- Phase gate: pnpm lint + pnpm test + pnpm build + integração, todos verdes com saída real.
- PENDENCIAS atualizado; UAT humano registrado aberto.
</verification>

<success_criteria>
Integridade provada empiricamente (concorrência + COALESCE + 23P01 silencioso) e
a fase selada pelos quatro comandos verdes, com o UAT humano registrado.
</success_criteria>

<output>
Create `.planning/phases/02-integridade-da-agenda/02-06-SUMMARY.md` when done
</output>
