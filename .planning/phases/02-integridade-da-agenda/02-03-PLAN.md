---
phase: 02-integridade-da-agenda
plan: 03
type: execute
wave: 2
depends_on: ['02-01', '02-02']
files_modified:
  - src/app/actions/public-booking.ts
autonomous: true
requirements: [AGE-04, AGE-05]
must_haves:
  truths:
    - "SC4 (AGE-04, D-05): no fluxo público, o erro 23P01 (exclusion_violation) mapeia para o discriminante `slot_indisponivel` já existente — não para `erro_interno` — e NÃO é reportado ao Sentry (perda de corrida é condição esperada)."
    - "SC5 (AGE-05, D-01): o select-then-insert de cliente (:463-511) é substituído por uma chamada à RPC reaproveitar_ou_criar_cliente (atômica, COALESCE), retornando o id."
    - "O INSERT de agendamento grava `data_hora_fim` (= data_hora + servico.duracao_minutos) no payload."
    - "A mensagem crua do Postgres (org_id, horário de terceiro) NUNCA atravessa para a UI — o padrão anti-PII de erroSinteticoSupabase é preservado."
  artifacts:
    - "src/app/actions/public-booking.ts com o ramo 23P01 → slot_indisponivel, a chamada à RPC de cliente e data_hora_fim no INSERT"
  key_links:
    - "O discriminante slot_indisponivel já é consumido pelo BookingApp (solta o slot morto, refaz a grade, aviso âmbar) — a fase reusa, não recria a UI pública."
    - "A ordem importa: o ramo `if (agError?.code === '23P01')` vem ANTES do ramo genérico de erro_interno/reportarExcecao."
  prohibitions:
    - "O ramo 23P01 NÃO pode chamar reportarExcecao — inundaria o Sentry com condição esperada. [D-05]"
    - "O erro_interno genuíno NÃO pode deixar de reportar — só a perda de corrida é silenciada; falha de infra continua indo ao Sentry."
    - "A .message do Postgres NÃO pode entrar no contexto do reporte nem no retorno — embute literais do input (PII)."
---

<objective>
Discriminar a perda de corrida no fluxo público de escrita e trocar o
select-then-insert de cliente pela RPC atômica. Quando o INSERT do agendamento
falha com `23P01` (a exclusion constraint fechou o TOCTOU que a validação da
engine deixa aberto), a action devolve `slot_indisponivel` — o mesmo
discriminante que o BookingApp já consome — sem reportar ao Sentry.

Purpose: fecha o SC4 no fluxo público (o cliente que perde a corrida vê cópia
honesta com os horários recarregados, nunca a mensagem do PostgreSQL com dados de
terceiro) e o SC5 (dedupe atômico de cliente por telefone).

Output: public-booking.ts com o ramo 23P01, a chamada à RPC e data_hora_fim no
INSERT.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-integridade-da-agenda/02-CONTEXT.md
@.planning/phases/02-integridade-da-agenda/02-RESEARCH.md
@.planning/phases/02-integridade-da-agenda/02-PATTERNS.md
@.planning/phases/02-integridade-da-agenda/02-02-SUMMARY.md
</context>

<flagged_assumptions>
Sonda de borda sem SPEC devolveu AGE-04/AGE-05 como `unresolved`. Assunção
explícita, derivada do ROADMAP SC4/SC5 + D-05/D-01 (locked): o "vencedor" da
corrida é o INSERT que a constraint aceita; o "perdedor" recebe `23P01` do
supabase-js em `error.code` (confirmado no código: public-booking-escrita.test.ts
já assere `error?.code === '23505'`, provando que o SQLSTATE atravessa o
supabase-js). A cópia que o cliente vê é `COPY_SLOT_INDISPONIVEL` (já contratada
em mensagens.ts). Nenhuma UI pública muda — só o roteamento do erro.
</flagged_assumptions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RPC de cliente + data_hora_fim no INSERT do agendamento (D-01)</name>
  <files>src/app/actions/public-booking.ts</files>
  <read_first>
    - src/app/actions/public-booking.ts:453-524 (select-then-insert de cliente a substituir; INSERT do agendamento a enriquecer com data_hora_fim; servico.duracao_minutos já em escopo — usado no booking_completed :547)
    - src/app/actions/public-booking.ts:470-482 (padrão anti-PII de tratamento de erro de infra: reportarExcecao(erroSinteticoSupabase(...)) + motivo genérico — preservar)
    - .planning/phases/02-integridade-da-agenda/02-PATTERNS.md §"public-booking.ts" (upsert COALESCE a substituir)
    - .planning/phases/02-integridade-da-agenda/02-RESEARCH.md §"Pattern 4" (chamada .rpc com p_tenant_id/p_telefone/p_nome/p_email)
    - .planning/phases/02-integridade-da-agenda/02-02-SUMMARY.md (assinatura final da RPC)
  </read_first>
  <behavior>
    - Chamar a action com telefone já cadastrado neste tenant reaproveita o mesmo id de cliente (uma linha em clientes), sem criar segunda.
    - Um e-mail antes ausente é preenchido pela chamada reincidente; um nome já curado NÃO é sobrescrito (COALESCE mantém o do banco).
    - O agendamento gravado tem data_hora_fim = data_hora + servico.duracao_minutos.
    - Erro de infra na RPC continua indo ao Sentry por erroSinteticoSupabase, sem dado do cliente, com motivo erro_interno.
  </behavior>
  <action>
    Substituir o bloco select-then-insert de cliente (:463-511) por uma chamada
    `await admin.rpc('reaproveitar_ou_criar_cliente', { p_tenant_id: tenantId,
    p_telefone: telefoneLimpo, p_nome: nomeLimpo, p_email: emailLimpo || null })`,
    lendo o id retornado para `clienteId`. Em erro de infra da RPC, manter o mesmo
    padrão vigente: `reportarExcecao(erroSinteticoSupabase(error), { fluxo:
    'booking_publico', etapa: 'buscar_cliente' })` e `return { ok:false,
    motivo:'erro_interno' }` — sem nenhum dado do cliente no contexto. No INSERT do
    agendamento (:514-524), acrescentar `data_hora_fim` ao payload, calculado de
    `dataHora + servico.duracao_minutos` (o mesmo servico.duracao_minutos já em
    escopo). Não alterar a validação/sanitização já existentes nem o formato de
    saída da engine.
  </action>
  <verify>
    <automated>pnpm build && grep -nE "reaproveitar_ou_criar_cliente|data_hora_fim" src/app/actions/public-booking.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'reaproveitar_ou_criar_cliente' src/app/actions/public-booking.ts` >= 1.
    - `grep -c 'data_hora_fim' src/app/actions/public-booking.ts` >= 1 (gravado no INSERT).
    - O tratamento de erro de infra da RPC não inclui nenhum dado do cliente no contexto do reporte.
    - `pnpm build` compila.
  </acceptance_criteria>
  <done>O caminho público cria/reaproveita cliente atomicamente pela RPC COALESCE e grava data_hora_fim no agendamento.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Discriminar 23P01 → slot_indisponivel, sem Sentry (D-05)</name>
  <files>src/app/actions/public-booking.ts</files>
  <read_first>
    - src/app/actions/public-booking.ts:438-451 (molde do retorno discriminado slot_indisponivel + evento de funil)
    - src/app/actions/public-booking.ts:526-542 (bloco agError atual que roteia TUDO para reportarExcecao + erro_interno)
    - src/app/book/[slug]/mensagens.ts:51,143 (COPY_SLOT_INDISPONIVEL já mapeado)
    - .planning/phases/02-integridade-da-agenda/02-RESEARCH.md §"Pattern 3" (ramo público)
    - .planning/phases/02-integridade-da-agenda/02-PATTERNS.md §"Condição esperada NÃO vai ao Sentry"
  </read_first>
  <behavior>
    - Quando o INSERT do agendamento falha com error.code === '23P01', a action retorna { ok:false, motivo:'slot_indisponivel' } e NÃO chama reportarExcecao.
    - Qualquer outro agError (ou !agendamento) continua caindo em reportarExcecao + { ok:false, motivo:'erro_interno' }.
  </behavior>
  <action>
    Inserir, ANTES do bloco genérico `if (agError || !agendamento)` (:526),
    um ramo `if (agError?.code === '23P01') { return { ok:false,
    motivo:'slot_indisponivel' } }`. Não chamar reportarExcecao nesse ramo (perda
    de corrida é condição esperada). Opcionalmente, capturar o evento de funil
    booking_failed com motivo slot_indisponivel dentro de um try/catch que nunca
    afeta o retorno (mesmo padrão de :442-446). O bloco genérico de erro_interno
    permanece intacto para falha de infra real. Escrever/estender o teste que
    prova, por contrafactual, que sem o ramo o retorno vira erro_interno e que
    reportarExcecao NÃO é chamado no caminho de corrida (mock de reportarExcecao
    assertando zero chamadas nesse ramo).
  </action>
  <verify>
    <automated>pnpm test && grep -n "23P01" src/app/actions/public-booking.ts</automated>
  </verify>
  <acceptance_criteria>
    - Existe um ramo que testa error.code === '23P01' e retorna slot_indisponivel ANTES do ramo erro_interno.
    - Há prova (teste) de que reportarExcecao NÃO é chamado no caminho 23P01 (mock com zero chamadas).
    - `pnpm test` (hermético) verde; `pnpm build` compila.
  </acceptance_criteria>
  <done>O SC4 público é satisfazível: quem perde a corrida recebe slot_indisponivel e o BookingApp já mostra o aviso âmbar com a grade recarregada, sem inundar o Sentry.</done>
  <reversibility rating="reversible">Ramo de erro adicional; revertível apagando o bloco if.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador anônimo → Server Action pública | Entrada não confiável (slug, servicoId, dataHora, nome, telefone, email) já validada/sanitizada; escrita via createAdminClient (service_role). |
| erro do Postgres → UI pública | A .message crua carrega org_id e horário de terceiro; só o discriminante atravessa. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-03-01 | Information Disclosure | Mensagem 23P01 crua vazando org_id/horário de terceiro para o cliente | high | mitigate | Discriminar por error.code, nunca propagar error.message; retorno é o discriminante fechado slot_indisponivel. |
| T-02-03-02 | Denial of Service (Sentry) | Perda de corrida reportada como exceção inunda a fila de erro | medium | mitigate | Ramo 23P01 NÃO chama reportarExcecao; teste com mock prova zero chamadas. |
| T-02-03-03 | Information Disclosure | Dado do cliente no contexto do reporte de infra | medium | mitigate | Preservar erroSinteticoSupabase (sem dado do cliente); manter o padrão :470-482. |
| T-02-03-SC | Tampering | npm/pip/cargo installs | low | accept | Nenhum pacote instalado. |
</threat_model>

<verification>
- `pnpm test` + `pnpm build` verdes.
- grep confirma o ramo 23P01, a chamada à RPC e data_hora_fim no INSERT.
- A prova de concorrência ponta a ponta (SC3/SC5 contra o banco real) é o plano 02-06, após o apply.
</verification>

<success_criteria>
Fluxo público: cliente atômico por RPC COALESCE, data_hora_fim gravado, e perda de
corrida (23P01) mapeada para slot_indisponivel sem Sentry.
</success_criteria>

<output>
Create `.planning/phases/02-integridade-da-agenda/02-03-SUMMARY.md` when done
</output>
