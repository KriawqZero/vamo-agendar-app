---
phase: 01-hardening-da-superf-cie-p-blica
plan: 17
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
    - scripts/verificar-controle-harness-anon.sh
    - scripts/verificar-superficie-anon.sh
autonomous: true
requirements: [SEG-01, SEG-02, SEG-03]

must_haves:
    truths:
        - "Fecha o gap reprovado \"`scripts/verificar-superficie-anon.sh` — 'checagem que não prova nada não pode passar' (must-have do plano 01-15)\": com o alvo inalcançável o harness sai **2**, e a frase `0 reprovada(s) — a role anon não devolveu linha nenhuma` deixa de ser impressa"
        - "O eixo da IDENTIDADE DO ALVO passa a ser medido em toda execução: uma tabela sabidamente inexistente e uma tabela declarada e fechada têm de responder de formas DISTINGUÍVEIS; indistinguíveis, o harness sai 2 em vez de exibir ESPERADO"
        - "Nenhuma execução sai 0 sem pelo menos uma PROVA POSITIVA: com zero checagem ESPERADA o harness sai 2 nomeando que nada foi provado, em vez de deixar `INCONCLUSIVAS` ser impresso e descartado"
        - "O instrumento ganha controle próprio e re-executável: `bash scripts/verificar-controle-harness-anon.sh` demonstra, na MESMA execução, que o harness reprova nos três estados de falha (alvo morto, projeto errado, alvo que nega tudo) e aprova contra o alvo real"
        - "O controle nasceu ANTES do conserto e sua primeira execução REPROVOU os quatro vereditos — controle escrito depois do conserto nunca prova que mediria a falha"
        - "O veredito do harness sobre o alvo real não regride: as 11 checagens continuam ESPERADO por `42501` e a COBERTURA continua 9 declaradas / 9 cobertas"
        - "O caminho público do cliente final não é tocado: este plano altera apenas dois arquivos em `scripts/`, nenhum em `src/`, `supabase/` ou `package.json`"
    artifacts:
        - "scripts/verificar-controle-harness-anon.sh — controle re-executável do instrumento, quatro vereditos"
        - "scripts/verificar-superficie-anon.sh — contador `ESPERADAS` decidindo o exit code e veredito `ALVO` de identidade do alvo"
    key_links:
        - "`registrar()` → contador de prova positiva → exit code: o elo que hoje existe para `REPROVADAS` e não existe para prova positiva (`:398-401`)"
        - "sonda de referência declarada + sonda de canário inexistente → veredito `ALVO` → exit 2: o elo que distingue 'fechado' de 'não é este banco'"
        - "`verificar-controle-harness-anon.sh` → `verificar-superficie-anon.sh` → stub HTTP local: o elo que prova que o instrumento consegue reprovar"
    prohibitions:
        - "Nunca imprimir afirmação positiva de fechamento de segurança sem ter produzido pelo menos uma prova positiva na mesma execução — exit 0 a partir de zero medição é o defeito, não o relatório"
        - "Nunca ler, copiar, sourcear ou imprimir o arquivo de ambiente real: o ambiente sintético do controle é escrito pelo próprio script com valores obviamente falsos, e só NOMES de variável podem aparecer na saída"
        - "Nunca afrouxar veredito existente do harness: nenhuma checagem que hoje REPROVA pode passar a ESPERADO ou INCONCLUSIVO como efeito colateral deste conserto"
        - "Nunca tocar o caminho público em `src/` — a Fricção Zero do cliente final não pode mudar por causa de instrumento de medição"
---

<objective>
Consertar o instrumento antes de usá-lo para medir qualquer coisa.

`scripts/verificar-superficie-anon.sh` é o artefato que `ROADMAP.md:195` e `01-04-PLAN.md:170` nomeiam como prova de SEG-01, SEG-02 e SEG-03 — isto é, a prova de que a **D-01** (a role `anon` perde o acesso à Data API por completo) e a **D-03** (default privileges revogadas para `anon` **e** `authenticated`) valem no banco e não só no repositório. O cabeçalho dele promete, com todas as letras, que "checagem que não prova nada não pode passar". O verificador mediu o contrário: copiou o harness e `supabase/schemas/` para um diretório isolado, apontou um `.env.local` para um host Supabase inexistente, e rodou sem tocar em mais nada. As 11 checagens registraram `HTTP 000 sem 42501 — não provou permissão negada`, o veredito COBERTURA passou ("9 declarada(s), 9 coberta(s)") e a última linha foi `Resumo: 11 checagem(ns), 0 reprovada(s) — a role anon não devolveu linha nenhuma.` com **exit code 0**. Uma afirmação positiva de segurança emitida a partir de zero medição.

A causa está em `:398-401`: o exit code é decidido só por `REPROVADAS -eq 0`; `INCONCLUSIVAS` é impresso e descartado. O conserto do WR-08 no plano 01-15 foi real e o verificador o confirmou na mesma execução — um `000` hoje vira INCONCLUSIVO e não ESPERADO, o eixo do NOME DA TABELA está fechado. **O falso verde apenas mudou de eixo.** O segundo cenário do `01-REVIEW.md` §CR-02 continua aberto e é explícito em `:262-273` e `:319-329`: com a URL apontando para outro projeto Supabase, nenhuma das nove tabelas existe lá, o PostgREST devolve `PGRST205`, `tabela_declarada` confere o nome contra `supabase/schemas/*.sql` — que é **local**, não remoto — e tudo vira ESPERADO com exit 0 e linguagem de prova positiva.

Este plano fecha os dois eixos que sobraram — **prova positiva** e **identidade do alvo** — e faz isso na ordem que esta fase pagou caro para aprender: o controle nasce primeiro e a primeira execução dele tem de REPROVAR. Harness escrito depois do conserto nunca prova que mediria a falha.

Purpose: devolver ao exit 0 deste script o direito de ser citado como evidência.
Output: um controle re-executável de quatro vereditos e um harness que sabe dizer "não medi".
</objective>

## ⛔ Serialização obrigatória — este plano roda SOZINHO na wave 1

Um plano por wave, nunca em paralelo. É constante desta fase desde a revisão 1 e os três modos de falha continuam valendo: `.next/` compartilhado faz uma build concorrente medir a árvore errada, a fixture do tenant de teste é apagada no meio, e a contagem cross-tenant da linha de base é poluída.

**Este plano vem primeiro pelo mesmo motivo que o 01-15 foi movido à frente do 01-14 na rodada anterior: conserte o instrumento antes de usá-lo para medir.** O plano 01-18 vai afirmar que a superfície pública ficou mais fechada, e a única bateria automatizada que mede superfície é justamente a que hoje não consegue reprovar.

## Artefatos que este plano produz

| Símbolo / arquivo | Tipo | Onde nasce |
|---|---|---|
| `scripts/verificar-controle-harness-anon.sh` | script de controle, quatro vereditos | novo |
| `ALVO_MORTO` | veredito do controle | `verificar-controle-harness-anon.sh` |
| `PROJETO_ERRADO` | veredito do controle | `verificar-controle-harness-anon.sh` |
| `TUDO_NEGADO` | veredito do controle | `verificar-controle-harness-anon.sh` |
| `CONTROLE` | veredito do controle (positivo) | `verificar-controle-harness-anon.sh` |
| `PORTA_CONTROLE` | variável de ambiente opcional (padrão 3993) | `verificar-controle-harness-anon.sh` |
| `ESPERADAS` | contador de prova positiva que decide o exit code | `verificar-superficie-anon.sh` |
| `ALVO` | veredito de bateria (identidade do alvo) | `verificar-superficie-anon.sh` |
| `TABELA_CANARIO` | constante de módulo (nome ausente dos schemas declarativos) | `verificar-superficie-anon.sh` |

Nenhum símbolo de `src/` é criado, alterado ou removido por este plano. Nenhum membro novo de `MotivoPublico`. Nenhuma migration.

## Suposições sinalizadas (sonda de cobertura de borda sem SPEC)

Esta fase não tem SPEC simples, então a sonda determinística de bordas rodou sobre SEG-01..SEG-05 e produziu **5 linhas, todas `unresolved`, quatro delas `unclassified`** (relatório em `.edge-coverage.tmp.json` no diretório da fase). As cinco ficam registradas aqui como suposição explícita, nunca convertidas em `backstop` automático e nunca descartadas em silêncio — 5 levantadas, 5 prestadas contas:

| # | Requisito | Categoria da sonda | O que registro |
|---|---|---|---|
| A-01 | SEG-01 | `concurrency` — "If interrupted or run in parallel, what is guaranteed?" | Sem sinal utilizável. A pergunta de concorrência, tal como a sonda a formula, é sobre execução de tarefa; a garantia real de SEG-01 é de privilégio de banco e está medida em `01-VERIFICATION.md` truth 2 (POST anônimo em `agendamentos` e `clientes` → 401/42501). A serialização estrita desta fase (um plano por wave) responde à parte de execução |
| A-02 | SEG-02 | `unclassified` | Sem sinal utilizável |
| A-03 | SEG-03 | `unclassified` | Sem sinal utilizável |
| A-04 | SEG-04 | `unclassified` | Sem sinal utilizável |
| A-05 | SEG-05 | `unclassified` | Sem sinal utilizável |

**Por que não há sinal, dito honestamente:** o classificador da sonda é orientado a inglês e o texto dos requisitos deste projeto está em pt-BR. Isso é limitação de ferramenta, não achado sobre a fase. A cobertura de borda real desta fase vive nos `must_haves` dos planos 01-01 a 01-16 e na evidência medida de `01-VERIFICATION.md` — 11 de 13 truths verificados por medição independente, não por leitura.

<assumption_delta_decision>
O detector de delta de suposição disparou (`kind: pluralization`, termo `fallback`), a partir de uma nota antiga do ROADMAP sobre o `'secret-key'` do SEG-05 — item já fechado no plano 01-03. Esse sinal é resíduo, não escopo.

**Existe, porém, uma pluralização real neste plano, e ela é registrada aqui em vez de acontecer em silêncio:** a noção de "prova de fechamento" do harness deixa de ter um eixo só (a resposta de cada tabela) e passa a ter dois (a resposta de cada tabela **e** a identidade do alvo).

- **Substantivo primário:** continua sendo `checagem` — a unidade que produz veredito por tabela.
- **Decisão: `add-alongside`.** O veredito `ALVO` entra ao lado das checagens, como veredito de bateria (mesma categoria da `COBERTURA` que já existe), e não substitui nem reformula a checagem.
- **Dívida aceita e gatilho escrito:** se aparecer um **terceiro** controle de nível de bateria — por exemplo "a chave usada é mesmo a publicável e não a secreta" —, o movimento correto deixa de ser acrescentar um quarto bloco solto e passa a ser **promover** os controles a um conceito próprio, com registrador e contagem próprios, separado das checagens por tabela. Enquanto forem dois, o custo da promoção não se paga.
</assumption_delta_decision>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-hardening-da-superf-cie-p-blica/01-VERIFICATION.md
@.planning/phases/01-hardening-da-superf-cie-p-blica/01-REVIEW.md
@CLAUDE.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: O controle do instrumento nasce e REPROVA — o falso verde reproduzido por comando</name>
  <precondition>O arquivo de ambiente da raiz existe e traz `NEXT_PUBLIC_SUPABASE_URL` e `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY`, a máquina alcança esse host, e `node` está no PATH — o veredito CONTROLE mede o alvo real e os outros três sobem um stub HTTP local. Sem isso o controle aborta com código 2 em vez de degradar para aprovação.</precondition>
  <files>scripts/verificar-controle-harness-anon.sh</files>
  <read_first>
    - `scripts/verificar-superficie-anon.sh` (INTEIRO — em especial a classificação de leitura em `:236-273`, a de escrita em `:290-329`, o bloco de exit em `:390-407`, e o cabeçalho que promete o oposto do que o código faz)
    - `scripts/verificar-fail-fast-boot.sh` (analog de gerência de processo: `set -m` só no lançamento, PID capturado em `$!`, `kill -- -"$PID"` no trap, `setsid` proibido, e o formato de quatro vereditos com o exit code como resultado)
    - `scripts/verificar-travessia-server-action.sh` (analog de estrutura: `registrar()`, `abortar()`, `mktemp -d` com `trap limpar EXIT INT TERM`, `porta_ocupada()`, e o veredito `CONTROLE` — que existe justamente para um build quebrado não ser lido como falha do que se queria medir)
    - `.planning/phases/01-hardening-da-superf-cie-p-blica/01-VERIFICATION.md` §gaps, primeiro gap (a reprodução exata que este controle precisa transformar em comando)
    - `.planning/phases/01-hardening-da-superf-cie-p-blica/01-REVIEW.md` §CR-02 (os dois cenários: alvo inalcançável e projeto errado)
  </read_first>
  <action>
Crie `scripts/verificar-controle-harness-anon.sh` — o controle do instrumento. Cabeçalho em pt-BR no mesmo tom dos outros três scripts, dizendo por que existe: um harness que não consegue reprovar é pior que harness nenhum, porque documenta uma garantia inexistente, e esta fase já foi queimada duas vezes por esse mecanismo exato.

**Quatro vereditos, com o exit code como resultado.**

`ALVO_MORTO` — reproduz o gap medido pelo verificador. Crie um diretório de trabalho dentro de um `mktemp -d`, copie para lá `scripts/verificar-superficie-anon.sh` e o diretório `supabase/schemas` inteiro, e **escreva** ali um arquivo de ambiente com exatamente duas linhas: a URL apontando para `https://alvo-que-nao-existe-9f3a2b.invalid` e a chave anônima com um valor obviamente falso escrito dentro do próprio script. O TLD `.invalid` é reservado pela RFC 2606 e não resolve em lugar nenhum — é o que torna esta sonda determinística sem depender do estado da rede. Rode o harness a partir desse diretório e capture exit code e saída. APROVA se o exit code for **2** e a saída **não** contiver a literal da frase de fechamento. REPROVA em qualquer outro arranjo, imprimindo o exit code observado e as últimas linhas da saída.

`PROJETO_ERRADO` — reproduz o segundo cenário do CR-02 sem depender de um segundo projeto Supabase. Suba um stub HTTP local que responde a **qualquer** caminho com HTTP 404 e um corpo JSON carregando o código de tabela fora do schema cache. Aponte o ambiente sintético para esse stub e rode o harness. APROVA se o exit code for **2**. Escreva junto do veredito por que ele existe: quando o alvo não é o banco deste projeto, nenhuma tabela declarada existe lá, e o harness confere o nome contra arquivos locais — todas as checagens viram ESPERADO sem que nada tenha sido medido.

`TUDO_NEGADO` — o eixo oposto, e ele não estava no relatório. Mesmo stub, outro modo: responde a **qualquer** caminho com HTTP 401 e um corpo carregando o código de permissão negada, inclusive para o nome do canário. APROVA se o exit code for **2**. Racional a escrever junto: um alvo que nega tudo indiscriminadamente — gateway hostil, proxy autenticando na frente, rate limit respondendo 401 — é indistinguível de fechamento real, e o canário é a única coisa que denuncia. Sem este veredito, o conserto fecharia um eixo e abriria outro pela terceira vez.

`CONTROLE` — o positivo, e sem ele o script provaria apenas que tudo reprova. Rode `bash scripts/verificar-superficie-anon.sh` a partir da **raiz do projeto**, sem tocar em ambiente nenhum. APROVA se o exit code for **0** e a saída contiver o rótulo do veredito de identidade do alvo que a Task 2 vai introduzir. Na execução de hoje este veredito REPROVA por ausência desse rótulo, e isso é o esperado.

**Mecânica obrigatória, e as razões.** O arquivo de ambiente real da raiz nunca é lido, copiado nem sourceado por este script: o ambiente sintético é escrito por ele, com valores que ninguém confundiria com credencial, e só NOMES de variável podem aparecer na saída — mesmo contrato dos outros dois harnesses. O stub é escrito pelo próprio script num heredoc para um arquivo `.cjs` dentro do diretório temporário (uns trinta linhas de `node:http`, modo escolhido por argumento), lançado com `set -m` ligado só na hora do lançamento, PID capturado em `$!`, e derrubado por `kill -- -"$PID"` no `trap`. `setsid` está **proibido**, em qualquer forma, pelo mesmo motivo dos outros dois harnesses: ele forka e retorna 0 quando o chamador já é líder de grupo, e nesse caminho o PID capturado não é o processo que se quer matar. A porta sai de `PORTA_CONTROLE`, padrão 3993, e o script ABORTA com código 2 se ela estiver ocupada. A prontidão do stub é aferida por polling de `curl --max-time`, com teto de tentativas; se ele não subir, ABORTE com 2 — nunca degrade para aprovação.

O script sai **0** só com os quatro aprovados, **1** com qualquer reprovação, e **2** em erro de preparação (`node` ausente, porta ocupada, `supabase/schemas` ilegível, `mktemp` falhando, stub que não sobe).

**Agora rode, ANTES de tocar em `scripts/verificar-superficie-anon.sh`.** Os quatro vereditos têm de REPROVAR. Cole a saída real no SUMMARY, com os exit codes observados em cada um. Se algum APROVAR nesta execução, PARE e investigue antes de seguir: ou o defeito não é o que o relatório descreve, ou o controle não está medindo o que pensa que mede — as duas hipóteses precisam ser registradas, e nenhuma delas se resolve seguindo em frente.
  </action>
  <verify>
    <automated>bash scripts/verificar-controle-harness-anon.sh; test $? -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/verificar-controle-harness-anon.sh` sai **1** — não 0, não 2 — e imprime `4 vereditos, 4 REPROVADO(S)`. Este é o critério de fail-first do plano: o controle precisa nascer vermelho
    - `ALVO_MORTO` reprova registrando que o harness saiu **0** contra um host inexistente — cole a linha exata no SUMMARY, porque ela é a reprodução por comando do gap que o verificador reproduziu à mão
    - `PROJETO_ERRADO` e `TUDO_NEGADO` reprovam registrando exit **0** do harness contra os dois modos do stub
    - `CONTROLE` reprova por AUSÊNCIA do rótulo do veredito de identidade do alvo, e a saída registra que o exit code do harness real já é 0 — a distinção entre "o veredito está certo" e "o instrumento não mede" tem de ficar legível no relatório
    - Nenhuma variável secreta é referenciada no arquivo: `grep -vE '^[[:space:]]*#' scripts/verificar-controle-harness-anon.sh | grep -cE 'SECRET_KEY|SERVICE_ROLE|QSTASH_|EVOLUTION_|RESEND_'` devolve `0`
    - O ambiente sintético é escrito, não copiado: `grep -c 'NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=' scripts/verificar-controle-harness-anon.sh` devolve valor `>= 1`
    - O script não deixa resíduo: depois da execução, `ls /tmp` não guarda o diretório de trabalho, e nenhuma porta segue escutando (`PORTA_CONTROLE` livre)
    - Escopo intocado: `git status --porcelain` lista exatamente um arquivo novo, `scripts/verificar-controle-harness-anon.sh`
  </acceptance_criteria>
  <reversibility rating="reversible">Arquivo novo em `scripts/`, sem efeito em banco, em código de produção ou em `package.json`; desfazer é apagá-lo.</reversibility>
  <done>O controle existe, é re-executável por comando, e sua primeira execução REPROVA os quatro vereditos — está provado que ele consegue medir a falha antes de qualquer conserto.</done>
</task>

<task type="auto">
  <name>Task 2: O harness ganha prova positiva e controle de identidade do alvo</name>
  <files>scripts/verificar-superficie-anon.sh</files>
  <read_first>
    - `scripts/verificar-superficie-anon.sh` (INTEIRO, de novo e no estado atual: `registrar()` em `:173-191`, a derivação de `TABELAS_DECLARADAS` em `:108-126`, o bloco de COBERTURA em `:367-388` e o bloco de exit em `:390-407`)
    - `scripts/verificar-controle-harness-anon.sh` (o que a Task 1 acabou de escrever — os quatro vereditos são a especificação executável deste conserto)
    - `.planning/phases/01-hardening-da-superf-cie-p-blica/01-VERIFICATION.md` §gaps, lista `missing` do primeiro gap (o piso de aceitação, item a item)
    - `.planning/phases/01-hardening-da-superf-cie-p-blica/01-REVIEW.md` §CR-02 (a forma sugerida do contador e do controle de alvo)
  </read_first>
  <action>
**(a) Prova positiva passa a decidir o exit code.** Acrescente um contador ao lado de `REPROVADAS` e `INCONCLUSIVAS`, incrementado no ramo final de `registrar()` — o que hoje só imprime o rótulo `[ESPERADO]` e não guarda nada. Antes de qualquer linha de "Resumo", se esse contador for zero, escreva em `stderr` que nenhuma checagem produziu prova positiva de fechamento, que a causa provável é alvo inalcançável, projeto errado ou rede caída, e que isto **não** é um verde — e saia com **2**. O código 2 já é, no contrato deste script, "o próprio harness não tem como medir"; é exatamente o que este estado é.

**(b) Veredito de identidade do alvo, rodado ANTES de todas as checagens.** Duas sondas GET na Data API, com a mesma chave anônima que o resto do script usa:

*Referência.* Percorra `TABELAS_DECLARADAS` — a lista derivada de `supabase/schemas/*.sql`, nunca redigitada — e pare na primeira tabela que responder com o código de permissão negada. Essa resposta é a prova positiva de três coisas de uma vez: o host respondeu, é um PostgREST, e o sistema de privilégios do Postgres se pronunciou sobre uma tabela que este projeto declara. `PGRST205`/404 na referência **não** serve: é precisamente o sinal ambíguo do CR-02. Se nenhuma das declaradas responder com o código de permissão negada, a referência falhou.

*Canário.* Uma constante de módulo com um nome de tabela que **não** consta dos schemas declarativos. Antes de sondar, confira o nome contra `tabela_declarada`: se ele existir na lista, ABORTE com 2 — o canário perdeu a função e um canário sem função é o mesmo defeito por outra porta. A sonda tem de responder `PGRST205` ou 404, e **não** pode responder o código de permissão negada.

*Regra do veredito.* Aprovado só quando as duas condições valem juntas. Qualquer outro arranjo — nenhuma declarada com permissão negada, canário respondendo permissão negada, canário indistinguível da referência, ou qualquer das duas sondas sem resposta — imprime o que foi observado nas DUAS sondas, lado a lado, e sai com **2**. Escreva junto do código a frase que justifica o par: sem ele, "fechado" e "não é este banco" são indistinguíveis, e foi assim que o falso verde do WR-08 mudou de eixo em vez de acabar.

Este veredito **não** entra na contagem de checagens de tabela nem no contador de prova positiva — é veredito de bateria, da mesma categoria da COBERTURA que já existe. Imprima-o na própria linha, com rótulo próprio. E ele roda **sempre**, inclusive quando há filtro de tabela na linha de comando: escopo reduzido pode dispensar cobertura, nunca a identidade do alvo.

**(c) Cabeçalho atualizado, porque ele é parte do artefato.** Acrescente os dois vereditos novos à seção de vereditos e substitua a promessa genérica por uma descrição do que o exit 0 passa a significar, enumerada: pelo menos uma prova positiva produzida, alvo identificado como o banco deste projeto, nenhuma checagem reprovada, e toda tabela declarada coberta. Acrescente também a linha que nomeia o controle — quem quiser confiar no exit code deste script pode rodar `bash scripts/verificar-controle-harness-anon.sh` e ver o instrumento reprovar de propósito.

**(d) Meça os dois, nesta ordem.** Primeiro o controle: ele tem de sair **0** com os quatro vereditos aprovados — os mesmos quatro que estavam vermelhos na Task 1. Depois o harness real: exit **0**, com as 11 checagens em ESPERADO por `42501`, COBERTURA em 9 declaradas / 9 cobertas, e a linha nova do veredito de alvo aprovada. Cole as duas saídas no SUMMARY, e cole também, ao lado, a saída vermelha da Task 1 — o contrafactual completo é o que distingue "consertou" de "sempre esteve assim".

Não altere nenhuma outra classificação do script. Nenhuma checagem que hoje REPROVA pode passar a ESPERADO ou INCONCLUSIVO como efeito colateral: se alguma passar, o conserto virou afrouxamento e o plano falhou no seu próprio critério.
  </action>
  <verify>
    <automated>bash scripts/verificar-controle-harness-anon.sh &amp;&amp; bash scripts/verificar-superficie-anon.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/verificar-controle-harness-anon.sh` sai **0** e imprime `4 vereditos, 0 reprovados` — os quatro vermelhos da Task 1 estão verdes, e o SUMMARY traz as duas saídas lado a lado
    - `bash scripts/verificar-superficie-anon.sh` sai **0**, imprime a linha do veredito de identidade do alvo, mantém as 11 checagens em ESPERADO com o código de permissão negada e a COBERTURA em 9 declaradas / 9 cobertas — o veredito sobre o alvo real não regrediu
    - O contador de prova positiva existe e decide: `grep -vE '^[[:space:]]*#' scripts/verificar-superficie-anon.sh | grep -c 'ESPERADAS'` devolve valor `>= 3` (declaração, incremento e teste)
    - **Controle negativo do canário, executado nesta task:** troque temporariamente a constante do canário para o nome de uma tabela que EXISTE nos schemas (`assinaturas` serve), rode o harness, confirme que ele ABORTA com código 2 nomeando o canário, cole a saída no SUMMARY e reverta a constante. Sem esta demonstração, a guarda do canário é uma condicional que ninguém viu falhar
    - `bash scripts/verificar-fail-fast-boot.sh` continua saindo 0 com os quatro vereditos — nenhum harness irmão foi afetado
    - `pnpm lint` e `pnpm test` continuam saindo 0 (nada em `src/` mudou; a asserção é de não-regressão do ambiente, e as contagens devem bater com as do início da task)
    - Escopo intocado: `git diff --name-only` nesta task lista exatamente `scripts/verificar-superficie-anon.sh`
  </acceptance_criteria>
  <reversibility rating="reversible">Duas adições num script de verificação, sem efeito em banco, em migration ou em código de produção; desfazer é reverter o arquivo.</reversibility>
  <done>O harness sabe dizer "não medi": sai 2 sem prova positiva e sai 2 quando não consegue distinguir uma tabela fechada de um alvo que não é este banco. O controle prova os dois por comando.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| harness → afirmação de fechamento citada em documento do projeto | O exit code deste script é citado no `ROADMAP.md:195` e no `01-04-PLAN.md:170` como prova de SEG-01, SEG-02 e SEG-03 |
| arquivo de ambiente → alvo medido | A URL do alvo é entrada não verificada: apontar para outro projeto muda tudo sem mudar nada visível no relatório |
| stub HTTP local → harness sob controle | O controle sobe um processo que se faz passar por um serviço remoto para medir como o harness reage |

## STRIDE Threat Register (ASVS L1 · bloqueia em `high`)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-17-01 | Spoofing | alvo da medição (URL da Data API) | high | mitigate | Alvo que não é o banco deste projeto produz ESPERADO em todas as checagens. Veredito de identidade com referência declarada respondendo permissão negada + canário inexistente respondendo fora do schema cache; indistinguíveis, exit 2 |
| T-01-17-02 | Repudiation | relatório e exit code do harness | high | mitigate | Exit 0 com zero medição é afirmação de segurança sem lastro, e é o que a fase citou como prova. O contador de prova positiva passa a decidir o exit code; zero prova positiva devolve 2 com a causa nomeada |
| T-01-17-03 | Tampering | o próprio instrumento | medium | mitigate | Conserto que só o autor sabe testar volta a apodrecer. O controle re-executável nasce antes do conserto, reprova primeiro, e fica no repositório como porta de entrada para a próxima revisão |
| T-01-17-04 | Information Disclosure | ambiente sintético do controle | high | mitigate | O controle escreve o próprio arquivo de ambiente com valores obviamente falsos; o real nunca é lido, copiado ou sourceado; nenhuma variável secreta é referenciada no arquivo e só NOMES aparecem na saída |
| T-01-17-05 | Denial of Service | porta do stub local | low | accept | O stub escuta em 127.0.0.1 numa porta configurável, morre no `trap`, e o script aborta com 2 se a porta estiver ocupada em vez de disputá-la |
| T-01-17-SC | Tampering | instalação de pacotes (npm/pnpm) | low | accept | Este plano não instala pacote nenhum — o stub usa `node:http`, que é da biblioteca padrão |
</threat_model>

<verification>
1. `bash scripts/verificar-controle-harness-anon.sh` → exit **1** na Task 1 (4 reprovados) e exit **0** na Task 2 (4 aprovados), com as duas saídas coladas no SUMMARY
2. `bash scripts/verificar-superficie-anon.sh` → exit 0, com o veredito de identidade do alvo aprovado, 11 checagens ESPERADO e COBERTURA 9/9
3. Controle negativo do canário → aborto com código 2, saída colada, constante revertida
4. `bash scripts/verificar-fail-fast-boot.sh` → exit 0, quatro vereditos
5. `pnpm lint` → exit 0
6. `pnpm test` → exit 0, hermético, com as mesmas contagens do início do plano
</verification>

<success_criteria>
- O harness reprova nos três estados de falha do instrumento e aprova no alvo real, provado por um comando que qualquer pessoa pode reexecutar
- O contrafactual completo está no SUMMARY: quatro vereditos vermelhos antes do conserto, quatro verdes depois
- O veredito do harness sobre o alvo real não mudou — o que mudou foi o direito de citá-lo
- Nada em `src/`, `supabase/` ou `package.json` foi tocado
</success_criteria>

<output>
Create `.planning/phases/01-hardening-da-superf-cie-p-blica/01-17-SUMMARY.md` when done
</output>
