---
phase: 01-hardening-da-superf-cie-p-blica
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/verificar-superficie-anon.sh
  - src/app/actions/public-booking.ts
  - supabase/migrations/<ts>_revoga_anon_assinaturas.sql
autonomous: true
requirements: [SEG-02]
user_setup:
  - service: supabase
    why: "Aplicar migrations no Supabase Cloud — não há banco local (D-06)"
    dashboard_config:
      - task: "Autenticar o MCP do Supabase (OAuth) OU liberar 'Bash(psql -h aws-1-sa-east-1.pooler.supabase.com *)' no permissions.allow"
        location: "Sessão do Claude Code (/mcp) ou .claude/settings.local.json"

must_haves:
  truths:
    - "curl anônimo em /rest/v1/assinaturas?select=tenant_id não devolve nenhuma linha com org_ (vazamento de org_id de tenants pagantes fechado — SEG-02 parcial, per D-01)"
    - "/book/[slug] continua respondendo 200 com o mesmo conteúdo depois do REVOKE — a leitura do plano vigente migrou para o cliente privilegiado ANTES do portão fechar (per D-02)"
    - "O pipeline de aplicação de DDL no Supabase Cloud está provado ponta a ponta: migration escrita no repo, aplicada no cloud, version registrada em supabase_migrations.schema_migrations (per D-06)"
    - "scripts/verificar-superficie-anon.sh existe, roda com as vars públicas do .env.local e falha (exit != 0) quando qualquer requisição anônima devolve 200 com linhas"
  artifacts:
    - "scripts/verificar-superficie-anon.sh"
    - "supabase/migrations/<ts>_revoga_anon_assinaturas.sql"
  key_links:
    - "obterDadosBookingPublico → obterPlanoVigentePublico agora recebe o cliente admin (se receber o anon depois do REVOKE, todo tenant pago degrada para gratuito em silêncio: slug customizado vira 404 e personalização some)"
    - "migration no repo ↔ version em supabase_migrations.schema_migrations (desalinhar quebra todo `db diff` futuro)"
---

<objective>
Tracer da fase: fechar o portão da Data API para UMA tabela (`assinaturas`) de ponta a ponta — código migrado para o cliente privilegiado, migration manual de REVOKE escrita e aplicada no Supabase Cloud, verificação por curl anônimo real — provando a arquitetura inteira da fase (D-01 + D-02 + D-06) com um commit antes de expandir para as outras oito tabelas.

`assinaturas` é a fatia certa: é a tabela cujo GRANT por coluna já vaza `tenant_id` (o org_id do Clerk de todo tenant pagante — inventário do RESEARCH, leitura 3), e só UMA função do caminho público a lê (`obterPlanoVigentePublico`), então o delta de código é mínimo e o risco de regressão é contido.

Purpose: descobrir em um commit — não em dez — se o pipeline MCP→Cloud funciona, se o REVOKE realmente bloqueia o PostgREST e se a leitura via admin client preserva o booking.
Output: harness de verificação reutilizável pela fase inteira + primeira tabela fechada de verdade.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-hardening-da-superf-cie-p-blica/01-CONTEXT.md
@.planning/phases/01-hardening-da-superf-cie-p-blica/01-RESEARCH.md
@.planning/phases/01-hardening-da-superf-cie-p-blica/01-PATTERNS.md
</context>

## Artifacts this phase produces (plan 01-01)

- `scripts/verificar-superficie-anon.sh` — script bash novo; aceita nomes de tabela como argumentos para rodar só um subconjunto de checagens; sem argumento roda todas
- `supabase/migrations/<ts>_revoga_anon_assinaturas.sql` — migration manual nova (REVOKE de privilégios, que `supabase db diff` não emite)
- Nenhum símbolo TypeScript novo — apenas troca de cliente em `obterDadosBookingPublico`

<tasks>

<task type="auto">
  <name>Task 1: Criar o harness de verificação anônima da Data API</name>
  <files>scripts/verificar-superficie-anon.sh</files>
  <read_first>
    - .planning/phases/01-hardening-da-superf-cie-p-blica/01-RESEARCH.md §"Verificação anônima da Data API" (o script de curl pronto, com os códigos esperados e a nota sobre PGRST205 vs 42501)
    - scripts/smoke-fundacao.mjs (estilo dos scripts existentes do projeto — mensagens pt-BR)
  </read_first>
  <action>
    Criar `scripts/verificar-superficie-anon.sh` encapsulando os curls do RESEARCH §"Verificação anônima da Data API". Comportamento:
    1. Carrega `NEXT_PUBLIC_SUPABASE_URL` e `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY` de `.env.local` via grep/cut (apenas vars públicas — nenhum segredo é lido).
    2. Checagens por tabela: `perfis_empresas` (select=* e select=tenant_id,telefone_contato), `agendamentos` (select=cliente_id e POST anônimo), `clientes` (POST anônimo), `excecoes_agenda` (select=motivo), e o loop de `select=tenant_id&limit=1` em `servicos`, `horarios_funcionamento`, `assinaturas`, `whatsapp_configs`, `disparos_whatsapp` — exatamente os comandos do RESEARCH.
    3. Assert por checagem: FALHA somente quando a resposta é 200 com corpo contendo linhas de dados (array JSON não-vazio ou string `org_`); erro de permissão (42501/403), PGRST205/404 e array vazio em POST rejeitado contam como ESPERADO — a nota A2 do RESEARCH manda assertar "não é 200 com linhas", nunca um código HTTP fixo.
    4. Aceita nomes de tabela como argumentos posicionais para rodar só as checagens daquelas tabelas (ex.: rodar apenas as de `assinaturas` nesta wave); sem argumento roda todas.
    5. Sai com exit 0 se tudo ESPERADO, exit 1 com resumo pt-BR listando cada checagem reprovada.
    Marcar executável (chmod +x). Mensagens em pt-BR.
  </action>
  <verify>
    <automated>bash scripts/verificar-superficie-anon.sh assinaturas; test $? -le 1</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/verificar-superficie-anon.sh` existe e é executável (`test -x` retorna 0)
    - `bash scripts/verificar-superficie-anon.sh assinaturas` roda apenas as checagens de assinaturas e imprime resultado por checagem (neste momento, ANTES da migration, a checagem de tenant_id em assinaturas deve REPROVAR — prova de que o script detecta superfície aberta, não só confirma fechada)
    - O script contém `grep -E '^NEXT_PUBLIC_SUPABASE_URL='` e não referencia nenhuma variável secreta (`SUPABASE_SECRET_KEY`, `SUPABASE_POSTGRES_PASSWORD` ausentes do arquivo)
    - `grep -rn "createBrowserClient" src/` retorna vazio (assunção A6 do RESEARCH provada antes de qualquer fechamento)
  </acceptance_criteria>
  <done>Harness pronto e provadamente capaz de detectar superfície aberta; A6 verificada.</done>
</task>

<task type="tracer">
  <name>Task 2: Fechar assinaturas ponta a ponta — código, migration, aplicação e prova</name>
  <files>src/app/actions/public-booking.ts, supabase/migrations/<ts>_revoga_anon_assinaturas.sql</files>
  <precondition>MCP do Supabase autenticado (mcp__supabase__list_migrations responde listando as 14 migrations) OU psql pelo pooler liberado no permissions.allow — pré-requisito do owner registrado no CONTEXT §"Pré-requisitos do owner"</precondition>
  <reversibility rating="reversible">REVOKE é revertível por um GRANT equivalente; banco é dev descartável com migration destrutiva autorizada pelo owner.</reversibility>
  <read_first>
    - src/app/actions/public-booking.ts (função obterDadosBookingPublico, linhas 230-290 — onde o plano vigente é lido)
    - src/lib/assinaturas.ts (obterPlanoVigentePublico recebe o client como parâmetro — a troca é no chamador)
    - supabase/migrations/20260709193156_restringe_colunas_assinaturas_anon.sql (precedente de migration manual de privilégio: cabeçalho pt-BR + SQL minúsculo)
    - .planning/phases/01-hardening-da-superf-cie-p-blica/01-CONTEXT.md §"Aplicação das mudanças no banco" (D-06: pooler aws-1, INSERT da version)
  </read_first>
  <action>
    Três movimentos, nesta ordem:

    1. **Código primeiro (per D-02):** em `obterDadosBookingPublico`, instanciar `createAdminClient()` (import já presente no arquivo) e passar o cliente admin — não o `supabase` anônimo — na chamada `obterPlanoVigentePublico(admin, perfil.tenant_id)`. Delta mínimo: nenhuma outra leitura muda neste plano (as demais migram no plano 01-02). O `tenant_id` passado vem de `perfil.tenant_id` resolvido no servidor a partir do slug — mitigação 1 da D-02 preservada.

    2. **Migration manual** `supabase/migrations/<timestamp>_revoga_anon_assinaturas.sql` (timestamp real no formato YYYYMMDDHHMMSS): revogar todos os privilégios de `public.assinaturas` da role `anon` (um único revoke all). Cabeçalho pt-BR explicando: escrita à mão porque `supabase db diff` não emite REVOKE (docs/SUPABASE_DECLARATIVE-DATABASE-SCHEMA.md), precedente `20260709193156`; e o porquê de negócio — o GRANT por coluna deixava `?select=tenant_id` devolver o org_id do Clerk de todo tenant pagante (Pitfall 1 do RESEARCH). NÃO tocar em `authenticated` nem `service_role` nesta migration. A policy anônima de `assinaturas` no schema declarativo fica para o plano 01-04 (limpeza junto das demais) — o portão já bloqueia.

    3. **Aplicar no cloud (per D-06):** via `mcp__supabase__apply_migration` com name igual ao nome do arquivo (preservando a version — memória do projeto: aplicar via MCP preservando a version). Conferir com `mcp__supabase__list_migrations` que a version nova aparece e que o total bate com os arquivos do repo.
  </action>
  <verify>
    <automated>bash scripts/verificar-superficie-anon.sh assinaturas && pnpm lint && pnpm test</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/actions/public-booking.ts` contém a chamada `obterPlanoVigentePublico` recebendo o cliente criado por `createAdminClient()` dentro de `obterDadosBookingPublico` (assertável por leitura do arquivo)
    - Migration nova existe em `supabase/migrations/` com timestamp válido e SEM as strings `authenticated` e `service_role` em nenhuma linha de revoke
    - `mcp__supabase__list_migrations` mostra 15 versions e a última corresponde ao arquivo novo
    - `bash scripts/verificar-superficie-anon.sh assinaturas` sai com exit 0 (checagem de tenant_id em assinaturas agora ESPERADO — sem linhas)
    - Com `pnpm dev` de pé e um slug real obtido via `mcp__supabase__execute_sql` (`select slug_gratuito from perfis_empresas limit 1`): `curl -s -o /dev/null -w '%{http_code}' http://localhost:3000/book/<slug>` retorna 200 — booking público intacto depois do REVOKE
    - `pnpm lint` e `pnpm test` verdes com saída real mostrada
  </acceptance_criteria>
  <done>Uma tabela fechada de verdade, verificada por curl anônimo real; pipeline de aplicação provado; booking público funcionando idêntico.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→PostgREST | chave publicável (anon) chega crua na Data API |
| Server Action→Postgres | service role bypassa RLS |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Information Disclosure | GET /rest/v1/assinaturas?select=tenant_id | high | mitigate | revoke all de anon em assinaturas (Task 2, migration manual) — verificado por curl real |
| T-01-02 | Elevation of Privilege | obterPlanoVigentePublico com admin client | medium | mitigate | tenant_id sempre resolvido no servidor a partir do slug (perfil.tenant_id de obterDadosBookingPublico), nunca do browser |
</threat_model>

<verification>
- `bash scripts/verificar-superficie-anon.sh assinaturas` exit 0
- `/book/<slug>` responde 200 com wizard intacto
- `pnpm lint && pnpm test` verdes
</verification>

<success_criteria>
- Vazamento de org_id via assinaturas fechado e provado por curl anônimo
- Pipeline migration→MCP→cloud→version registrado funcionando
- Harness de verificação pronto para as waves seguintes
</success_criteria>

<output>
Create `.planning/phases/01-hardening-da-superf-cie-p-blica/01-01-SUMMARY.md` when done
</output>
