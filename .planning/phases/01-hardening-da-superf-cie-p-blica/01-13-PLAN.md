---
phase: 01-hardening-da-superf-cie-p-blica
plan: 13
type: execute
wave: 4
depends_on: ["01-11", "01-12"]
gap_closure: true
files_modified:
    - docs/PENDENCIAS.md
    - .planning/phases/01-hardening-da-superf-cie-p-blica/01-CONTEXT.md
    - .planning/phases/01-hardening-da-superf-cie-p-blica/COVERAGE.md
autonomous: true
requirements: [SEG-05]

must_haves:
    truths:
        - "`docs/PENDENCIAS.md` deixa de descrever como aberto o que a fase fechou: a autenticação por assinatura do QStash é registrada como implementada, apontando `src/lib/qstash-assinatura.ts` e o plano que a entregou, e o fallback `'secret-key'` é registrado como extinto com o comando que prova"
        - "O risco que CONTINUA vivo passa a existir por escrito: a chave de assinatura já circulou em URL publicada e em log, e por isso precisa ser rotacionada — item com as duas etapas, dono nomeado (o owner) e prazo datado"
        - "A rotação NÃO é marcada como feita por este plano nem por nenhum executor: só o owner mexe no painel da Upstash, e o item nasce aberto"
        - "A premissa técnica que sustentou o deferimento no `01-CONTEXT.md` fica corrigida COM o histórico preservado: o texto original permanece legível, anotado com o que foi medido, por quem e quando — decisão que lê como certa hoje mas nasceu de premissa falsa é a coisa mais cara de deixar no registro"
        - "A linha `Client.publishJSON` de `COVERAGE.md` deixa de carregar um motivo que o verificador provou falso; a decisão é reafirmada ou trocada, mas com uma razão de uma linha que sobrevive a escrutínio"
        - "Os quatro achados do review não aprovados nesta rodada (WR-01, WR-03, WR-04, WR-06) ficam registrados como deferimentos com razão escrita e gatilho de retorno — nenhum some em silêncio, e o de menor custo com maior aderência ao tema da fase é apontado como tal"
        - "Os sete itens de UAT humano continuam abertos e não marcados; a contagem `7 abertas / 0 marcadas` é conferida antes e depois da edição"
    artifacts:
        - "docs/PENDENCIAS.md — item de webhook reescrito, item novo de rotação de chave datado, quatro deferimentos registrados"
        - ".planning/phases/01-hardening-da-superf-cie-p-blica/01-CONTEXT.md — deferimento anotado com a medição que refuta a premissa"
        - ".planning/phases/01-hardening-da-superf-cie-p-blica/COVERAGE.md — motivo do OPT-OUT de `Client.publishJSON` corrigido"
    key_links:
        - "`src/lib/qstash-assinatura.ts` (plano 01-03) → item de webhook em `docs/PENDENCIAS.md`: de aberto para fechado, com o artefato nomeado"
        - "`src/lib/whatsapp-helper.ts` (plano 01-11) → item novo de rotação: a etapa 1 está feita e escrita como feita; a etapa 2 é do owner e nasce aberta"
        - "`01-REVIEW.md` §WR-01/WR-03/WR-04/WR-06 → seção de deferimentos em `docs/PENDENCIAS.md`: o caminho de volta de cada um"
    prohibitions:
        - "Nenhum item da lista de UAT humano pode ser marcado como concluído por um executor — verificação visual não se infere de código HTTP nem de suíte de teste"
        - "A rotação de chave na Upstash não pode ser marcada como feita, nem descrita como feita, por nenhum executor"
        - "Nunca reproduzir valor de chave, token ou segredo em documento, commit, PR ou mensagem — o item de rotação nomeia a VARIÁVEL, nunca o valor"
        - "Nunca apagar o texto original de uma decisão de `01-CONTEXT.md`: correção é anotação com histórico, não reescrita — decisão registrada do plano 01-09"
        - "Nunca registrar como fechado nada que este plano não tenha visto fechar com comando: cada afirmação de fechamento carrega o comando e a saída"
        - "Nunca introduzir Prisma, Drizzle, better-auth ou Mercado Pago; nunca usar `npm`/`yarn`; nunca rodar `supabase start`/`stop`/`db reset` sem `--linked` nem `docker compose up`"
---

<objective>
Fazer os documentos dizerem o que o código diz — e, mais importante, registrar o que continua aberto.

`docs/PENDENCIAS.md:1071-1077` afirma hoje duas coisas falsas: que o fallback `'secret-key'` "vale nos dois lados" (extinto — `grep -rn "secret-key" src/ scripts/` volta vazio, conferido pelo verificador) e que "o ideal é migrar para verificação da assinatura real do QStash" (feito no plano 01-03). Ao mesmo tempo, a parte que CONTINUA verdadeira e virou o achado mais grave da fase — a chave de assinatura circulando em URL publicada — não aparece como pendência viva em seção nenhuma. O item 6 da Definition of Done do projeto exige esse arquivo coerente, e o resto do documento é exemplar nisso: a seção de superfície remanescente registra fechamento com medição. Esta entrada é uma inconsistência isolada, e é o gap 3 da reverificação (WR-05 do review).

**A etapa 1 está feita; a etapa 2 é do owner e não tem como ser feita hoje.** O plano 01-11 parou de publicar a chave. Mas a chave atual já circulou — em URL publicada, no console da Upstash (até 14 dias) e possivelmente em log de acesso de cada hop. Precisa ser considerada comprometida e rotacionada no painel da Upstash **depois que a fila secar**. Isso é ação humana, com prazo, fora do alcance de qualquer executor. O que este plano pode fazer é a única coisa que impede o item de evaporar: escrevê-lo com dono, prazo e as duas etapas separadas.

**Duas premissas falsas ainda no registro.** O `01-CONTEXT.md:176` difere a remoção do parâmetro alegando que "removê-lo agora exigiria lidar com duas gerações de URL". O verificador provou que não: `route.ts:29` valida contra `req.url`, então mensagens em voo casam com a própria URL de publicação, carreguem o parâmetro ou não. A mesma premissa sustenta o OPT-OUT de `Client.publishJSON` em `COVERAGE.md`. Decisão que hoje até pode continuar valendo, mas cuja razão escrita foi refutada, é dívida de registro: o próximo leitor confia na razão, não na decisão.

Purpose: fechar o gap 3 e deixar de dever ao registro aquilo que a rodada aprendeu.
Output: um documento de pendências coerente, um risco vivo com prazo, duas premissas corrigidas com histórico preservado e quatro deferimentos com caminho de volta.
</objective>

## ⛔ Serialização obrigatória — este plano roda SOZINHO na wave 4

Um plano por wave. Este é o plano que **escreve depois da prova**: ele só pode rodar quando 01-11 e 01-12 já estiverem concluídos, porque afirma por escrito que a chave saiu da URL e que a travessia foi consertada. Escrever antes seria repetir exatamente o defeito que queimou esta fase — critério que lê como satisfeito enquanto a medição diz o contrário.

## Gate de reexecução (obrigatório antes do primeiro Edit)

Antes de editar qualquer arquivo, rode e cole a saída real no SUMMARY:

1. `grep -vE '^\s*(//|\*|/\*)' src/lib/whatsapp-helper.ts | grep -c '?secret'` → deve devolver `0`
2. `grep -rn "secret-key" src/ scripts/` → deve voltar vazio
3. `bash scripts/verificar-travessia-server-action.sh` → deve sair 0
4. `bash scripts/verificar-fail-fast-boot.sh` → deve sair 0

Qualquer um falhando: **PARE** e devolva o estado ao owner sem editar documento nenhum. É o mesmo gate do plano 01-09, e existe pelo mesmo motivo.

## Artefatos que este plano produz

| Símbolo / arquivo | Tipo | Onde nasce |
|---|---|---|
| Item "Rotação das signing keys do QStash" | pendência datada com dono | `docs/PENDENCIAS.md` |
| Seção "Achados do code review da Phase 1 diferidos" | bloco de deferimentos | `docs/PENDENCIAS.md` |

Nenhum símbolo de código. É deliberado: este plano não toca `src/`.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-hardening-da-superf-cie-p-blica/01-VERIFICATION.md
@.planning/phases/01-hardening-da-superf-cie-p-blica/01-REVIEW.md
@.planning/phases/01-hardening-da-superf-cie-p-blica/01-11-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: O item do webhook passa a descrever o mundo como ele é, e o resíduo ganha prazo</name>
  <files>docs/PENDENCIAS.md</files>
  <precondition>Os planos 01-11 e 01-12 estão concluídos e commitados, e os quatro comandos do gate de reexecução acima passaram sobre o HEAD atual. Sem isso, este plano escreve afirmação que a medição não sustenta — pare e devolva ao owner.</precondition>
  <read_first>
    - `docs/PENDENCIAS.md` linhas 1050-1090 (o item de revisão de segurança a reescrever; ancore por CONTEÚDO, não por número de linha — o hook de prettier e as edições anteriores deslocam numeração)
    - `docs/PENDENCIAS.md` linhas 640-660 (a seção "Superfície remanescente", que registra fechamento com medição — é o MOLDE de qualidade a replicar, não invente formato novo)
    - `docs/PENDENCIAS.md` linhas 800-880 (os sete itens de UAT humano — são intocáveis por este plano; a contagem é asserção)
    - `docs/PENDENCIAS.md` linha 999 (o item que **já cita** `src/lib/qstash-assinatura.ts:42`, sobre o `throw` da verificação — leia antes de escrever o item novo: é por causa dele que o critério de aceite é relativo e não `>= 1`, e é ele que você não deve confundir com o item que esta task cria)
    - `.planning/phases/01-hardening-da-superf-cie-p-blica/01-REVIEW.md` §CR-01 (os três vetores de exposição concretos, para o item novo não virar vago)
    - `.planning/phases/01-hardening-da-superf-cie-p-blica/01-VERIFICATION.md` (gap 1 e gap 3 no frontmatter — a lista `missing` do gap 3 é literalmente a especificação desta tarefa)
    - `.planning/phases/01-hardening-da-superf-cie-p-blica/01-11-SUMMARY.md` (o que o plano 01-11 de fato entregou, para citar com precisão)
    - `src/lib/qstash-assinatura.ts` (o artefato a apontar como prova de que a autenticação por assinatura está implementada)
  </read_first>
  <action>
**(a) Reescreva o item de webhook** dentro de "Revisão de segurança geral". Ele deixa de ser um item aberto sobre o secret em query string e passa a registrar, no molde da seção "Superfície remanescente":

- que a autenticação passou a ser por assinatura criptográfica do QStash (`Upstash-Signature` verificado por `Receiver`), nomeando `src/lib/qstash-assinatura.ts`, o plano que entregou (01-03) e o veredito `WEBHOOK` de `scripts/verificar-fail-fast-boot.sh` (`401,401,401,200`) como a evidência que se pode reexecutar;
- que o fallback inseguro embutido no código foi extinto, com o comando que prova (o `grep` do gate de reexecução) e a saída vazia;
- que a fase também passou a exigir as chaves de assinatura no boot, com o veredito `MORTE` como prova.

Preserve a análise que justificou o achado original — não apague o histórico, marque-o como resolvido. É o mesmo padrão do plano 01-09.

**(b) Abra o item novo, que é o resíduo real.** Título claro sobre a rotação das signing keys do QStash. Conteúdo obrigatório:

- **Por que existe:** a chave de assinatura foi publicada em texto claro na URL de destino de todo lembrete até o plano 01-11. É a mesma chave com que o webhook autentica, e HMAC é simétrico — quem a leu forja assinatura válida. Nomeie os três vetores concretos do CR-01: log de acesso HTTP de cada hop entre QStash e Railway; console da Upstash, onde a URL de destino fica visível na listagem por até 14 dias; e o log da aplicação, que recebia o corpo de erro do QStash (também fechado no 01-11).
- **Etapa 1 — FEITA.** Parar de publicar o parâmetro. Nomeie `src/lib/whatsapp-helper.ts`, o plano 01-11 e o comando de verificação. Registre também, em uma frase, por que isso não mata lembrete em voo: a verificação usa `req.url`, então cada mensagem valida contra a URL com que foi publicada.
- **Etapa 2 — ABERTA, do owner, com prazo.** Rotacionar `QSTASH_CURRENT_SIGNING_KEY` e `QSTASH_NEXT_SIGNING_KEY` no painel da Upstash **depois que a fila secar**. Data-limite: **2026-08-05** (14 dias após 2026-07-22, o horizonte máximo de agendamento de lembrete no QStash). Escreva por que a espera é necessária: rotacionar com a fila cheia invalida a assinatura dos lembretes já publicados e os mata em silêncio, que é o modo de falha que a mensageria deste projeto tem por desenho. Escreva também o que fazer depois de rotacionar: atualizar as duas variáveis no Railway e no `.env.local`, e reexecutar `scripts/verificar-fail-fast-boot.sh`.
- **Quem pode fechar:** só o owner. Deixe isso escrito no item, do mesmo jeito que a seção de UAT humano faz.

Nomeie **variáveis**, nunca valores. Nenhum segredo entra em documento.

**(c) Não toque nos sete itens de UAT humano.** Conte antes e depois.
  </action>
  <verify>
    <automated>grep -q '2026-08-05' docs/PENDENCIAS.md && test "$(grep -c 'qstash-assinatura.ts' docs/PENDENCIAS.md)" -ge 2 && ! grep -q 'o ideal é migrar para verificação da assinatura real' docs/PENDENCIAS.md</automated>
  </verify>
  <acceptance_criteria>
    - O item novo existe e é datado: `grep -c '2026-08-05' docs/PENDENCIAS.md` devolve valor `>= 1` (medido no planejamento: `0`)
    - O item novo aponta o artefato de código — critério RELATIVO, porque `qstash-assinatura.ts` **já é citado hoje** em `docs/PENDENCIAS.md:999`, num item anterior sobre o `throw` da verificação: `grep -c 'qstash-assinatura.ts' docs/PENDENCIAS.md` sobe de `1` para `>= 2`. Meça os dois valores e cole no SUMMARY a saída de `grep -n`, com os números de linha de TODAS as ocorrências — é o que prova que a citação nova está dentro do item de webhook reescrito, e não que ela já existia em outro lugar do arquivo. Um critério `>= 1` aqui passaria com o documento intocado
    - Alvo ABSOLUTO — o texto stale sumiu: `grep -c 'o ideal é migrar para verificação da assinatura real' docs/PENDENCIAS.md` devolve `0` (medido: `1`)
    - Alvo ABSOLUTO — nenhum valor de segredo entrou no arquivo: `grep -cE 'sig_|qstash_[A-Za-z0-9]{16,}' docs/PENDENCIAS.md` devolve `0` (medido: `0`; o critério é de não-regressão)
    - Os sete itens de UAT continuam abertos e nenhum marcado: dentro da seção "UAT humano pendente da Phase 1", a contagem de `- [ ]` é `7` e a de `- [x]` é `0` — meça com `awk` delimitando a seção e cole os dois números no SUMMARY, antes e depois da edição
    - A saída dos quatro comandos do gate de reexecução está colada no SUMMARY, feita ANTES do primeiro Edit
  </acceptance_criteria>
  <done>O item do webhook descreve o código atual com evidência reexecutável; o resíduo existe como item aberto, com dono, prazo e as duas etapas separadas; nenhum item de UAT foi tocado.</done>
</task>

<task type="auto">
  <name>Task 2: As duas premissas refutadas são corrigidas com histórico, e os quatro deferimentos ficam por escrito</name>
  <files>.planning/phases/01-hardening-da-superf-cie-p-blica/01-CONTEXT.md, .planning/phases/01-hardening-da-superf-cie-p-blica/COVERAGE.md, docs/PENDENCIAS.md</files>
  <read_first>
    - `.planning/phases/01-hardening-da-superf-cie-p-blica/01-CONTEXT.md` §"Deferred Ideas" (o primeiro bullet, sobre trocar o parâmetro da URL de publicação — o texto original tem de sobreviver)
    - `.planning/phases/01-hardening-da-superf-cie-p-blica/COVERAGE.md` (a tabela do QStash; a linha `Client.publishJSON` é a única a mexer)
    - `.planning/phases/01-hardening-da-superf-cie-p-blica/01-REVIEW.md` §WR-01, §WR-03, §WR-04, §WR-06 (o conteúdo de cada deferimento, com arquivo e linha)
    - `src/app/api/webhooks/lembrete/route.ts` linhas 15-36 (a medição que refuta a premissa — confira antes de escrever que ela é falsa)
    - `docs/PENDENCIAS.md` (o local onde a seção de deferimentos entra; siga a organização existente do arquivo em vez de criar seção órfã no fim)
  </read_first>
  <action>
**(a) Corrija o deferimento do `01-CONTEXT.md` sem apagá-lo.** O bullet original permanece legível; acrescente logo abaixo uma nota de correção datada dizendo: (i) que a premissa "removê-lo agora exigiria lidar com duas gerações de URL" foi medida como **falsa** na reverificação de 2026-07-22; (ii) por quê — a verificação da assinatura usa `req.url`, então cada mensagem valida contra a URL com que foi publicada e as duas gerações convivem sem conflito; (iii) que o deferimento foi consequentemente revertido e o item entregue no plano 01-11. Assine a nota com a origem (reverificação sobre o HEAD `4596463`), do mesmo jeito que a correção de `.env.example` no próprio `01-CONTEXT.md:95-97` faz — esse bloco é o molde e está no mesmo arquivo.

**(b) Corrija a linha `Client.publishJSON` de `COVERAGE.md`.** A decisão continua sendo OPT-OUT, mas o motivo muda, porque o atual foi refutado. O motivo novo tem de ser verdadeiro e de uma linha: o publisher por `fetch` cru já existe, é coberto por teste e a remoção do parâmetro não dependia do SDK — trocar o transporte agora acrescentaria superfície sem fechar critério nenhum; reavaliar quando houver necessidade real de retry/DLQ, que é escopo da Phase 11. Se ao ler a tabela o executor concluir que INTEGRATE passou a ser a decisão certa, é legítimo trocar — desde que o motivo escrito também sobreviva a escrutínio. Não deixe nenhuma linha da tabela sem decisão nem nenhum OPT-OUT sem motivo: o portão de selagem valida esse arquivo inteiro.

**(c) Registre os quatro deferimentos em `docs/PENDENCIAS.md`.** Uma seção, quatro itens, cada um com: o identificador do review, arquivo e linha, o que acontece se não for feito (consequência concreta, não rótulo), por que não entrou nesta rodada e o gatilho que o traz de volta. Conteúdo mínimo de cada:

- **WR-01** — a Server Action pública devolve `tenant_id` e `slug_gratuito` ao chamador (`src/app/actions/public-booking.ts`). Quem invocar a action diretamente com um slug válido recebe o `org_id` do Clerk daquele tenant. **Registre explicitamente que este é o mais barato dos quatro e o mais aderente ao tema da fase**: é exatamente a informação que o Success Criteria 1 protege na superfície da Data API, obtida por outra porta; o conserto é desestruturar o retorno mantendo `tenant_id` na projeção (o filtro depende dele) e não devolvê-lo. Não entrou porque não foi aprovado nesta rodada — não porque seja difícil. Gatilho: a próxima rodada de hardening, ou antes disso se o owner quiser puxá-lo.
- **WR-03** — caminho de escrita público sem limite de tamanho em `clienteNome` e sem validação de formato em `clienteEmail`. Consequência: linha de tamanho arbitrário em `clientes`, e o nome entra direto no template do WhatsApp — um nome de 100 kB vira uma mensagem de 100 kB na instância Evolution do profissional. Distinto do rate limiting já registrado. Gatilho: Phase 3 (anti-abuso) ou Phase 5 (contato flexível), o que vier primeiro.
- **WR-04** — a verificação por `req.url` pode recusar todas as requisições atrás do proxy da Railway se protocolo, host ou barra final divergirem do que o QStash assinou, e o caminho de falha é inteiramente silencioso. Consequência: lembrete nenhum sai e ninguém fica sabendo. Gatilho: o item de UAT "Lembrete do QStash ponta a ponta" — se um 401 aparecer no log, este item vira urgente.
- **WR-06** — falha de transporte do WhatsApp devolve 500 e o QStash reenvia, sem checagem de idempotência contra `disparos_whatsapp`. Consequência: o cliente final recebe o mesmo lembrete duas ou três vezes, o que num produto cujo diferencial é o WhatsApp custa reputação do profissional. Gatilho: primeiro piloto com volume real, ou a Phase 11 (observabilidade).

Escreva cada um como decisão adiada, nunca como esquecimento. Nenhum deles é fechado, marcado ou minimizado.
  </action>
  <verify>
    <automated>grep -q 'WR-01' docs/PENDENCIAS.md && grep -q 'WR-06' docs/PENDENCIAS.md && test "$(grep -c 'req.url' .planning/phases/01-hardening-da-superf-cie-p-blica/01-CONTEXT.md)" -ge 2</automated>
  </verify>
  <acceptance_criteria>
    - **Meça as contagens abaixo ANTES de editar** e anote inicial e final no SUMMARY. Valores entre parênteses medidos no planejamento (HEAD `0ed1125`). Todo caminho de arquivo aqui é a partir da raiz do repositório — nada de `COVERAGE.md` solto
    - Os quatro deferimentos existem: `grep -cE 'WR-01|WR-03|WR-04|WR-06' docs/PENDENCIAS.md` devolve valor `>= 4` (medido: `0`)
    - INVARIANTE — o texto original do deferimento sobreviveu no `01-CONTEXT.md`: `grep -c 'duas gerações de URL' .planning/phases/01-hardening-da-superf-cie-p-blica/01-CONTEXT.md` devolve o mesmo valor medido no início da task (medido: `1`)
    - A correção foi anexada — critério RELATIVO, porque `req.url` **já aparece hoje** no `01-CONTEXT.md` (linha 80, dentro da D-05, e é justamente o fato que refuta a premissa): `grep -c 'req.url' .planning/phases/01-hardening-da-superf-cie-p-blica/01-CONTEXT.md` sobe de `1` para `>= 2`. Meça os dois valores e cole-os no SUMMARY; um critério `>= 1` aqui seria satisfeito sem nenhuma edição
    - A nota de correção é datada e atribuída: `grep -c '2026-07-22' .planning/phases/01-hardening-da-superf-cie-p-blica/01-CONTEXT.md` devolve valor MAIOR que o medido no início da task (medido: `0`)
    - Toda linha da tabela de `COVERAGE.md` tem decisão e todo OPT-OUT tem motivo — INVARIANTE: `grep -c 'OPT-OUT' .planning/phases/01-hardening-da-superf-cie-p-blica/COVERAGE.md` devolve o mesmo valor medido no início da task (medido: `7` — seis linhas da tabela do QStash mais a menção na regra final "todo OPT-OUT com motivo de uma linha"). **Se a decisão da linha `Client.publishJSON` for trocada para INTEGRATE, o valor cai para `6` e isso é esperado — registre a mudança e o motivo no SUMMARY.** Em nenhum caso apague linha da tabela para fazer a contagem bater: o portão de selagem valida este arquivo, e mutilá-lo é dano maior que o defeito
    - A razão refutada sumiu: `grep -c 'colide com a fila em trânsito' .planning/phases/01-hardening-da-superf-cie-p-blica/COVERAGE.md` devolve `0` (medido: `1`)
    - Os sete itens de UAT continuam `7` abertos e `0` marcados
    - Nenhum arquivo de `src/` foi tocado: `git diff --name-only` não lista nada sob `src/`
  </acceptance_criteria>
  <reversibility rating="reversible">Correção de registro documental; reverter é um `git revert` e o texto original nunca foi apagado.</reversibility>
  <done>As duas premissas refutadas estão corrigidas com o texto original preservado; os quatro deferimentos têm consequência concreta e gatilho de volta; `COVERAGE.md` não carrega mais motivo falso.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repositório versionado → qualquer leitor | `docs/PENDENCIAS.md` e `.planning/` são texto público do repo: nenhum segredo pode entrar |
| registro de risco → memória organizacional | Risco que não está escrito deixa de existir quando a sessão fecha; é o modo de falha que este plano combate |

## STRIDE Threat Register (ASVS L1 · bloqueia em `high`)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-13-01 | Repudiation | rastreio da rotação de chave comprometida | high | mitigate | A etapa 1 sozinha não fecha o risco: a chave já circulou. Sem item datado com dono, a rotação evapora com a sessão e a chave comprometida vira permanente. Item com data-limite `2026-08-05`, dono nomeado (owner) e critério de fechamento escrito |
| T-01-13-02 | Information Disclosure | `docs/PENDENCIAS.md` | medium | mitigate | Documento versionado e público: o item nomeia as VARIÁVEIS (`QSTASH_CURRENT_SIGNING_KEY`, `QSTASH_NEXT_SIGNING_KEY`), nunca valores. Critério de aceite com `grep` por padrão de segredo |
| T-01-13-03 | Repudiation | registro de decisão com premissa refutada | medium | mitigate | Apagar o texto original esconderia que a decisão nasceu de premissa falsa, e o próximo leitor confiaria na razão. Correção é anotação com histórico e atribuição, com critério de aceite exigindo o texto original ainda presente |
| T-01-13-04 | Repudiation | quatro achados de review não aprovados | medium | mitigate | Achado sem registro reaparece como surpresa numa fase futura. Cada um com consequência concreta e gatilho de retorno; WR-01 marcado como o mais barato e mais aderente ao tema, para o owner poder puxá-lo com informação |
| T-01-13-SC | Tampering | instalação de pacotes (npm/pnpm) | low | accept | Este plano não toca `src/` nem instala pacote nenhum |
</threat_model>

<verification>
1. Os quatro comandos do gate de reexecução, rodados ANTES do primeiro Edit, com saída colada
2. `grep -cE 'WR-01|WR-03|WR-04|WR-06' docs/PENDENCIAS.md` → `>= 4`
3. `grep -c '2026-08-05' docs/PENDENCIAS.md` → `>= 1`
4. `grep -c 'o ideal é migrar para verificação da assinatura real' docs/PENDENCIAS.md` → `0`
5. `grep -c 'colide com a fila em trânsito' .planning/phases/01-hardening-da-superf-cie-p-blica/COVERAGE.md` → `0`
6. Contagem da seção de UAT: `7` abertos, `0` marcados, antes e depois
7. `git diff --name-only` não lista nada sob `src/`
</verification>

<success_criteria>
- `docs/PENDENCIAS.md` descreve o webhook de lembrete como ele é hoje, com evidência reexecutável
- O risco residual existe por escrito, com dono, prazo e etapa 2 aberta
- As duas premissas refutadas estão corrigidas sem apagar o histórico
- Os quatro deferimentos têm razão e gatilho de volta; nenhum sumiu em silêncio
</success_criteria>

<output>
Create `.planning/phases/01-hardening-da-superf-cie-p-blica/01-13-SUMMARY.md` when done
</output>
