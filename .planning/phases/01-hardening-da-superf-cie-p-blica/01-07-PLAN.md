---
phase: 01-hardening-da-superf-cie-p-blica
plan: 07
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - vitest.config.ts
  - package.json
  - src/app/actions/__tests__/public-booking-escrita.test.ts
autonomous: true
requirements: [SEG-01, SEG-02]

must_haves:
  truths:
    - "O caminho de ESCRITA do booking público é exercitado de verdade contra o Supabase de dev: `criarAgendamentoPublico` cria cliente novo, grava o agendamento e devolve a linha (o `RETURNING` do `.select()` funciona sob as policies e privilégios pós-Phase 1)"
    - "O contrato por slug do plano 01-02 está provado ponta a ponta: as duas Server Actions públicas recebem `slug` e resolvem o `tenant_id` no servidor (per D-04) — nenhum identificador de tenant vem do chamador"
    - "Cliente existente é reaproveitado por telefone: agendar duas vezes com o mesmo número no mesmo tenant não cria segunda linha em `clientes`, e o segundo agendamento aponta para o mesmo `cliente_id`"
    - "A sanitização de telefone é exercitada: entrada formatada com parênteses, espaço e hífen grava só dígitos em `clientes.telefone`"
    - "O acoplamento por substring que sustenta a recuperação de double-booking está pinado nas DUAS pontas: a action produz a mensagem contendo 'já foi preenchido' e `BookingApp.tsx` casa exatamente essa substring — uma reescrita futura quebra o teste, não a UX em silêncio"
    - "A cópia da caixa de erro de slots é produzida verbatim quando o slug não resolve: `Não foi possível carregar os horários. Tente de novo.` (per D-04 — o caminho de falha nasceu com a troca de identificador)"
    - "A suíte é hermética quanto a rede de terceiro (mensageria e analytics mockadas) e limpa tudo que cria; rodar duas vezes seguidas dá o mesmo resultado"
    - "Sem credenciais, `pnpm test` continua verde — mas o pulo é BARULHENTO: banner em stderr e um comando dedicado (`pnpm test:integracao`) que REPROVA em vez de pular"
    - statement: "A recuperação visual do double-booking no navegador — aviso âmbar, grade refeita, cliente de volta na etapa de data/hora — continua sem prova automatizada; o teste cobre só o lado servidor do acoplamento"
      verification: backstop
  artifacts:
    - "src/app/actions/__tests__/public-booking-escrita.test.ts"
    - "vitest.config.ts — `resolve.alias` para `@`"
    - "package.json — script `test:integracao`"
  key_links:
    - "vitest → alias `@` → `src/app/actions/public-booking.ts` (sem o alias o módulo nem importa: ele usa `@/lib/...` e o `vitest.config.ts` hoje não tem `resolve.alias`)"
    - "teste → `createAdminClient()` → Supabase de dev (as credenciais saem de `.env.local`, lidas linha a linha, só as duas chaves necessárias)"
    - "`public-booking.ts` (mensagem com 'já foi preenchido') ↔ `BookingApp.tsx` (`mensagem.includes(...)`) — asserção nas duas pontas"
  prohibitions:
    - statement: "Nenhuma mensagem de erro das duas Server Actions públicas pode ser reescrita — o contrato de copy do 01-UI-SPEC §'Acoplamento de string' e a recuperação de double-booking dependem do texto literal"
      status: flagged-unverified
    - statement: "Proibido `select('*')` no caminho público (mitigação 2 da D-02) — o teste não pode afrouxar as projeções explícitas para facilitar asserção"
      status: flagged-unverified
    - statement: "Nenhum framework de teste novo: só vitest. Sem Playwright, sem supertest, sem dotenv — nenhuma dependência nova em `package.json`"
      status: flagged-unverified
    - statement: "Nenhum valor de `.env.local` pode ser ecoado em log, mensagem de erro, snapshot ou asserção — só os NOMES das variáveis"
      status: flagged-unverified
    - statement: "`mcp__supabase__apply_migration` está proibido neste projeto; este plano não toca schema nem migrations"
      status: flagged-unverified
    - statement: "`service_role` nunca aparece em nenhuma linha de `revoke`"
      status: flagged-unverified
---

<objective>
Fechar o **Gap 2** da `01-VERIFICATION.md` (SC2b, marcado `PRESENT_BEHAVIOR_UNVERIFIED`): toda a prova automatizada da Phase 1 cobre **leitura** — `/book/avantis` → 200 e o canário `/book/ozm317u4` → 404. O caminho de **escrita** (lookup/criação de cliente por telefone, INSERT do agendamento com `RETURNING` sob as policies novas) não é exercitado por teste nenhum e nenhum humano o percorreu. Isso depois de o plano 01-02 ter trocado o identificador que as duas Server Actions públicas recebem (`tenantId` → `slug`, per D-04). Regressão aqui **degrada em silêncio**: agenda vazia, sem erro.

Decisão do owner (travada, não relitigar): **teste de integração em vitest contra o Supabase de dev**. O banco é explicitamente descartável (constraint do `.claude/CLAUDE.md` e `docs/RESET_AMBIENTE_DEV.md`), e o Core Value do milestone — "um agendamento real cair na agenda do profissional sem que nada quebre no caminho" — é literalmente o que este teste percorre.

Descoberta desta análise, e é bloqueante: **`vitest.config.ts` não tem `resolve.alias`**. As suítes existentes vivem em `src/lib/__tests__/` e importam por caminho relativo; `public-booking.ts` importa por `@/lib/...`. Sem o alias o módulo nem carrega. Isso vem primeiro.

Purpose: transformar a regressão mais cara desta fase — a que o CONTEXT §specifics chama de "obrigatória e não negociável" — de dívida de olho humano em comando que roda em toda alteração.
Output: suíte de integração da escrita pública, alias de módulo no vitest, comando dedicado que reprova quando não pode provar.
</objective>

## Suposições explícitas — arestas não resolvidas do probe de edge

O probe determinístico devolveu **5 arestas aplicáveis, 0 resolvidas**; quatro vieram `unclassified` e uma como `concurrency`. O classificador é anglófono e os requisitos estão em pt-BR — trate a classificação como sinal fraco. Este plano carrega duas; nenhuma é descartada em silêncio (rollup completo no plano 01-09).

- **[SUPOSIÇÃO A-SEG-01 — `concurrency`, não resolvida]** *"Se interrompido ou rodando em paralelo, o que é garantido?"* — **esta suíte NÃO resolve a aresta.** O caso de double-booking é sequencial por construção: o primeiro agendamento é gravado, e só então o segundo tenta o mesmo `datetime`. Duas requisições **simultâneas** ainda podem gerar dois agendamentos ativos sobrepostos — a janela de corrida vai da leitura da engine ao INSERT, é reconhecida em `docs/PENDENCIAS.md` §"Prevenção atômica de double-booking" e é escopo de AGE-03 na Phase 2. Fica sinalizada, não fechada. Não escrever teste de concorrência aqui: ele reprovaria por um defeito conhecido e planejado para outra fase.
- **[SUPOSIÇÃO A-SEG-02 — `unclassified`, não resolvida]** A suíte prova que o caminho público continua lendo o que precisa **com o cliente privilegiado** depois do `REVOKE` total de `anon` (per D-01/D-02), mas não prova nada sobre a superfície `anon` em si — isso é o `scripts/verificar-superficie-anon.sh`, re-executado no plano 01-09.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-hardening-da-superf-cie-p-blica/01-VERIFICATION.md
@.planning/phases/01-hardening-da-superf-cie-p-blica/01-CONTEXT.md
@.planning/phases/01-hardening-da-superf-cie-p-blica/01-UI-SPEC.md
@CLAUDE.md
</context>

## Artifacts this phase produces (plan 01-07)

- `src/app/actions/__tests__/public-booking-escrita.test.ts` — suíte nova. Símbolos internos: `TENANT_TESTE` (const, valor `org_teste_integracao_booking`), `lerCredenciaisSupabase()`, `prepararTenantDeTeste()`, `limparTenantDeTeste()`
- `vitest.config.ts` — bloco `resolve.alias` novo mapeando `@` para `./src`
- `package.json` — script novo `test:integracao`
- Linhas novas no banco de dev, **efêmeras**: um tenant `org_teste_integracao_booking` com um serviço e sete janelas de funcionamento, removido no teardown. Nenhuma migration, nenhuma mudança de schema
- Nenhuma dependência nova

<tasks>

<task type="tracer">
  <name>Task 1: Fatia ponta a ponta — um agendamento público real, do slug à linha no banco</name>
  <files>vitest.config.ts, src/app/actions/__tests__/public-booking-escrita.test.ts</files>
  <reversibility rating="reversible">Arquivo de teste novo e duas linhas de configuração; o tenant de teste é removido pelo próprio teardown e não há migration envolvida.</reversibility>
  <read_first>
    - `vitest.config.ts` (17 linhas — o bloco `env` existente e o comentário que explica por que ele existe: constantes de módulo avaliadas no import. Note que NÃO há `resolve.alias`)
    - `src/app/actions/public-booking.ts` (INTEIRO — `resolverPerfilPublicoPorSlug`, `criarAgendamentoPublico` e `obterSlotsPublicos`; repare que `createAdminClient()` é chamado DENTRO de cada função, então as variáveis de ambiente só precisam existir no momento da chamada, não no import)
    - `src/lib/supabase/admin.ts` (`createAdminClient()` lê `SUPABASE_SECRET_KEY` e `NEXT_PUBLIC_SUPABASE_URL` de `process.env` na chamada, e lança sem a chave)
    - `supabase/schemas/01_perfis_empresas.sql` (colunas obrigatórias do perfil: `tenant_id`, `slug` UNIQUE, `slug_gratuito`, `nome_estabelecimento`; e os CHECKs de `antecedencia_minima_minutos` e `horizonte_maximo_dias`)
    - `supabase/schemas/02_servicos.sql`, `supabase/schemas/03_horarios_funcionamento.sql`, `supabase/schemas/06_clientes.sql`, `supabase/schemas/07_agendamentos.sql` (as FKs: `tenant_id` referencia `perfis_empresas` com `ON DELETE CASCADE` nas quatro; mas `agendamentos.servico_id` referencia `servicos` com `ON DELETE RESTRICT` — isso decide a ORDEM da limpeza)
    - `src/lib/planos.ts` §`obterSlugEfetivo` e `src/lib/assinaturas.ts` §`obterPlanoVigentePublico` (sem linha em `assinaturas` o plano vigente é `gratuito`, e no gratuito o slug efetivo é o `slug_gratuito` — é por ele que o teste precisa acessar)
    - `scripts/verificar-superficie-anon.sh` linhas 44-64 (o precedente do projeto para ler credenciais de `.env.local` sem sourceá-lo: filtra a linha pelo nome da variável, corta no primeiro `=`, remove aspas)
  </read_first>
  <action>
**(a) `vitest.config.ts`** — acrescentar um bloco `resolve.alias` mapeando o prefixo `@` para o diretório `./src`, resolvido com `fileURLToPath(new URL('./src', import.meta.url))` para funcionar independentemente do diretório de execução. Comentário curto em pt-BR: o `tsconfig.json` já declara esse alias para o compilador, mas o vitest não lê `paths` do tsconfig; sem o alias aqui, qualquer suíte que toque `src/app/` falha no import antes de rodar um caso sequer. Não mexer no bloco `env` existente.

**(b) Criar `src/app/actions/__tests__/public-booking-escrita.test.ts`.** Cabeçalho em pt-BR explicando por que a suíte existe e por que ela é de INTEGRAÇÃO e não de unidade: o gap da `01-VERIFICATION.md` é justamente o caminho de escrita sob as policies e privilégios reais, e mock de banco provaria só que o mock funciona.

**Credenciais.** Função `lerCredenciaisSupabase()` que lê `.env.local` com `node:fs` e extrai **apenas** `NEXT_PUBLIC_SUPABASE_URL` e `SUPABASE_SECRET_KEY`, no mesmo estilo do harness de superfície: casa a linha pelo nome, corta no primeiro sinal de igual, tira aspas. Devolve `null` quando qualquer uma faltar. Atribuir as duas a `process.env` antes do primeiro uso — `createAdminClient()` as lê na chamada, então isso pode acontecer no `beforeAll`. **Nunca** imprimir, assertar sobre, nem incluir em mensagem de erro o valor de qualquer das duas.

**Pulo barulhento.** Calcular a ausência de credenciais no escopo do módulo e usar `describe.skipIf(...)` para a suíte de integração, com o nome do `describe` dizendo em voz alta que ela **exige credenciais do Supabase de dev**. Quando pular, emitir um `console.warn` em bloco, com moldura, explicando que o caminho de escrita do booking **não foi verificado nesta execução** e como habilitar. Um `describe` separado, que **nunca** é pulado, contém uma sentinela: quando `process.env.EXIGIR_INTEGRACAO === '1'` e não há credenciais, o caso **falha** com mensagem nomeando as duas variáveis. É isso que impede um pulo silencioso de virar falso verde.

**Mocks — e só estes dois.** `vi.mock('@/lib/notificacoes-agendamento')` para que nenhuma chamada saia para a Evolution API ou o QStash, e `vi.mock('@/lib/analytics/server')` porque `capturarEventoTenant` chama `after()` de `next/server`, que não existe fora de um contexto de request. **Não mockar** o cliente Supabase, a engine de disponibilidade, nem `assinaturas`/`planos` — é exatamente o que precisa ser real. Guardar as referências mockadas para assertar chamada (prova de fiação sem rede).

**Fixture determinística.** Constante `TENANT_TESTE` com valor `org_teste_integracao_booking` — determinístico de propósito, não aleatório: sufixo aleatório acumula lixo no banco quando uma execução morre no meio. `prepararTenantDeTeste()` roda a limpeza ANTES de criar, e então insere via `createAdminClient()`: um `perfis_empresas` com `slug` e `slug_gratuito` distintos e reconhecíveis como de teste, `timezone` `America/Sao_Paulo`, `antecedencia_minima_minutos` 0 e `horizonte_maximo_dias` 30; um `servicos` ativo com `duracao_minutos` 30; e sete linhas de `horarios_funcionamento`, uma por `dia_semana` de 0 a 6, das 08:00 às 18:00, ativas — a cobertura da semana inteira é o que torna o teste independente do dia em que roda. **Sem linha em `assinaturas`**: o plano vigente é `gratuito` e portanto o slug efetivo é o `slug_gratuito`; é por ele que todas as chamadas passam.

`limparTenantDeTeste()` apaga na ordem `agendamentos` → `clientes` → `perfis_empresas`, todos filtrados por `tenant_id`. A ordem é obrigatória: apagar o perfil primeiro dispara cascata simultânea em `servicos` e `agendamentos`, e a FK `agendamentos.servico_id` é `ON DELETE RESTRICT` — a remoção pode falhar dependendo da ordem interna do Postgres. Apagando os agendamentos antes, a cascata do perfil resolve `servicos` e `horarios_funcionamento` sozinha. Chamar no `beforeAll` (antes de criar) e no `afterAll`.

**Escolha do horário — sem literal cravado.** Calcular a data alvo como hoje mais dois dias no fuso do tenant usando `diaLocal` de `@/lib/timezone`, chamar `obterSlotsPublicos(slugGratuito, dataAlvo, 30)` e usar o `datetime` do primeiro slot devolvido. Isso não é conveniência: `criarAgendamentoPublico` valida o horário por **igualdade exata** contra a saída da engine, então consumir a própria saída é o que exercita esse contrato em vez de contorná-lo. Se a lista vier vazia, o teste falha com mensagem explicando que a fixture de horários não cobriu o dia.

**O caso da fatia (um só nesta task).** Cliente novo: chamar `criarAgendamentoPublico` com o slug gratuito, o id do serviço da fixture, o `datetime` obtido, um nome e um telefone **formatado** com parênteses, espaço e hífen. Assertar: o retorno traz `id`, `data_hora` e `status` igual a `confirmado`; uma consulta ao banco confirma a linha em `agendamentos` com o `tenant_id` da fixture; existe exatamente uma linha em `clientes` para aquele tenant, e o campo de telefone contém **apenas dígitos** (a sanitização por remoção de não-dígitos); o mock de mensageria foi chamado uma vez, com o `agendamentoId` retornado e o `tenant_id` da fixture — prova de que a fiação de notificação continua ligada sem que nada saia pela rede.
  </action>
  <verify>
    <automated>pnpm vitest run src/app/actions/__tests__/public-booking-escrita.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm vitest run src/app/actions/__tests__/public-booking-escrita.test.ts` passa com **0 testes pulados** na máquina do owner (que tem `.env.local`), com a saída real colada no SUMMARY
    - `grep -c "resolve" vitest.config.ts` retorna ≥ 1 e o arquivo contém `alias`; sem isso o import de `public-booking.ts` falha e o resto não roda
    - Rodar o comando **duas vezes seguidas** produz o mesmo resultado verde (idempotência da fixture) — as duas saídas coladas no SUMMARY
    - Depois da suíte, `select count(*) from agendamentos where tenant_id = 'org_teste_integracao_booking'` e a mesma contagem em `clientes` e `perfis_empresas` retornam **0** — verificado por consulta explícita e registrado
    - `grep -c "vi.mock" src/app/actions/__tests__/public-booking-escrita.test.ts` retorna exatamente **2** (mensageria e analytics — nada além disso é mockado)
    - `grep -v "^\s*[/*]" src/app/actions/public-booking.ts | grep -c "select('\*')"` retorna 0 (as projeções explícitas continuam intactas — o teste não afrouxou nada)
    - `git diff --stat pnpm-lock.yaml` vem vazio (nenhuma dependência nova)
  </acceptance_criteria>
  <done>Um agendamento público real, criado pela Server Action contra o banco de dev, cai em `agendamentos` e volta pelo `RETURNING` — provado por comando.</done>
</task>

<task type="auto">
  <name>Task 2: Expandir — reaproveitamento por telefone, rejeição de double-booking e o acoplamento de string nas duas pontas</name>
  <files>src/app/actions/__tests__/public-booking-escrita.test.ts</files>
  <read_first>
    - `src/app/actions/public-booking.ts` linhas 190-237 (o lookup de cliente por `tenant_id` + `telefone` com `maybeSingle()` e o ramo de criação) e linhas 170-181 (a validação de slot e a mensagem lançada)
    - `src/app/book/[slug]/BookingApp.tsx` linhas ~270-285 (o `catch` que decide a recuperação por `mensagem.includes(...)`, solta o slot, incrementa a tentativa de slots e volta para a etapa de data/hora)
    - `.planning/phases/01-hardening-da-superf-cie-p-blica/01-UI-SPEC.md` §"🚨 Acoplamento de string que a refatoração pode quebrar em silêncio" (o contrato: nenhuma mensagem de erro das duas actions públicas pode ser reescrita nesta fase)
  </read_first>
  <action>
Acrescentar três casos à suíte, todos dentro do mesmo `describe` de integração e reaproveitando a fixture da Task 1.

**(a) Reaproveitamento de cliente por telefone.** Agendar um segundo horário — outro `datetime` da mesma grade — com o **mesmo telefone** do primeiro caso, mas com nome diferente. Assertar: continua existindo **exatamente uma** linha em `clientes` para aquele tenant e aquele telefone; o `cliente_id` do segundo agendamento é **igual** ao do primeiro (consultado no banco). É o comportamento que `public-booking.ts` implementa com `maybeSingle()` sobre `tenant_id` + `telefone`, e que nada exercitava.

**(b) Rejeição de double-booking.** Chamar `criarAgendamentoPublico` de novo com o `datetime` **já ocupado** pelo primeiro caso. Assertar que a promessa é rejeitada e que a mensagem do erro **contém** a substring que a recuperação da UI usa. Assertar também que nada foi gravado: a contagem de `agendamentos` do tenant permanece a mesma de antes da chamada.

**(c) O acoplamento nas DUAS pontas.** O caso (b) prova o lado produtor. O consumidor mora no navegador e não é importável aqui — então provar por **asserção de fonte**: ler `src/app/book/[slug]/BookingApp.tsx` do disco com `node:fs` e assertar que o arquivo contém a chamada de casamento de substring com exatamente o mesmo trecho que a action produz, derivando o trecho de uma constante única declarada no topo da suíte e usada nas duas asserções. Se alguém reescrever qualquer uma das duas pontas, o teste fica vermelho — que é literalmente o pedido da `01-VERIFICATION.md`, que encontrou o acoplamento correto no código e nunca exercitado.

Comentário em pt-BR acima do bloco explicando a intenção de negócio: sem o casamento, o cliente final fica preso numa caixa vermelha estática embaixo do formulário de contato, olhando para um horário que não existe mais, em vez de voltar para a grade refeita.
  </action>
  <verify>
    <automated>pnpm vitest run src/app/actions/__tests__/public-booking-escrita.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - A suíte passa com **4 casos** de integração (o da Task 1 mais os três desta), saída real colada no SUMMARY
    - Existe uma constante única no topo do arquivo com o trecho da mensagem, referenciada tanto na asserção sobre o erro da action quanto na asserção sobre o conteúdo de `BookingApp.tsx` — assertável por `grep -c` do nome da constante retornando ≥ 3
    - `grep -c "já foi preenchido" src/app/book/\[slug\]/BookingApp.tsx` retorna 1 e `grep -c "já foi preenchido" src/app/actions/public-booking.ts` retorna 1 (as duas pontas seguem casando; nenhuma foi reescrita)
    - No caso (b), a contagem de `agendamentos` do tenant de teste antes e depois da chamada rejeitada é a mesma — assertado no teste, não conferido a olho
    - A limpeza continua zerando as três tabelas depois da suíte
  </acceptance_criteria>
  <done>Reaproveitamento por telefone, rejeição de horário ocupado e o acoplamento de string produtor↔consumidor estão cobertos por teste executável.</done>
</task>

<task type="auto">
  <name>Task 3: Cópia da caixa de erro de slots, comando dedicado que reprova, e Definition of Done</name>
  <files>src/app/actions/__tests__/public-booking-escrita.test.ts, package.json</files>
  <read_first>
    - `src/app/actions/public-booking.ts` §`obterSlotsPublicos` (o ramo em que o slug não resolve lança com a cópia que é renderizada VERBATIM ao cliente final)
    - `.planning/phases/01-hardening-da-superf-cie-p-blica/01-UI-SPEC.md` §"Regra sobre erros novos" (a cópia exata é contrato, e vazar texto técnico ali é proibido porque a caixa é visível ao cliente final)
    - `package.json` §`scripts` (só cinco scripts hoje; `test` é `vitest run`)
    - `docs/PENDENCIAS.md` §"🧪 UAT humano pendente da Phase 1", item da caixa de erro de slots (a própria pendência sugere este teste como "teste barato")
  </read_first>
  <action>
**(a) Caso da cópia.** Acrescentar um caso que chama `obterSlotsPublicos` com um slug inexistente e assertar que a promessa é rejeitada com a mensagem **exata** `Não foi possível carregar os horários. Tente de novo.` — igualdade estrita, não `contains`, porque a string inteira é renderizada verbatim na caixa vermelha da etapa de data/hora. Assertar também que a mensagem **não** contém o slug consultado nem os fragmentos `tenant`, `org_` ou `PGRST` — é a regra do CLAUDE.md de nunca vazar erro cru do Supabase para a UI, aqui literalmente visível ao cliente final.

Este caso **não** precisa de fixture de tenant, mas precisa das credenciais (a action instancia o cliente privilegiado antes de resolver o slug) — deixá-lo dentro do mesmo `describe` de integração.

**Registrar no comentário do caso, e depois no SUMMARY, o que ele NÃO cobre:** que a cópia chega à tela dentro da caixa vermelha com `role="alert"` e o botão de nova tentativa continua sendo item de olho humano. O teste prova que a action produz a string certa, não que a UI a renderiza.

**(b) `package.json`** — acrescentar o script `test:integracao`, que define `EXIGIR_INTEGRACAO=1` e roda o vitest **apenas** neste arquivo de suíte. Sem credenciais, a sentinela da Task 1 faz o comando sair diferente de 0 em vez de pular — é o comando a rodar quando a pergunta for "o caminho de escrita está provado?", e é o que impede que o pulo pareça sucesso.

**(c)** Fechar a Definition of Done do projeto: rodar `pnpm lint`, `pnpm test` e `pnpm build`, colando a saída real de cada um no SUMMARY. Registrar em `pnpm test` a contagem de arquivos e de testes, comparando com a linha de base da fase (13 arquivos, 196 testes) para que o crescimento fique explícito.
  </action>
  <verify>
    <automated>pnpm test:integracao && pnpm lint && pnpm test && pnpm build</automated>
    <human-check>O wizard no navegador (serviço → data/hora → nome + WhatsApp → "Horário confirmado!"), a recuperação visual do double-booking com aviso âmbar e a caixa vermelha de erro de slots continuam sendo verificação de olho humano — esta suíte cobre o lado servidor, não a tela. Os itens permanecem abertos em `docs/PENDENCIAS.md` §"UAT humano pendente da Phase 1".</human-check>
  </verify>
  <acceptance_criteria>
    - `pnpm test:integracao` sai **0** com as credenciais presentes, e sai **≠ 0** quando executado com `.env.local` inacessível — as duas execuções registradas no SUMMARY (a segunda pode ser simulada apontando o teste para um caminho de env inexistente, sem mover o arquivo real)
    - `grep -c "test:integracao" package.json` retorna 1, e `git diff package.json` mostra alteração **apenas** dentro de `scripts` (nenhuma dependência nova)
    - A suíte passa com **5 casos** de integração no total, saída real colada
    - A asserção da cópia usa igualdade estrita com `Não foi possível carregar os horários. Tente de novo.`
    - `pnpm lint` sai 0, `pnpm test` sai 0, `pnpm build` sai 0 — os três com saída real, e o SUMMARY registra a contagem nova de testes contra a linha de base de 196
  </acceptance_criteria>
  <done>Existe um comando que responde "sim ou não" à pergunta que a Phase 1 não conseguiu responder: o caminho de escrita do booking funciona?</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| suíte de teste → `.env.local` | o teste precisa da chave de service role para instanciar o cliente privilegiado |
| suíte de teste → Supabase de dev | escrita real em banco compartilhado com o tenant de trabalho do owner |
| `public-booking.ts` → `BookingApp.tsx` | contrato por substring, sem tipo que o sustente |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-23 | Information Disclosure | suíte lendo `SUPABASE_SECRET_KEY` de `.env.local` | high | mitigate | Leitura restrita às duas variáveis necessárias, por casamento de linha; valor nunca é impresso, assertado, serializado em snapshot nem incluído em mensagem de erro (proibição explícita no `must_haves`). O arquivo já é legível pelo agente por autorização do owner registrada em `CLAUDE.md` §Segredos (fase DEV) |
| T-01-24 | Tampering | teste escrevendo no banco de dev compartilhado | medium | mitigate | Tenant determinístico `org_teste_integracao_booking`, limpeza executada ANTES de criar e DEPOIS da suíte, filtros sempre por `tenant_id` do teste; critério de aceite exige contagem 0 nas três tabelas após a execução. Nenhum tenant real é lido ou escrito |
| T-01-25 | Repudiation | suíte pulada em silêncio vira falso verde e o gap volta | medium | mitigate | Banner emoldurado em stderr no pulo, nome do `describe` declarando a exigência, e o comando `pnpm test:integracao` com sentinela que REPROVA em vez de pular |
| T-01-26 | Denial of Service | mensageria real disparada por teste (WhatsApp para número inventado, item na fila do QStash) | medium | mitigate | `vi.mock` de `@/lib/notificacoes-agendamento` — nenhuma chamada sai para Evolution API ou QStash; a fiação é provada por asserção sobre o mock |
| T-01-SC | Tampering | supply chain (npm/pnpm) | high | mitigate | Nenhuma dependência nova; critério de aceite exige `git diff --stat pnpm-lock.yaml` vazio e alteração de `package.json` restrita a `scripts` |
</threat_model>

<verification>
- `pnpm test:integracao` → exit 0 com 5 casos, e exit ≠ 0 sem credenciais
- Duas execuções seguidas verdes (idempotência) e contagem 0 nas três tabelas depois
- `pnpm lint` / `pnpm test` / `pnpm build` → exit 0 com saída real
- Asserção de fonte casando `public-booking.ts` e `BookingApp.tsx`
</verification>

<success_criteria>
- SC2b da `01-VERIFICATION.md` deixa de ser `PRESENT_BEHAVIOR_UNVERIFIED` no lado servidor: o caminho de escrita é exercitado por comando
- O que permanece humano está dito com precisão, não escondido: a tela do wizard, o aviso âmbar e a caixa vermelha continuam na lista de UAT
- Nenhuma mensagem de erro das actions públicas foi reescrita
</success_criteria>

<output>
Create `.planning/phases/01-hardening-da-superf-cie-p-blica/01-07-SUMMARY.md` when done
</output>
