---
phase: 01-hardening-da-superf-cie-p-blica
plan: 09
type: execute
wave: 2
depends_on: ["01-06", "01-07", "01-08"]
gap_closure: true
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - docs/PENDENCIAS.md
  - docs/09-OBSERVABILIDADE_E_EMAIL.md
autonomous: true
requirements: [SEG-01, SEG-02, SEG-03, SEG-04, SEG-05]

must_haves:
  truths:
    - "As três provas da fase rodam de novo sobre o HEAD final e passam juntas: superfície anônima (exit 0, 0 inconclusivas), fail-fast de boot (exit 0, três vereditos) e escrita do booking (comando dedicado, sem pulo)"
    - "Nenhum documento é editado antes das provas passarem — a Task 1 é gate, e a fase já foi queimada uma vez por critério que lia como satisfeito enquanto a medição dizia o contrário"
    - "`REQUIREMENTS.md` deixa de descrever SEG-05 como parcialmente falso: a marca vira `[x]`, o texto descreve o comportamento real (o processo encerra) e a linha da tabela de rastreabilidade deixa de dizer `Partial` (per D-05)"
    - "O critério 5 do ROADMAP é verificado como literalmente verdadeiro antes de a fase ser dada por fechada, e a nota de execução da Phase 1 nomeia o comando que o prova"
    - "`docs/PENDENCIAS.md` fica coerente com o código em três pontos: a superfície remanescente das duas policies está fechada (não mais registrada como aberta), o item WR-02 sobre o boot descreve o encerramento real, e a lista de UAT humano diz item a item o que a automação passou a cobrir e o que continua exigindo olho"
    - "Os sete itens de UAT humano NÃO são marcados como feitos: nenhum executor pode fechá-los, e dois deles ficam explicitamente como parcialmente cobertos, com o que falta escrito"
    - "As cinco arestas não resolvidas do probe de edge têm disposição final registrada — nenhuma sumiu em silêncio"
  artifacts:
    - ".planning/REQUIREMENTS.md (SEG-05 e a linha de rastreabilidade corrigidas)"
    - ".planning/ROADMAP.md (nota de execução da Phase 1 com a evidência do critério 5)"
    - "docs/PENDENCIAS.md (três blocos atualizados)"
    - "docs/09-OBSERVABILIDADE_E_EMAIL.md (semântica de boot descrita como é)"
  key_links:
    - "scripts/verificar-fail-fast-boot.sh (01-06) → critério 5 do ROADMAP → SEG-05 em REQUIREMENTS.md (a evidência que autoriza a correção da redação)"
    - "src/app/actions/__tests__/public-booking-escrita.test.ts (01-07) → item do wizard em docs/PENDENCIAS.md (cobre o servidor, não a tela)"
    - "migration de policies residuais (01-08) → seção 'Superfície remanescente' de docs/PENDENCIAS.md (de aberta para fechada)"
  prohibitions:
    - statement: "Nenhum item da lista de UAT humano pode ser marcado como concluído por um executor — verificação visual não se infere de código HTTP nem de teste de integração; 'pendente' honesto é saída válida, passe fabricado não é"
      status: flagged-unverified
    - statement: "Nenhum documento pode ser editado antes de as três provas da Task 1 passarem; se qualquer uma reprovar, PARAR e devolver o estado ao owner"
      status: flagged-unverified
    - statement: "`mcp__supabase__apply_migration` está proibido neste projeto; este plano não toca banco nem migrations"
      status: flagged-unverified
    - statement: "`service_role` nunca aparece em nenhuma linha de `revoke`"
      status: flagged-unverified
    - statement: "Proibido `select('*')` no caminho público — o texto de fechamento não pode afirmar cobertura que afrouxe essa regra"
      status: flagged-unverified
    - statement: "Nenhuma dependência nova, nenhum framework de teste novo, nenhum comando de infraestrutura local (`supabase start`/`stop`/`db reset` sem `--linked`, `docker compose up`)"
      status: flagged-unverified
---

<objective>
Fechar o resíduo dos três gaps: **reparar a documentação que eles tornaram falsa** e **reexecutar as provas da própria fase** sobre o HEAD final, num único plano, para que nenhum arquivo de documentação seja tocado por dois planos da mesma wave.

Hoje `REQUIREMENTS.md` marca SEG-05 com `[~]` e um parágrafo longo explicando que o critério é literalmente falso, e a tabela de rastreabilidade diz `Partial`. Depois do plano 01-06 isso deixa de ser verdade — e deixar o código correto com a documentação descrevendo-o como quebrado é a mesma classe de erro, só invertida. Da mesma forma, `docs/PENDENCIAS.md` registra as duas policies residuais como abertas (o 01-08 as fecha), descreve o boot como um processo que sobrevive servindo 500 (o 01-06 muda isso) e lista sete itens de UAT dos quais dois passam a ter cobertura parcial automatizada (pelo 01-07).

**A ordem é inegociável e é o valor deste plano:** primeiro rodar as provas, só então escrever. A fase já foi queimada uma vez por critério que lia como satisfeito enquanto a medição dizia o contrário — o remédio é o gate, não a boa intenção.

Purpose: terminar a fase com código e documentos dizendo a mesma coisa, e com a lista do que continua não verificado escrita com precisão em vez de esvaziada por conveniência.
Output: três provas reexecutadas com saída real, quatro documentos corrigidos, sete itens de UAT ainda abertos e honestos.
</objective>

## Suposições explícitas — rollup das cinco arestas do probe de edge

A fase não tem `SPEC.md`, então `## Edge Coverage` e `## Prohibitions` não foram fornecidos e o protocolo de fallback vale. O probe determinístico devolveu **5 arestas aplicáveis, 0 resolvidas, 5 não resolvidas** (quatro `unclassified`, uma `concurrency` em SEG-01). O classificador é anglófono e os requisitos estão em pt-BR — **a classificação é sinal fraco, não verdade**. Este é o rollup final; nenhuma aresta é descartada em silêncio.

| Aresta | Classificação do probe | Onde é carregada | Disposição final |
|---|---|---|---|
| **A-SEG-01** | `concurrency` — *"se interrompido ou rodando em paralelo, o que é garantido?"* | 01-07 (teste sequencial), 01-08 (DDL + ledger sem transação comum) | **NÃO RESOLVIDA, sinalizada.** Duas requisições simultâneas no mesmo slot ainda podem gerar dois agendamentos ativos: a janela vai da leitura da engine ao INSERT. É AGE-03 na Phase 2, com constraint de exclusão no banco. Não fechar aqui e não escrever teste de concorrência que reprovaria por defeito planejado para outra fase |
| **A-SEG-02** | `unclassified` | 01-07, 01-08 | **PARCIALMENTE ENDEREÇADA.** Enumerabilidade com a chave publicável: fechada e verificada. Enumeração de `tenant_id` por conta autenticada: fechada pelo 01-08 nas duas tabelas residuais; **continua não resolvida** para as demais tabelas, onde `authenticated` mantém privilégio por desenho e a defesa é o RLS tenant-scoped, auditado mas não exaustivamente provado por probe |
| **A-SEG-03** | `unclassified` | 01-09 (este plano) | **NÃO RESOLVIDA, sinalizada.** O harness prova 401/42501 nas colunas sensíveis, e a perda total da Data API por `anon` satisfaz o requisito com folga (zero colunas ⊂ colunas necessárias). O que permanece suposição é a durabilidade: nada impede uma fase futura de conceder `GRANT` de coluna e reabrir. A trava é a regra escrita em `docs/03` mais o harness rodando em toda fase |
| **A-SEG-04** | `unclassified` | 01-08 | **NÃO RESOLVIDA, sinalizada.** "Tabela nova nasce fora da Data API" foi provado uma vez, empiricamente, com tabela descartável no plano 01-04. O mecanismo é o `ALTER DEFAULT PRIVILEGES for role postgres`; a migration do 01-08 é proibida de conter privilégio, com grep negativo como trava. A prova volta a valer a pena na Phase 7, quando nascer a primeira tabela nova de verdade |
| **A-SEG-05** | `unclassified` | 01-06 | **RESOLVIDA no que importa, sinalizada no resto.** A aresta real — "o processo morre mesmo?" — passa a ser medida por `scripts/verificar-fail-fast-boot.sh`. Continua não resolvida para: encerramento sob orquestrador (Railway em crash-loop) e comportamento com várias variáveis ausentes ao mesmo tempo (a mensagem lista todas, mas o teste isola uma) |

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-hardening-da-superf-cie-p-blica/01-VERIFICATION.md
@.planning/phases/01-hardening-da-superf-cie-p-blica/01-05-SUMMARY.md
@.planning/phases/01-hardening-da-superf-cie-p-blica/01-CONTEXT.md
@CLAUDE.md
</context>

## Artifacts this phase produces (plan 01-09)

Nenhum símbolo de código novo. Este plano produz **estado documental verificado**:

- `.planning/REQUIREMENTS.md` — linha de SEG-05 reescrita (marca `[~]` → `[x]`) e a linha `SEG-05 | Phase 1 | Partial — …` da tabela de rastreabilidade passando a `Complete`
- `.planning/ROADMAP.md` — uma linha nova em "Notas de execução" da Phase 1 nomeando `scripts/verificar-fail-fast-boot.sh` como a prova do critério 5, e a lista de planos da fase refletindo 01-06 a 01-09
- `docs/PENDENCIAS.md` — três blocos: seção "Superfície remanescente depois do hardening da Phase 1" marcada como fechada com o nome da migration; item WR-02 sobre o boot corrigido; checklist "🧪 UAT humano pendente da Phase 1" anotado item a item com a cobertura automatizada nova
- `docs/09-OBSERVABILIDADE_E_EMAIL.md` — a descrição da semântica de fail-fast de env passando a dizer que o processo encerra, nomeando o símbolo e o harness
- Nenhum arquivo em `src/`, `supabase/` ou `scripts/`; nenhuma dependência nova

<tasks>

<task type="auto">
  <name>Task 1: Gate — reexecutar as três provas da fase sobre o HEAD final</name>
  <files>—</files>
  <read_first>
    - `scripts/verificar-superficie-anon.sh` (os três vereditos e por que **zero INCONCLUSIVAS** é a métrica que importa, não zero reprovadas)
    - `scripts/verificar-fail-fast-boot.sh` (criado no plano 01-06 — os três vereditos e a variável de escape)
    - `.planning/phases/01-hardening-da-superf-cie-p-blica/01-VERIFICATION.md` §"Behavioral Spot-Checks" e §"Probe Execution" (a linha de base contra a qual comparar: quais checagens passavam, qual falhava e com que números)
    - `package.json` §`scripts` (o comando `test:integracao` criado no plano 01-07)
  </read_first>
  <action>
Executar as cinco provas abaixo, em sequência, colando a **saída real** de cada uma no SUMMARY. Nenhum arquivo de documentação pode ser editado antes de todas passarem.

  1. `bash scripts/verificar-superficie-anon.sh` — exigir exit 0, com o resumo mostrando 0 reprovadas e **nenhuma** seção de inconclusivas. Comparar com a linha de base registrada na `01-VERIFICATION.md` (11 checagens) e apontar qualquer diferença de contagem, mesmo que verde.
  2. `bash scripts/verificar-fail-fast-boot.sh` — exigir exit 0 com os três vereditos aprovados. É a prova que autoriza a correção da redação de SEG-05 e do critério 5; sem ela a Task 2 não roda.
  3. `pnpm test:integracao` — exigir exit 0 **sem nenhum caso pulado**. Um pulo aqui significa credenciais ausentes, e nesse estado o caminho de escrita continua não provado: PARAR e escalar, não seguir.
  4. Reexecutar as três sondas HTTP do webhook contra o build de produção, no estilo do plano 01-05: POST sem cabeçalho de assinatura, POST com o parâmetro de secret legado na query string e POST com assinatura forjada — as três devem devolver **401**, com o controle de `GET /` devolvendo **200**. É a metade de SEG-05 que já estava fechada; reexecutar aqui garante que o `process.exit` novo não introduziu regressão no caminho autenticado.
  5. `pnpm lint`, `pnpm test` e `pnpm build` — os três com saída real, Definition of Done do projeto. Registrar a contagem de arquivos e testes e compará-la com a linha de base da fase (13 arquivos, 196 testes), explicando o crescimento.

**Regra de parada:** qualquer reprovação encerra o plano aqui, com o estado devolvido ao owner. Documentar uma fase como fechada sobre prova que não rodou é exatamente o defeito que a `01-VERIFICATION.md` apontou, e repeti-lo em sentido inverso não é melhor.
  </action>
  <verify>
    <automated>bash scripts/verificar-superficie-anon.sh && bash scripts/verificar-fail-fast-boot.sh && pnpm test:integracao && pnpm lint && pnpm test && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - As cinco provas estão coladas no SUMMARY com a saída real e o código de saída de cada uma
    - `scripts/verificar-superficie-anon.sh` → exit 0, resumo com **0 reprovada(s)** e sem seção de INCONCLUSIVAS
    - `scripts/verificar-fail-fast-boot.sh` → exit 0 e os três vereditos aprovados nomeados na saída
    - `pnpm test:integracao` → exit 0 e a linha de resumo do vitest com **0 skipped**
    - As três sondas do webhook devolvem 401 e o controle devolve 200 — os quatro códigos colados
    - `pnpm lint` / `pnpm test` / `pnpm build` → exit 0, com a contagem de testes comparada explicitamente aos 196 da linha de base
  </acceptance_criteria>
  <done>As três frentes de gap estão provadas por comando sobre o mesmo HEAD, e a fase pode falar sobre si mesma sem inventar nada.</done>
</task>

<task type="auto">
  <name>Task 2: Reparar a documentação de requisitos e roadmap que os gaps tornaram falsa</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, docs/09-OBSERVABILIDADE_E_EMAIL.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` linha 17 (a linha de SEG-05 com a marca `[~]` e o parágrafo que explica o critério falso) e linha 151 (a linha `SEG-05` da tabela §Traceability, hoje `Partial`)
    - `.planning/ROADMAP.md` §"Phase 1: Hardening da superfície pública" (o critério 5 e o bloco "Notas de execução")
    - `docs/09-OBSERVABILIDADE_E_EMAIL.md` (a seção que descreve o fail-fast de variável obrigatória — é a referência que o `CLAUDE.md` aponta e que o próximo deploy vai ler)
    - O SUMMARY do plano 01-06 (o comportamento medido depois do conserto, com os três vereditos)
  </read_first>
  <action>
**(a) `.planning/REQUIREMENTS.md`.** Trocar a marca de SEG-05 de `[~]` para `[x]` e reescrever o texto da linha para descrever o comportamento **real**: o webhook só aceita requisição com assinatura válida do QStash, e sem as chaves configuradas o processo de produção encerra com código diferente de zero em vez de ficar de pé servindo erro. O parágrafo longo que explicava o critério falso sai — mas **não** sem deixar rastro: acrescentar uma frase curta registrando que o comportamento foi medido como falso na primeira verificação da Phase 1 e fechado pelo plano 01-06, com o nome do harness que prova. Um requisito que muda de estado sem registrar por quê é como o histórico se perde.

Na tabela §Traceability, trocar o valor da linha de SEG-05 de `Partial — …` para `Complete`, mantendo o formato das outras 55 linhas. Conferir que os outros quatro SEG continuam `Complete` e que nenhuma outra linha foi alterada por engano.

**(b) `.planning/ROADMAP.md`, Phase 1.** Antes de escrever qualquer coisa, conferir que o critério 5 é agora **literalmente** verdadeiro à luz da prova 2 da Task 1. O texto do critério em si não muda — ele descreve o comportamento correto e passa a ser verdade. O que muda é o bloco "Notas de execução": acrescentar uma linha nomeando `scripts/verificar-fail-fast-boot.sh` como o comando que prova o critério 5, e uma linha registrando que a primeira medição da fase encontrou o processo sobrevivendo e que a semântica de boot foi alterada por decisão do owner. Atualizar também a contagem e a lista de planos da fase para incluir 01-06 a 01-09, com as waves. **Editar por substituição de trecho, nunca reescrever o arquivo inteiro:** o `ROADMAP.md` tem doze fases e uma escrita de arquivo inteiro apaga tudo que estiver fora da janela do diff.

**(c) `docs/09-OBSERVABILIDADE_E_EMAIL.md`.** Corrigir a descrição da semântica de fail-fast: em produção, variável obrigatória ausente não apenas "derruba o boot" no sentido vago — o processo **encerra** com código diferente de zero, e a mensagem em stderr nomeia todas as ausentes de uma vez. Nomear o símbolo responsável e o harness que prova, e registrar as duas guardas que mantêm o resto funcionando: só em produção, e só no runtime nodejs. Manter intacta a instrução existente de acrescentar à lista toda variável nova que falharia em silêncio — ela continua valendo e agora tem consequência maior.
  </action>
  <verify>
    <automated>grep -n "SEG-05" .planning/REQUIREMENTS.md && grep -c "Partial" .planning/REQUIREMENTS.md && grep -n "verificar-fail-fast-boot" .planning/ROADMAP.md docs/09-OBSERVABILIDADE_E_EMAIL.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^- \[~\] \*\*SEG-05" .planning/REQUIREMENTS.md` retorna **0** e `grep -c "^- \[x\] \*\*SEG-05" .planning/REQUIREMENTS.md` retorna **1**
    - `grep -c "Partial" .planning/REQUIREMENTS.md` retorna **0** (nenhuma linha de rastreabilidade sobrou parcial)
    - `grep -c "| Complete |" .planning/REQUIREMENTS.md` retorna **5** (os cinco SEG da Phase 1; as demais linhas continuam `Pending`)
    - `grep -c "verificar-fail-fast-boot" .planning/ROADMAP.md` retorna ≥ 1 e o mesmo para `docs/09-OBSERVABILIDADE_E_EMAIL.md`
    - A seção da Phase 1 no `ROADMAP.md` lista os planos 01-06, 01-07, 01-08 e 01-09 com as respectivas waves, e a contagem de planos foi atualizada
    - `git diff --stat .planning/ROADMAP.md` mostra alteração restrita à seção da Phase 1 (nenhuma outra fase tocada) — conferido lendo o diff, não presumido
    - O SUMMARY registra explicitamente que a correção só foi escrita depois de a prova 2 da Task 1 sair exit 0, citando o número
  </acceptance_criteria>
  <done>Requisito, roadmap e documentação de observabilidade descrevem o comportamento que a máquina mede — nem otimista, nem desatualizado.</done>
</task>

<task type="auto">
  <name>Task 3: Fechar em docs/PENDENCIAS.md o que fechou, e escrever com precisão o que continua aberto</name>
  <files>docs/PENDENCIAS.md</files>
  <read_first>
    - `docs/PENDENCIAS.md` §"Superfície remanescente depois do hardening da Phase 1" (a seção inteira: os dois riscos, a decisão de registrar em vez de fechar e o procedimento de fechamento em quatro passos)
    - `docs/PENDENCIAS.md` §WR-02 (o item sobre o boot, já corrigido uma vez pelo plano 01-05 para dizer que o processo sobrevive)
    - `docs/PENDENCIAS.md` §"🧪 UAT humano pendente da Phase 1" (os sete itens com o motivo de cada um degradar em silêncio)
    - Os SUMMARYs dos planos 01-06, 01-07 e 01-08 (o que cada um provou, com os números)
  </read_first>
  <action>
**(a) Superfície remanescente.** A seção descreve como aberto um estado que o plano 01-08 fechou. Reescrevê-la como fechada: manter a análise dos dois riscos (é o registro de por que a mudança foi feita e evita que alguém recrie a policy achando que ajuda), e trocar o bloco de decisão e o procedimento de quatro passos por um registro de fechamento nomeando a migration aplicada, a data e a evidência de `pg_policies`. Preservar a nota de que a D-07 não se aplicava e de que o conserto foi `DROP` puro — é a explicação que impede a discussão de ser reaberta na próxima sessão.

**(b) WR-02.** O item afirma que o processo sobrevive servindo 500 e apresenta duas saídas em aberto para o owner. Uma delas foi escolhida e executada. Reescrever para o estado atual: o processo encerra com código diferente de zero, o comando que prova, e as duas guardas. Manter — e reforçar — o alerta de janela de crash-loop já registrado: com o encerramento real, subir um deploy antes de provisionar todas as obrigatórias no Railway derruba o boot de propósito, que é o comportamento pedido, mas o intervalo entre merge e configuração continua sendo risco operacional com nome e endereço.

**(c) Checklist de UAT humano — o ponto mais delicado deste plano.** Os sete itens **continuam abertos**. Nenhum é marcado. O que muda é a anotação de cobertura, item a item, sem nenhum arredondamento para cima:

  - **Wizard completo** — *parcialmente coberto*. A suíte do plano 01-07 exercita o caminho de escrita ponta a ponta no servidor: resolução por slug, criação de cliente, sanitização de telefone, INSERT com `RETURNING`. **Não** cobre: as telas do navegador, a ausência de fricção nova, a transição para a tela de sucesso e a linha aparecendo na agenda do dashboard. Continua exigindo olho humano.
  - **Recuperação de double-booking** — *parcialmente coberto*. O teste prova que a action rejeita o horário ocupado com a mensagem certa e que `BookingApp.tsx` casa exatamente essa substring, nas duas pontas. **Não** cobre: o aviso âmbar renderizado, a grade refeita e o cliente voltando à etapa de data/hora. Continua exigindo olho humano.
  - **Caixa de erro de slots** — *parcialmente coberto*. O teste prova que a action produz a cópia exata e não vaza texto técnico. **Não** cobre: a cópia aparecendo na caixa vermelha com o papel de alerta e o botão de nova tentativa. Continua exigindo olho humano.
  - **Dashboard tela a tela** — *reforçado, não coberto*. O plano 01-08 provou por SQL que, sob a role autenticada, o próprio tenant enxerga inclusive as linhas inativas depois do DROP — o que torna o modo de falha "tela vazia sem erro" ainda mais improvável. Improvável não é verificado; o item continua aberto.
  - **Personalização por plano**, **lembrete do QStash ponta a ponta** e **backstops visuais** — *sem mudança*. Nenhuma cobertura nova. Continuam abertos exatamente como estão.

Acrescentar, no topo da seção, uma frase dizendo que a cobertura automatizada cresceu e que isso **reduz** o risco de cada item sem fechá-lo — para que ninguém leia "parcialmente coberto" como "pode pular".

**(d)** Conferir que nenhuma caixa de seleção da seção mudou de estado: `docs/PENDENCIAS.md` é o lugar onde o owner vai procurar o que falta, e um item marcado por engano é pior que um item esquecido.
  </action>
  <verify>
    <automated>grep -c "^- \[ \]" docs/PENDENCIAS.md && grep -c "fecha_policies_residuais" docs/PENDENCIAS.md && pnpm lint</automated>
    <human-check>Os sete itens de UAT continuam pendentes e só o owner pode executá-los: wizard completo até "Horário confirmado!" com a linha caindo na agenda, recuperação de double-booking em duas abas, dashboard tela a tela, personalização Pro × gratuito, lembrete do QStash ponta a ponta, caixa de erro de slots renderizada e backstops visuais com dado extremo. Este plano os documenta com precisão; não os fecha nem pode fechá-los.</human-check>
  </verify>
  <acceptance_criteria>
    - `grep -c "fecha_policies_residuais" docs/PENDENCIAS.md` retorna ≥ 1 (a migration aplicada está nomeada no registro de fechamento)
    - Na seção "🧪 UAT humano pendente da Phase 1", a contagem de caixas não marcadas continua **7** e a de caixas marcadas continua **0** — assertado por contagem no bloco da seção, não a olho
    - Os três itens parcialmente cobertos trazem, cada um, uma frase começando pelo que a automação **não** cobre — a ausência dessa frase em qualquer um dos três reprova a task
    - `grep -c "verificar-fail-fast-boot" docs/PENDENCIAS.md` retorna ≥ 1 (o item WR-02 nomeia o comando que prova o comportamento novo)
    - `git status --porcelain` lista exatamente os quatro caminhos de `files_modified` deste plano e nada mais
    - `pnpm lint` sai 0 (nenhum arquivo de código foi tocado; a checagem é contra alteração acidental)
  </acceptance_criteria>
  <done>O documento que o owner consulta antes de cada etapa está coerente com o código, e a lista do que falta ficou mais precisa em vez de mais curta.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentação → decisão futura de deploy | `docs/PENDENCIAS.md` e `docs/09` são o que o owner lê antes de subir produção |
| documentação → verificação da fase | `REQUIREMENTS.md` e `ROADMAP.md` são a fonte contra a qual o verificador julga |
| executor → lista de UAT humano | só o owner pode fechar aqueles itens |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-32 | Repudiation | documentação afirmando fechado o que não foi medido | high | mitigate | A Task 1 é gate absoluto: nenhum documento é editado antes das cinco provas passarem, com saída real colada; regra de parada explícita em caso de reprovação |
| T-01-33 | Repudiation | item de UAT humano marcado como feito por um executor | high | mitigate | Proibição explícita no `must_haves`; critério de aceite exige que a contagem de caixas marcadas continue **0** e a de não marcadas continue **7**; a cobertura nova é anotada como parcial com o que falta escrito primeiro |
| T-01-34 | Tampering | escrita de arquivo inteiro em `ROADMAP.md` apagando fases fora da janela do diff | high | mitigate | Instrução explícita de editar por substituição de trecho; critério de aceite exige leitura do diff confirmando alteração restrita à seção da Phase 1 |
| T-01-35 | Information Disclosure | correção de documentação apagando o histórico do erro | medium | mitigate | A reescrita de SEG-05 preserva uma frase registrando que o critério foi medido como falso e por qual plano foi fechado; a seção de superfície remanescente preserva a análise dos dois riscos |
| T-01-SC | Tampering | supply chain (npm/pnpm) | high | mitigate | Nenhum install; `git status` restrito a quatro arquivos de documentação |
</threat_model>

<verification>
- As cinco provas da Task 1, com saída real e código de saída
- `REQUIREMENTS.md` sem nenhuma ocorrência de `Partial` e com SEG-05 marcado `[x]`
- `ROADMAP.md` com a nota nomeando o harness e o diff restrito à Phase 1
- `docs/PENDENCIAS.md` com 7 caixas abertas e 0 marcadas na seção de UAT
- `pnpm lint` exit 0
</verification>

<success_criteria>
- Os três gaps da `01-VERIFICATION.md` estão fechados por código **e** refletidos na documentação, na ordem certa (prova antes de texto)
- Os cinco IDs de requisito da fase têm estado correto e verificável
- O que continua não verificado está escrito com mais precisão do que antes — sete itens de UAT humano abertos, três deles com cobertura parcial nomeada, e cinco arestas de probe com disposição final
</success_criteria>

<output>
Create `.planning/phases/01-hardening-da-superf-cie-p-blica/01-09-SUMMARY.md` when done
</output>
