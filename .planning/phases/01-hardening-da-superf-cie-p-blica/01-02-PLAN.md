---
phase: 01-hardening-da-superf-cie-p-blica
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/app/actions/public-booking.ts
  - src/app/book/[slug]/page.tsx
  - src/app/book/[slug]/BookingApp.tsx
  - src/lib/supabase/admin.ts
  - src/lib/assinaturas.ts
autonomous: true
requirements: [SEG-02, SEG-03]

must_haves:
  truths:
    - "Todas as leituras do caminho público (perfil, serviços, plano vigente, engine de disponibilidade) usam createAdminClient() com lista de colunas explícita e .eq('tenant_id', …) resolvido no servidor a partir do slug (per D-02, mitigações 1 e 2)"
    - "criarAgendamentoPublico e obterSlotsPublicos recebem slug — o org_id do Clerk não viaja mais do browser para as actions públicas (per D-04)"
    - "O payload RSC de /book/[slug] não contém nenhum org_ (perfil.tenant_id removido da projeção de page.tsx; per D-04)"
    - "tenantHash continua calculado no servidor a partir do org_id cru via hashTenantId(perfil.tenant_id) em page.tsx — o funil não muda de chave (UI-SPEC A2)"
    - "servicos deixa de viajar inteiro para o browser: só id, nome, descricao, preco, duracao_minutos (UI-SPEC A4)"
    - "Slug que não resolve em obterSlotsPublicos lança exatamente 'Não foi possível carregar os horários. Tente de novo.' — nunca cai silenciosamente em TIMEZONE_PADRAO/antecedência 15/horizonte 14 (UI-SPEC, regra sobre erros novos)"
    - "O JSDoc de src/lib/supabase/admin.ts enumera as leituras públicas como terceiro ponto autorizado (per D-02, mitigação 3)"
    - "As três actions autenticadas com .insert/.upsert(...).select(...) continuam enxergando a própria linha: agendamentos.ts:318-320, agendamentos.ts:285-286, perfis-empresas.ts:234-238 gravam tenant_id = orgId de auth() (pré-verificação da D-07 antes do plano 01-04 trocar as policies de SELECT)"
    - "UI#1: lista de serviços vazia renderiza a caixa tracejada com a copy travada (EtapaServico.tsx:41-42)"
    - "UI#2: lista de serviços vem resolvida do RSC — nenhum estado de loading novo no cliente"
    - "UI#3: falha de resolução do estabelecimento cai em notFound() com 404 real antes do streaming"
    - "UI#4: card de serviço exibe nome, descrição, preço e duração — as cinco colunas da projeção explícita presentes"
    - "UI#5: descricao nula no card continua tratada; nenhuma outra coluna do card falta"
    - "UI#7: zero serviços = estado vazio; um e muitos usam o mesmo card, sem copy singular/plural"
    - "UI#8: dia sem horário livre renderiza a caixa tracejada e a fileira de datas continua navegável"
    - "UI#9: loading de slots segue sendo 9 skeletons h-11 animate-pulse dentro de aria-live=polite"
    - "UI#10: erro de slots segue sendo caixa vermelha role=alert + botão 'Tentar de novo' que reexecuta a busca via setTentativaSlots"
    - "UI#11: grade populada usa a saída da engine anti-buraco em formato inalterado (contrato do anti double-booking)"
    - "UI#12: dia parcialmente ocupado continua sendo o caso normal da engine, não um estado degradado"
    - "UI#14: um slot e muitos usam o mesmo botão; zero é o estado vazio"
    - "UI#15: horário mantém formato fixo HH:MM e rótulo curto de data"
    - "UI#16: comanda de resumo contida por truncate/line-clamp existentes; nenhuma classe de layout alterada"
    - "UI#18: formulário de contato inicial com os dois campos vazios e CTA disabled com opacity-50"
    - "UI#19: pending via useActionState — CTA vira 'Confirmando…' e disabled, sem overlay"
    - "UI#20: corrida de slot volta para data/hora com aviso âmbar e grade refeita (substring 'já foi preenchido' de BookingApp.tsx:274 continua casando com public-booking.ts:106)"
    - "UI#21: só nome ou só telefone não avança — validação de 10–11 dígitos barra na UI e a action revalida"
    - "UI#23: tela de sucesso com conteúdo de tamanho fixo, contido pelo layout atual"
    - "UI#25: rótulos da barra inferior/CTA são as strings fixas travadas no Copywriting Contract — nenhuma reescrita"
    - "UI#26: tenant sem logo, capa ou descrição renderiza o painel de marca sem buraco de layout"
    - "UI#27: imagens do painel via next/image, sem mudança de carregamento"
    - "UI#28: imagem que falha não bloqueia a página nem o wizard"
    - "UI#29: personalização Pro continua sanitizada por plano em obterDadosBookingPublico — com admin client (bypass de RLS) a sanitização é a ÚNICA defesa; tenant gratuito nunca exibe cor/logo/capa (UI-SPEC C13)"
    - "UI#30: truncate/line-clamp-2/line-clamp-3/max-w-full do painel de marca inalterados"
    - "UI#32: página 404 de slug inexistente com conteúdo fixo de not-found.tsx"
    - "UI#33: copy do not-found literal como travada no Copywriting Contract (not-found.tsx:8,11)"
    - "UI#34: stepper não tem estado de loading próprio — reflete a etapa corrente"
    - "UI#35: stepper não tem caminho de erro próprio; erro de etapa é tratado na etapa"
    - "UI#36: quatro etapas fixas no stepper; nunca cresce"
    - "UI#37: rótulos de etapa são strings fixas travadas"
  artifacts:
    - "src/app/actions/public-booking.ts (contrato por slug + projeções explícitas)"
    - "src/app/book/[slug]/page.tsx (payload sem tenant_id)"
    - "src/app/book/[slug]/BookingApp.tsx (trabalha com slug)"
  key_links:
    - "BookingApp → obterSlotsPublicos/criarAgendamentoPublico por slug (se o slug não resolver no servidor, a mensagem é a copy travada — nunca erro cru)"
    - "obterDadosBookingPublico → personalizacao sanitizada por plano (única defesa pós-admin-client; regressão = visual + monetização, silenciosa)"
    - "public-booking.ts:106 mensagem de double-booking ↔ BookingApp.tsx:274 substring (acoplamento que a refatoração pode quebrar em silêncio)"
  prohibitions:
    - statement: "Fricção Zero: nenhuma etapa, campo, confirmação, CAPTCHA, login, OTP ou atraso novo visível ao cliente final no booking público"
      status: flagged-unverified
    - statement: "Nenhuma mensagem de erro existente das duas actions públicas é reescrita (a recuperação de double-booking casa por substring; UI-SPEC §Copywriting Contract)"
      status: flagged-unverified
---

<!-- planner-discipline-allow: tenant_id -->
<!-- planner-discipline-allow: org_ -->

<objective>
Migrar TODAS as leituras do caminho público para `createAdminClient()` com projeção explícita de colunas (D-02, mitigações 1-3) e trocar o contrato das duas actions públicas de `tenantId` para `slug` resolvido no servidor (D-04) — tirando o org_id do Clerk do payload do browser e tornando o `tenant_id` controlado pelo cliente uma impossibilidade estrutural, antes que o plano 01-04 feche a Data API por completo.

Edge coverage (fallback): SEG-02 e SEG-03 estavam `unclassified` no probe — resolvidos como covered usando os critérios de sucesso 1 e 3 do ROADMAP, que já são verificáveis por curl (a prova final roda nos planos 01-04/01-05; este plano entrega o pré-requisito de código).

Purpose: com a D-01 o booking só sobrevive se as leituras já estiverem no cliente privilegiado; e com service role no caminho, filtro por tenant resolvido no servidor + projeção explícita são critério de aceite, não recomendação (CONTEXT D-02).
Output: caminho público inteiro servido por admin client blindado; payload RSC sem org_.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-hardening-da-superf-cie-p-blica/01-CONTEXT.md
@.planning/phases/01-hardening-da-superf-cie-p-blica/01-PATTERNS.md
@.planning/phases/01-hardening-da-superf-cie-p-blica/01-UI-SPEC.md
@.planning/phases/01-hardening-da-superf-cie-p-blica/01-01-SUMMARY.md
</context>

## Artifacts this phase produces (plan 01-02)

- `COLUNAS_PERFIL_PUBLICO` — constante de módulo (não exportada) em `src/app/actions/public-booking.ts` com as 13 colunas do perfil público
- `COLUNAS_SERVICO_PUBLICO` — constante de módulo (não exportada) com as 5 colunas do serviço público
- `resolverPerfilPublicoPorSlug` — função auxiliar assíncrona NÃO exportada em `src/app/actions/public-booking.ts` (lookup slug → fallback slug_gratuito → validação obterSlugEfetivo)
- Prop `slug: string` no componente `BookingApp` (substitui `perfil.tenant_id` na interface de props)
- Assinaturas alteradas: `criarAgendamentoPublico({ slug, servicoId, dataHora, clienteNome, clienteTelefone, clienteEmail? })` e `obterSlotsPublicos(slug, dateStr, duracaoMinutos)`

<tasks>

<task type="auto">
  <name>Task 1: public-booking.ts — admin client, projeções explícitas e contrato por slug</name>
  <files>src/app/actions/public-booking.ts</files>
  <read_first>
    - src/app/actions/public-booking.ts (estado atual completo — as três funções exportadas e o padrão de erro das linhas 129-140)
    - .planning/phases/01-hardening-da-superf-cie-p-blica/01-RESEARCH.md §"Superfície mínima da página pública" (mapa exato de colunas lidas vs. filtradas) e §"Code Examples" → "Projeção explícita no caminho público" (as duas constantes prontas)
    - .planning/phases/01-hardening-da-superf-cie-p-blica/01-UI-SPEC.md §B (enumeração completa das colunas que a UI consome — coluna esquecida não gera erro, gera campo vazio) e §"Regra sobre erros novos"
    - src/lib/planos.ts (obterSlugEfetivo — validação de slug efetivo por plano)
  </read_first>
  <action>
    Refatorar as três funções exportadas, preservando TODO o comportamento observável (mensagens de erro literais, formato de retorno, eventos de analytics):

    1. Constantes de módulo (não exportadas — o arquivo é 'use server', só funções async podem ser exportadas): `COLUNAS_PERFIL_PUBLICO` = 'tenant_id, slug, slug_gratuito, nome_estabelecimento, descricao, instagram, endereco, timezone, antecedencia_minima_minutos, horizonte_maximo_dias, cor_marca, logo_url, capa_url' e `COLUNAS_SERVICO_PUBLICO` = 'id, nome, descricao, preco, duracao_minutos' (exatamente as listas do RESEARCH §Code Examples).

    2. Helper não exportado `resolverPerfilPublicoPorSlug(admin, slug)`: reaproveita a mecânica que hoje vive em obterDadosBookingPublico (linhas 233-258) — busca por `slug`, fallback por `slug_gratuito`, valida `obterSlugEfetivo(perfil, plano) === slug` com `obterPlanoVigentePublico(admin, perfil.tenant_id)`; devolve o perfil projetado por COLUNAS_PERFIL_PUBLICO + o plano, ou null. Todas as queries com o cliente admin.

    3. `obterDadosBookingPublico(slug)`: passa a usar `createAdminClient()` para todas as leituras via o helper; a query de `servicos` projeta COLUNAS_SERVICO_PUBLICO mantendo os filtros `.eq('tenant_id', perfil.tenant_id)` e `.eq('ativo', true)` (colunas de filtro não precisam estar na projeção). Sanitização de personalizacao por plano INALTERADA (UI#29/C13 — é a única defesa agora). Retorno com o mesmo shape de hoje (perfil com cor_marca/logo_url/capa_url neutralizados em null).

    4. `criarAgendamentoPublico`: o campo `tenantId` do parâmetro vira `slug`; o tenant é resolvido no servidor via helper — se null, lançar a mensagem já existente 'Estabelecimento inválido ou indisponível.' (nenhuma copy nova). Todas as leituras (perfil, servico, engine via obterSlotsDisponiveis) passam a usar o admin client; timezone/antecedencia/horizonte vêm do perfil resolvido (mesmos defaults ?? 15 / ?? 14 de hoje QUANDO o perfil resolve). Escritas já usam admin — inalteradas. Todos os `capturarEventoTenant` continuam recebendo o tenant_id resolvido no servidor.

    5. `obterSlotsPublicos(slug, dateStr, duracaoMinutos)`: resolve via helper com admin; se o slug NÃO resolver, lançar exatamente 'Não foi possível carregar os horários. Tente de novo.' (copy mandatória do UI-SPEC — este é o único texto novo autorizado na fase) em vez do fallback silencioso atual para TIMEZONE_PADRAO/15/14; se resolver, chama obterSlotsDisponiveis com o admin client e as regras do tenant.

    Manter o padrão de erro do arquivo (console.error pt-BR → reportarExcecao(erroSinteticoSupabase(...)) → throw amigável) em toda função tocada. Não tocar em src/lib/booking-engine.ts (o cliente é parâmetro).
  </action>
  <verify>
    <automated>pnpm lint && pnpm test && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "select('\*')" src/app/actions/public-booking.ts` retorna vazio (não escrever a chamada de projeção total nem em comentário)
    - `grep -c "createClient" src/app/actions/public-booking.ts` considerando apenas o cliente com sessão: o import de `@/lib/supabase/server` foi removido do arquivo (nenhuma leitura pública resta na role anon)
    - `grep -c "eq('tenant_id'" src/app/actions/public-booking.ts` >= 4 (todo query do caminho público filtra por tenant resolvido no servidor)
    - O arquivo contém `COLUNAS_PERFIL_PUBLICO`, `COLUNAS_SERVICO_PUBLICO` e `resolverPerfilPublicoPorSlug` sem `export` nessas declarações
    - As strings 'Este horário já foi preenchido ou está indisponível. Por favor, selecione outro.', 'Estabelecimento inválido ou indisponível.' e 'Erro ao processar dados de contato.' permanecem literais no arquivo (diff não as toca)
    - A string 'Não foi possível carregar os horários. Tente de novo.' existe em obterSlotsPublicos
    - `pnpm lint`, `pnpm test`, `pnpm build` verdes com saída real
  </acceptance_criteria>
  <done>Caminho público inteiro em admin client com projeção explícita, contrato por slug, copy preservada.</done>
</task>

<task type="auto">
  <name>Task 2: page.tsx + BookingApp.tsx — org_id fora do payload do browser</name>
  <files>src/app/book/[slug]/page.tsx, src/app/book/[slug]/BookingApp.tsx</files>
  <read_first>
    - src/app/book/[slug]/page.tsx (projeção de perfil nas linhas 63-72; tenantHash na 76)
    - src/app/book/[slug]/BookingApp.tsx (interface de props na linha 20; usos de perfil.tenant_id nas linhas 148, 173, 261; recuperação de double-booking na 274)
    - .planning/phases/01-hardening-da-superf-cie-p-blica/01-UI-SPEC.md §A (invariantes de identificador e payload)
  </read_first>
  <action>
    Em `page.tsx` (per D-04): remover `tenant_id: perfil.tenant_id` da projeção passada a BookingApp e passar a prop nova `slug={slug}` (o slug da URL, já validado como efetivo por obterDadosBookingPublico). `tenantHash={hashTenantId(perfil.tenant_id)}` fica EXATAMENTE como está — o hash continua derivando do org_id cru no servidor (invariante A2; derivar do slug quebraria o funil).

    Em `BookingApp.tsx`: remover `tenant_id` da interface do perfil; adicionar `slug: string` às props; trocar os três consumos — chamada `obterSlotsPublicos(slug, dataSelecionada, servicoSelecionado.duracao_minutos)`, array de dependências do useEffect (slug no lugar de perfil.tenant_id), e `criarAgendamentoPublico({ slug, ... })`. Nenhuma outra linha muda: estados, copy, classes, animações e a recuperação por substring da linha 274 ficam intactos.
  </action>
  <verify>
    <automated>pnpm lint && pnpm build && grep -c "tenant_id" "src/app/book/[slug]/BookingApp.tsx" | grep -qx 0 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "tenant_id" "src/app/book/[slug]/BookingApp.tsx"` = 0 (invariante A3)
    - `grep -n "hashTenantId(perfil.tenant_id)" "src/app/book/[slug]/page.tsx"` retorna exatamente 1 linha (invariante A2 preservado)
    - Com `pnpm dev` e um slug real: `curl -s http://localhost:3000/book/<slug> | grep -c 'org_'` = 0 (invariante A1) e o payload não contém `ativo`, `created_at` nem `updated_at` de serviços (invariante A4)
    - `curl -s -o /dev/null -w '%{http_code}' http://localhost:3000/book/inexistente` = 404 (invariante C11)
    - `pnpm lint` e `pnpm build` verdes com saída real
  </acceptance_criteria>
  <done>org_id fora do payload RSC; wizard funciona por slug; 404 real preservado.</done>
</task>

<task type="auto">
  <name>Task 3: JSDocs honestos — admin.ts e assinaturas.ts</name>
  <files>src/lib/supabase/admin.ts, src/lib/assinaturas.ts</files>
  <read_first>
    - src/lib/supabase/admin.ts (JSDoc atual das linhas 3-20 — enumeração numerada, "restrito a dois pontos")
    - src/lib/assinaturas.ts (JSDoc de obterPlanoVigentePublico, linhas 54-57 — fala do GRANT por coluna para anon, que deixou de existir no plano 01-01)
  </read_first>
  <action>
    Em `admin.ts` (per D-02, mitigação 3): atualizar o JSDoc de dois para três pontos autorizados, acrescentando o item 3 — leituras do booking público (perfil por slug, serviços ativos, plano vigente e engine de disponibilidade), com a justificativa: a role anon perdeu a Data API na Phase 1; toda query pública carrega filtro de tenant resolvido no servidor a partir do slug e lista explícita de colunas. Manter o tom do JSDoc atual (enumeração numerada, justificativa de negócio por ponto, aviso final sobre client components).

    Em `assinaturas.ts`: atualizar o JSDoc de `obterPlanoVigentePublico` — a variante não é mais servida pelo GRANT por coluna da role anon (revogado na Phase 1); ela agora recebe o cliente privilegiado dos caminhos públicos server-side, e o chamador é responsável por passar tenant_id resolvido no servidor. Nenhuma linha de código muda nos dois arquivos — só comentário.
  </action>
  <verify>
    <automated>pnpm lint && grep -c "dois pontos" src/lib/supabase/admin.ts | grep -qx 0 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "dois pontos" src/lib/supabase/admin.ts` = 0 e o JSDoc contém um item 3 mencionando leituras do booking público
    - O JSDoc de obterPlanoVigentePublico não afirma mais que a role anon lê assinaturas por GRANT de coluna
    - `git diff --stat` dos dois arquivos mostra apenas mudanças de comentário (nenhuma linha executável)
  </acceptance_criteria>
  <done>Documentação de código coerente com a superfície real.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→Server Action | slug e dados de contato chegam não confiáveis do cliente final |
| Server Action→Postgres (service role) | RLS bypassado — filtro de tenant vira responsabilidade do código |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-03 | Information Disclosure | payload RSC de /book/[slug] | high | mitigate | projeção explícita em page.tsx; tenant_id removido; grep org_ = 0 (Task 2) |
| T-01-04 | Elevation of Privilege | leituras públicas com service role | high | mitigate | .eq('tenant_id') resolvido do slug no servidor em toda query + colunas explícitas (Task 1); tenantId nunca vem do corpo da requisição |
| T-01-05 | Information Disclosure | erroSlots renderizado verbatim ao cliente final | medium | mitigate | única copy nova autorizada é a mandatória do UI-SPEC; proibido vazar slug/tenant/org_/código PostgREST |
| T-01-06 | Tampering | tenant gratuito exibindo personalização paga (bypass de RLS) | medium | mitigate | sanitização por plano em obterDadosBookingPublico preservada e verificada (UI#29/C13) |
</threat_model>

<verification>
- Greps de payload (org_ = 0, sem colunas extras de serviços)
- `pnpm lint && pnpm test && pnpm build` verdes
- Wizard completo por slug funcionando em dev (prova manual definitiva no plano 01-05)
</verification>

<success_criteria>
- Nenhuma leitura pública resta na role anon (pré-requisito do REVOKE total do plano 01-04)
- org_id fora do payload do browser e das assinaturas das actions públicas
- Copy e comportamento do wizard idênticos byte a byte
</success_criteria>

<output>
Create `.planning/phases/01-hardening-da-superf-cie-p-blica/01-02-SUMMARY.md` when done
</output>
