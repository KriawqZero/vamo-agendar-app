---
phase: 01-hardening-da-superf-cie-p-blica
plan: 05
type: execute
wave: 4
depends_on: ["01-02", "01-03", "01-04"]
files_modified:
  - docs/PENDENCIAS.md
autonomous: false
requirements: [SEG-01, SEG-02, SEG-03, SEG-04, SEG-05]

must_haves:
  truths:
    - "curl anônimo em /rest/v1/perfis_empresas não devolve a lista de profissionais — nem telefone_contato, nem o org_id do Clerk (critério 1 do ROADMAP, verificado com o script rodado, não afirmado)"
    - "POST anônimo em /rest/v1/agendamentos e /rest/v1/clientes é rejeitado, e o booking público continua funcionando exatamente como antes (critério 2)"
    - "curl anônimo em agendamentos e excecoes_agenda não devolve cliente_id nem o texto livre de motivo (critério 3)"
    - "Tabela nova no schema public não aparece na Data API sem GRANT explícito (critério 4 — provado no plano 01-04, reconferido aqui pelo script)"
    - "POST sem assinatura válida do QStash no webhook de lembrete é rejeitado com 401 — inclusive com o secret antigo em query string — e o boot de produção recusa subir sem QSTASH_NEXT_SIGNING_KEY, nomeando a variável (critério 5, prova empírica da assunção A1)"
    - "O payload RSC de /book/[slug] não contém org_ (invariante A1 do UI-SPEC, reconferido pós-integração)"
    - "docs/PENDENCIAS.md registra o handoff da Phase 2 (o repro do bug 'assume 30 minutos' mudou de lugar — Pitfall 8) e a observação da superfície remanescente de authenticated em servicos/horarios_funcionamento"
    - statement: "UI#6: tenant com 20+ serviços ativos faz a lista rolar no container da etapa sem quebrar o layout"
      verification: backstop
    - statement: "UI#13: horizonte_maximo_dias=30 alonga a fileira de datas (snap-x com scrollbar oculta no mobile, lg:grid-cols-7 no desktop) sem quebrar"
      verification: backstop
    - statement: "UI#17: nome de serviço longo na comanda de resumo não quebra o layout"
      verification: backstop
    - statement: "UI#22: nome de cliente muito longo no input e no resumo não quebra o layout"
      verification: backstop
    - statement: "UI#24: nome_estabelecimento longo na tela de sucesso não quebra o layout"
      verification: backstop
    - statement: "UI#31: nome_estabelecimento, descricao e endereco longos juntos no painel de marca não quebram o layout"
      verification: backstop
  artifacts:
    - "docs/PENDENCIAS.md atualizado (Definition of Done, regra 6)"
  key_links:
    - "script de superfície + wizard manual: os dois juntos são a prova da fase — CONTEXT §specifics: afirmação de que fechou sem o curl rodado não conta, e o wizard verificado à mão é regressão obrigatória e não negociável"
    - "lembrete em voo → webhook assinado (url: req.url): a prova ponta a ponta fecha a assunção A3 — um 401 legítimo aqui é falha silenciosa por design"
  prohibitions:
    - statement: "Não consertar de passagem o bug do 'assume 30 minutos' (booking-engine.ts:303) — é escopo da Phase 2 e mexer aqui confunde a verificação de lá (Deferred Idea explícita do CONTEXT)"
      status: flagged-unverified
---

<objective>
Portão de saída da fase: rodar TODAS as provas contra o sistema integrado — script de superfície anônima completo, boot fail-fast de produção (assunção A1), webhook 401 nos dois formatos de requisição inválida — registrar os handoffs em docs/PENDENCIAS.md, e submeter o booking público e o dashboard à regressão manual obrigatória do CONTEXT (§specifics: "fechou de verdade" é o critério, não "fechou o que o roadmap pedia").

Purpose: os cinco critérios de sucesso do ROADMAP são observáveis apenas contra o banco vivo e a aplicação rodando; e as regressões prováveis desta fase degradam em silêncio (agenda vazia, lembrete que não chega, personalização que some) — só o UAT humano as pega.
Output: fase provada critério a critério, com evidência de comando registrada e aprovação humana explícita.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-hardening-da-superf-cie-p-blica/01-CONTEXT.md
@.planning/phases/01-hardening-da-superf-cie-p-blica/01-UI-SPEC.md
@.planning/phases/01-hardening-da-superf-cie-p-blica/01-03-SUMMARY.md
@.planning/phases/01-hardening-da-superf-cie-p-blica/01-04-SUMMARY.md
</context>

## Artifacts this phase produces (plan 01-05)

- Nenhum símbolo de código novo — este plano só verifica, registra e submete a UAT
- Entradas novas em `docs/PENDENCIAS.md`: handoff da Phase 2 (Pitfall 8) e observação de superfície authenticated em servicos/horarios

<tasks>

<task type="auto">
  <name>Task 1: Verificação integrada — os cinco critérios contra o sistema vivo</name>
  <files>(nenhum arquivo modificado — execução de provas)</files>
  <precondition>Planos 01-02, 01-03 e 01-04 concluídos e aplicados; .env.local com as quatro variáveis do QStash (verificado pelo owner em 2026-07-21 — o executor usa o env sem lê-lo)</precondition>
  <read_first>
    - .planning/phases/01-hardening-da-superf-cie-p-blica/01-RESEARCH.md §"Verificação anônima da Data API" (códigos esperados) e §"Assumptions Log" (A1: comportamento do throw no boot é confiança MÉDIA — esta task é a prova empírica)
    - scripts/verificar-superficie-anon.sh
  </read_first>
  <action>
    Rodar e registrar (saída real no SUMMARY, comando a comando):
    1. `bash scripts/verificar-superficie-anon.sh` completo — exit 0 (critérios 1, 2, 3 e a reconferência do 4).
    2. Webhook (com `pnpm dev` de pé): POST sem header de assinatura → 401; POST em `/api/webhooks/lembrete?secret=secret-key` (regressão do fallback antigo, comandos prontos no RESEARCH §critério 5) → 401.
    3. Payload RSC: `curl -s http://localhost:3000/book/<slug real> | grep -c 'org_'` = 0; wizard responde 200.
    4. Prova da assunção A1 (boot fail-fast): `pnpm build` (deve passar SEM depender das chaves — o hook não roda no build) e depois `QSTASH_NEXT_SIGNING_KEY='' timeout 30 pnpm start; echo "exit=$?"` — o processo deve morrer no boot com mensagem contendo QSTASH_NEXT_SIGNING_KEY (a lista completa de ausentes pode conter outras variáveis se o .env.local local não tiver as 13 — o assert é a presença do nome da chave na mensagem e exit != 0). Se o processo SOBREVIVER (A1 falsa), PARAR e registrar: a fase não fecha o critério 5 sem guarda adicional — escalar em vez de improvisar.
    5. Definition of Done da fase: `pnpm lint && pnpm test && pnpm build` verdes com saída real.
  </action>
  <verify>
    <automated>bash scripts/verificar-superficie-anon.sh && pnpm lint && pnpm test && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - Script completo exit 0 com saída mostrada
    - Os dois curls de webhook retornam 401 (saída com http_code mostrada)
    - grep de org_ no payload RSC = 0 e /book/<slug> = 200
    - Boot de produção sem a chave: exit != 0 e mensagem contendo QSTASH_NEXT_SIGNING_KEY (registrado literalmente no SUMMARY)
    - `pnpm lint`, `pnpm test`, `pnpm build` verdes com saída real
  </acceptance_criteria>
  <done>Cinco critérios do ROADMAP provados por comando, com evidência registrada — nenhum "fechou" afirmado sem curl rodado.</done>
</task>

<task type="auto">
  <name>Task 2: Registrar handoffs e observações em docs/PENDENCIAS.md</name>
  <files>docs/PENDENCIAS.md</files>
  <read_first>
    - docs/PENDENCIAS.md (seção "🟠 Obrigatório antes do lançamento público" → "Integridade e pertencimento multi-tenant" — onde o hardening está listado e será marcado)
    - .planning/phases/01-hardening-da-superf-cie-p-blica/01-RESEARCH.md §Pitfall 8 (o repro do 'assume 30 minutos' mudou de lugar)
  </read_first>
  <action>
    Atualizar `docs/PENDENCIAS.md` (Definition of Done, regra 6) com três registros:
    1. Marcar o hardening da Data API e do webhook como resolvido na seção correspondente, citando a fase e a data.
    2. Handoff para a Phase 2 (Pitfall 8): com a leitura pública via service role, o embed servicos(duracao_minutos) devolve duração real também para serviço desativado — o fallback de 30 minutos de booking-engine.ts:303 deixou de disparar no caminho público. AGE-01/AGE-02 continuam necessários; o plano da Phase 2 não pode escrever repro que dependa do sintoma antigo no fluxo público.
    3. Observação de superfície (fora do escopo SEG, registrada para decisão futura): as policies compartilhadas de SELECT em servicos e horarios_funcionamento continuam permitindo a um usuário AUTENTICADO ler serviços/horários ativos de outros tenants — pré-existente, sem mudança nesta fase; a role anônima está bloqueada no GRANT.
    Lembrete já registrado no CONTEXT (não duplicar, só conferir que consta): remoção do ?secret= da URL de publicação reavaliada quando a fila do QStash drenar (14 dias).
  </action>
  <verify>
    <automated>grep -n "assume 30 minutos\|booking-engine.ts:303" docs/PENDENCIAS.md && grep -n "horarios_funcionamento" docs/PENDENCIAS.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - docs/PENDENCIAS.md contém o handoff da Phase 2 mencionando booking-engine.ts:303 e a mudança do repro
    - docs/PENDENCIAS.md contém a observação sobre SELECT authenticated cross-tenant em servicos/horarios_funcionamento
    - A entrada de hardening na seção "Obrigatório antes do lançamento público" referencia a Phase 1 como executada
  </acceptance_criteria>
  <done>Dívida e handoffs escritos onde a próxima fase vai procurar.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Checkpoint — regressão manual obrigatória (booking público + dashboard)</name>
  <what-built>Fase 1 completa: Data API fechada para a role anônima (leituras públicas via cliente privilegiado, contrato por slug), policies do dashboard substituídas por versões tenant-scoped, webhook de lembrete autenticado por assinatura do QStash, boot fail-fast com as chaves.</what-built>
  <how-to-verify>
    Com `pnpm dev` de pé (regressão manual obrigatória do CONTEXT §specifics):

    1. **Wizard completo de /book/[slug]** — do serviço até a tela de sucesso: escolher serviço → data/hora → nome + WhatsApp → confirmar → "Horário confirmado!". Conferir o agendamento aparecendo na agenda do dashboard. Nenhuma etapa, campo ou atraso novo (Fricção Zero).
    2. **Recuperação de double-booking (C5)**: duas abas no mesmo slot; a segunda deve voltar à etapa de data/hora com o aviso âmbar e a grade refeita — nunca uma caixa vermelha estática no formulário de contato.
    3. **Checklist do dashboard (Pitfall 3 — regressão silenciosa)**: agenda carrega com os agendamentos; novo agendamento manual salva; bloqueio/exceção salva; aba Perfil salva; serviços listam. Agenda VAZIA onde deveria ter dados = policy substituta errada.
    4. **Personalização por plano (C13)**: comparar um tenant Pro (cor/logo/capa aparecem) e um gratuito (não aparecem).
    5. **Lembrete ponta a ponta (assunção A3)**: criar agendamento com lembrete próximo (ou disparar o webhook com uma mensagem real do QStash) e confirmar que o lembrete chega — um 401 no log ("Assinatura QStash inválida") indica mismatch de URL atrás de proxy; plano B documentado no RESEARCH §Padrão 3.
    6. **Backstops visuais (UI#6/13/17/22/24/31)** — com dado extremo, se viável nesta sessão: 20+ serviços; horizonte 30 dias; nomes/descrição/endereço longos. O que não for verificado aqui fica human_needed no verify, nunca passa calado.
  </how-to-verify>
  <resume-signal>Digite "aprovado" ou descreva o que quebrou (item + comportamento observado)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| internet→Data API / webhook / páginas públicas | superfícies verificadas nesta rodada final |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-16 | Denial of Service | lembretes em voo rejeitados por mismatch de URL (proxy reescrevendo host) | medium | mitigate | prova ponta a ponta no UAT (item 5); sintoma e plano B documentados (montar URL de APP_URL após drenar a fila) |
| T-01-17 | Information Disclosure | regressão silenciosa de sanitização por plano expondo personalização paga | medium | mitigate | UAT item 4 comparando tenant Pro × gratuito (C13) |
| T-01-18 | Denial of Service | dashboard degradado em silêncio (agenda vazia) por policy substituta errada | high | mitigate | UAT item 3 — checklist explícito das cinco operações |
</threat_model>

<verification>
- Script de superfície + curls de webhook + boot fail-fast + DoD, todos com saída registrada
- UAT humano aprovado (wizard, dashboard, double-booking, personalização, lembrete)
</verification>

<success_criteria>
- Os cinco critérios de sucesso do ROADMAP verificados contra o sistema vivo, com evidência
- Regressão manual obrigatória aprovada pelo owner — "fechou de verdade"
- Handoffs da Phase 2 registrados
</success_criteria>

<output>
Create `.planning/phases/01-hardening-da-superf-cie-p-blica/01-05-SUMMARY.md` when done
</output>
